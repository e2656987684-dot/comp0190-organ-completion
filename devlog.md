# 开发日志 (devlog)

## 2026-06-25
- 目标：搭好项目骨架与 Git 仓库
- 做了：建仓库、目录结构、.gitignore、README、PROGRESS
- 结果：
- 决策：
- 下一步：
- 卡点：

## 2026-08-05
- 目标：数据从 50 对扩到 100 对之后，从"跑通"转向"提升效果"。
- 做了：
  - 数据准备：`notebooks/demo/MSN_train_skullfix.ipynb` 新增「0. 数据准备」cell，
    在 notebook 里直接调用 `src/data/prepare_skullfix.py`（不用再开终端切环境，
    `comp0190` 已并入 `comp0190-msn`），`N_SAMPLES` 默认改成 `0`（全部 100 对）。
    已跑过一次，`data/cache/skullfix_pairs_4096_6144.npz` 已是 100 对的缓存。
  - 复查了一次实际训练结果（`experiments/msn_skullfix/{run.json,history.csv}`），
    发现问题：`--minutes 3` 在 100 对数据下只够跑 19 个 epoch（比之前 40 对时測的
    "3分钟≈40epoch"少很多），且 val_loss 在 epoch 17 见顶（1.0950）之后 epoch 18
    已经回升（1.1946）才被 `--minutes` 掐停——没有任何机制检测到"已经过了最优点"，
    纯粹是运气好停在了没差太多的地方。`ModelCheckpoint(save_best_only=True)` 本身
    是对的，`best.h5` 存的确实是 epoch17 的权重，但训练进程自己不会提前收手。
  - 针对上面的问题，在 `src/models/train_skullfix.py` 里加了三处（对应基础提升
    清单里的 3/4/5 项），默认参数保持向后兼容，不加参数时行为和之前完全一样：
    - `--early-stop-patience`（默认 20，0 关闭）：加了
      `EarlyStopping(monitor="val_loss", restore_best_weights=True)`，
      val_loss 连续 N 轮不再变好就主动停，不用再赌 `--minutes` 蒙对时机。
    - `--n-folds` / `--fold`（默认 0，即维持原来的单次 `--val-frac` 随机切分）：
      100 个样本单次 80/20 切分的验证集只有 20 个颅骨，噪声偏大；加上这两个参数
      之后可以做 k-fold 交叉验证，多切几份取平均，而不是信一次运气。
    - `--run-name`（默认空，行为不变）：`--out-dir` 下的可选子目录，为了以后要
      重新扫收敛曲线（换几个 `--minutes` 值多跑几次）时，各次跑的
      `history.csv`/`best.h5` 不会互相覆盖。notebook 里读权重的 cell 硬编码的是
      `experiments/msn_skullfix/best.h5`，所以默认（不传 `--run-name`）时输出路径
      完全不变，不会破坏现有的评估 cell。
- 结果：（改动已过语法检查和 `--help` 检查，还没有实际重新跑训练验证效果，见下一步）
- 决策：这一轮只动了"基础提升"里风险最低、复用价值最高的三项（早停、k-fold、
  防覆盖的 run 目录），架构层面的改动和数据增强先不动，等这三项验证过再说。
- 下一步（按之前讨论的优先级，留作待办）：
  - 【基础，未做】给模型加正则化（dropout / AdamW 的 weight decay）——187M 参数
    对 80 个训练样本，过拟合目前几乎没有约束。
  - 【基础，未做】`cd_dcd_loss` 的 `dcd_weight` 目前硬编码 1.0
    （`msn_skullfix.py` `cd_dcd_loss`），可以暴露成 CLI 参数或做成随训练进度从
    0 斜坡升到 1。
  - 【基础，未做】用新加的 `--run-name` 实际跑一遍收敛曲线扫描（比如
    `--minutes` 取 5/10/20/30），更新 notebook 里那张已经过时的"3/5/7分钟"表。
  - 【基础，未做】数据增强：绕 SI 轴小角度旋转 + 轻微 jitter（注意大角度旋转会
    破坏颅骨在 LPS 坐标系下的方向先验）。
  - 【进阶，未做】用 `msn_downloads/MSN_weights3.h5`（作者发布的预训练权重）做
    微调而不是从零训练，100 个样本对 187M 参数依然是重度记忆化体量。
  - 【进阶，未做】解码器目前只吃编码器 max-pool 出来的一个全局向量
    （`build_decoder`），缺损颅骨里本来完好的表面也被整个重新生成，没有原始
    MSN 论文里"可见区域直接透传 + minimum density sampling 合并"那一步，这项
    如果做，预期收益在几个改动里最大，但工作量也最大。
  - 【进阶，未做】`PointSampler(mode="unique")` 是不重复随机采样，不是真正的
    FPS；数据准备阶段已经在用 `fpsample`，可以把同样的思路搬进 encoder 的
    `E-SG1`/`E-SG2` 下采样步骤。
  - 【进阶，未做】在 `paper()`（187M）和 `small()`（9.4M）之间找一个更匹配
    100 样本规模的中间容量，而不是默认焊死用最大配置。
  - 【数据工程，未做】接入 SkullBreak（README 里提到的目标数据集之一），从根本
    上缓解样本量不足，比任何单项建模改动的预期收益都大。
- 卡点：以上三项代码改动还没有实际跑一次训练验证是否达到预期效果（尤其是
  EarlyStopping 会不会因为 100 个验证样本本身噪声大而提前停得太早，
  patience=20 是拍的一个保守值，需要实跑后再调）。

- 追加：把 `--minutes`/`--epochs` 的默认值也重新配平了。之前 `--epochs` 默认
  10000、`--minutes` 默认 55，10000 从来没被跑到过、纯粹是摆设。现在
  `EarlyStopping` 已经是默认开启的真正停止信号，把两个默认值都调整为：
  `--epochs` 300（按 ~9.5s/epoch 算，全跑满约 47 分钟，仍然远高于预期的实际
  收敛点，只是不再是 10000 那种不现实的数字），`--minutes` 90（比 300 epoch
  需要的时间更宽松，正常情况下不会是它先触发，只在 EarlyStopping/epoch 上限
  都没起作用时兜底）。notebook 训练 cell 里的 `MINUTES` 变量也同步从 3 改成
  90，不然 cell 会一直显式传 `--minutes 3` 把脚本的新默认值覆盖掉。
  单折预计训练时长：EarlyStopping 触发前大概率在几十分钟以内结束（对着
  40 样本时"前20 epoch吃掉95%进步"的经验外推，100 样本单折不做 k-fold 时估计
  10~15 分钟量级，但这仍是未经验证的推测）；如果之后开 k-fold，总时长要乘以
  折数，且 `train_skullfix.py` 本身不会自动循环所有折，跑完整个 k-fold 需要
  手动改 `--fold`/`--run-name` 跑多次。

## 2026-08-05（补记：这次改动一度被"放弃更改"整体还原）
- 情况：`train_skullfix.py`、`devlog.md`（这份文件本身）、以及 notebook 里的
  「0. 数据准备」cell 和 `MINUTES` 改动，一度被整体还原回了改动前的版本（大概率
  是编辑器里误点了 Discard Changes，而不是 git 层面的操作——当时 `git status`
  显示这几个文件在 git 里从未被 commit 过，本来就该停留在"已修改未提交"状态）。
  发现后已经把上面记录的内容原样重新应用了一遍。
- 决策：这几个文件目前都还没有被 git 提交（`git status` 会显示为 modified）。
  如果不想再经历一次"改动消失"，跟用户确认后可以考虑先 `git add` + `git commit`
  一次，把这一轮改动落到 git 历史里，这样以后即使编辑器里误 discard，也能从
  git 历史里找回，而不是只存在于当前工作目录里。

## 2026-08-05（数据准备卡死：worker 内存无限增长 + OOM 重启死循环）
- 现象：`prepare_skullfix.py --workers 8` 跑了 18 分钟没结束，`.npz` 一直没出现
  （`np.savez` 是最后一步，所以中途看不到半成品）。用户感觉"卡住了"。
- 诊断（实测证据）：
  - worker 的 RSS 随它处理过的任务数**单调增长**，且新 worker 不断被补进来。
    抓到的一张快照：最初那批 worker（14:16 启动）已经涨到 7.2 GB / 7.5 GB，
    14:26/14:27/14:29/14:30 陆续新建的分别是 3.6 / 2.8 / 2.1 / 1.6 GB，
    而 14:34 刚生成的那个只有 3 MB —— 年纪越大占用越高，说明内存只涨不跌。
  - 本容器的 **cgroup 内存上限是 42.8 GiB**，不是 `free -h` 显示的 187 GiB
    （`free` 报的是宿主机，不是配额）。8 个这样的 worker 撑不到跑完就会撞上限，
    被 OOM 杀掉，`Pool` 再默默补一个新的 —— 于是陷入"杀掉→重启"的死循环。
  - 更致命的是 `pool.imap` 会**永久等待**随 worker 一起死掉的那个结果，
    所以这个任务不会自己结束，只会一直耗着。
- 根因：`Pool(args.workers)` 用了默认的 `maxtasksperchild=None`，worker 永不重启；
  每个任务瞬时要分配 248 MB 的 int32 体数据 + 114 万顶点 / 227 万面的网格 +
  采样中间量（峰值 1~2 GB），glibc 又不把释放的堆还给操作系统，于是 RSS 累积。
- 修复：`Pool(args.workers, maxtasksperchild=1)` —— 每做完一对就重启 worker，
  峰值被钉在"单任务工作集 × worker 数"（8 个约 16 GB，安全）。代价是每个任务多
  一次进程 spawn（约 0.2 秒），相对 2~3 秒的任务耗时可忽略。
- 同时修正了一个**之前给错的建议**：早先我按"48 逻辑核 / 内存充足"推荐把 workers
  开到 32~40，这是错的。这个负载是**内存带宽密集型**而非算力密集型，实测 24 对：
  独占 6.9s/对，4 workers 3.9s，8 workers 3.1s，12 workers 3.4s，24 workers 4.6s
  —— 12 比 8 慢、24 比 4 还慢，整台 24 核机器最多只能榨出约 2.3 倍加速。
  已把脚本 `--workers` 默认值和 notebook 的 `WORKERS` 都定在实测最优的 **8**，
  并把这组数字写进脚本 docstring，避免以后又凭核数拍脑袋调大。
- 另一个值得记的坑：tqdm 经 subprocess 管道打到 notebook 里时，`pool.imap` 按序
  回收结果 → 进度条会先静默十几秒再一次性跳几格，而它显示的**第一个速率**把全部
  启动开销都算到那 1 个样本头上（实测小样本跑出 `15.22s/it`，收敛后其实是
  `3.21s/it`）。之前误判"一对要一分钟"就是被这个读数骗了，不是真实吞吐。
- 明确不做：`marching_cubes(step_size=2)` + 体数据转 uint8 这组优化（实测能在多
  worker 场景快 1.6 倍，且精度损失已验证等同于采样噪声地板）**按用户要求不加**，
  保持现在的 step_size=1 / int32 路径不变。
- 卡点：卡死的那个进程组需要手动 kill（Claude 这边执行 kill 被权限策略拦下了）。

## 2026-08-05（预训练权重基线：新建 notebook + 一个静默失败的坑）
- 目标：把作者发布的 `MSN_weights3.h5` 在**已修复对齐**的数据上重跑一遍，
  作为和本项目从零训练结果对比的基线。
- 踩到并修正的一个错误判断（重要）：我先前建议"把预训练权重加载进
  `msn_skullfix.py` 的 `paper()`，两边走同一套代码"，**这是错的**。实测：
  `paper()` 与 `MSN_weights3.h5` 只有 3 个权重组能按名字匹配上
  （`D-OUT_lin`/`D1-IN`/`D2-IN`，共 32 组），而且
  `load_weights(by_name=True, skip_mismatch=True)` **不报错就返回** ——
  约 96% 的网络仍是随机初始化，会安静地产出垃圾预测。
  根因是层嵌套方式不同、不是形状不同：demo 的 `LBR` 把 Dense+ReLU 包在名为
  `E-IN_LBR1` 的嵌套 Model 里（存成 `E-IN_LBR1/E-IN_LBR1_lin/kernel`），
  重写版是顶层裸 Dense `E-IN_LBR1_lin`（找 `E-IN_LBR1_lin/kernel`），
  `by_name` 匹配不了。注意力块同理（`E-SA1` vs `E-SA1_Q/_K/_V`）。
  `msn_skullfix.py` docstring 里"paper() can load MSN_weights3.h5"那句话
  需要更正，目前先在 `msn_demo_arch.py` 的 docstring 里写清楚了。
- 做了：
  - 新增 `src/models/msn_demo_arch.py`：把 demo notebook 的架构定义
    （cells 4/6/8/10/12）**逐字**抽出来成模块。验证过它能以**严格模式**
    （不传 by_name / skip_mismatch）加载 `MSN_weights3.h5`，40/40 权重张量确实被改写。
    这个文件里的层名是有语义的，不要"顺手清理"。
  - 新增 `notebooks/MSN_baseline_pretrained.ipynb`：读已对齐的 `.npz`
    （不再碰 `.ply`，也**不再调 `normalize_point_cloud`**）、只评估训练时那
    20 颗验证集颅骨、指标乘各自的 `scale_mm` 出毫米、内置权重加载自检。
  - 更新 README 的仓库结构说明（原来那份是最初的骨架规划，和现状对不上），
    补了"两条数据路线（.npz vs .ply）分别是什么、该用哪条"的对照表。
- 结果（冒烟测试，3 颗验证颅骨）：预训练权重 CD_t 约 9.7 / 10.1 / 11.1 mm，
  本项目从零训练是 7.08 mm。完整 20 颗的数字要跑完 notebook 才有。
- 待确认：`MSN_weights3.h5` 训练时是否见过颅骨类数据。这决定了对比表里该写
  "zero-shot 迁移"还是别的表述，写进论文前必须查证原作者说明。
- 已知未解决：demo 的 `UniformSampler` 是有状态随机抽样，同一输入两次调用输出
  会有差异（训练 notebook 实测 1.03）。基线 notebook 里固定了全局种子，但要
  完全逐位可复现需要改成 stateless 抽样 —— 那属于改动 demo 架构，没做。

## 2026-08-05（流程决策：迭代期用单折，定稿后再跑 5 折）
- 决策：`--n-folds` 的代码已经就位，但**迭代期不开**，继续用默认的单次 80/20
  划分（seed 42）。等所有改动（正则化 / dcd_weight / 数据增强 / 可见区域透传 等）
  都定稿之后，再跑一次 5 折出最终数字。
- 理由：5 折是单折的 5 倍时间，每改一版就重跑 5 折纯属浪费。
- 副作用（必须记住，否则最后会误判）：
  - 单折验证集只有 20 颗，噪声不小 —— 上次曲线里 epoch 112→113 的 val_loss
    从 0.9243 跳到 0.9908（单轮 7%），折合 CD_t 约 0.4mm 的抖动。
    **迭代期只有 1mm 以上的变化才算可信，+0.3mm 级别的"改进"大概率是噪声。**
  - 反复对着同一批 20 颗决定"改动留不留"，本质是拿验证集做选择，改的次数多了
    会隐性过拟合到这 20 颗。**最终 5 折的数字大概率会比迭代期看到的差一些，
    这是正常的**，5 折那个才是真数字。
  - 迭代期保持 seed=42 不变，至少保证各版改动之间横向可比；不要中途换 seed。

## 2026-08-05/06（预训练基线跑完 + 表面质量工具 + 一次前提被数据推翻）

### 1. 预训练权重基线：跑完了，本项目模型明显更好
`notebooks/MSN_baseline_pretrained.ipynb` 在**同一批 20 颗验证集**上跑完
`MSN_weights3.h5`，结果存 `experiments_log/pretrained_baseline/eval_val20.csv`：

| | CD_t | CD_p | DCD |
|---|---:|---:|---:|
| 预训练权重（基线） | **10.71 mm**（中位 10.80，std 0.91，范围 8.40~12.25） | 23.62 | 1.428 |
| 从零训练（本工作） | **7.08 mm** | — | — |

本项目模型好 **3.63 mm（34%）**，且基线 std 只有 0.91mm、20 颗全落在
8.4~12.2mm，说明这个差距是稳的，不是抽样运气。

⚠️ 这个对照的性质仍需在论文里写准：是"预训练权重直接应用于颅骨"
vs"在颅骨上从零训练"，**不是**同任务下两个方法的较量。
`MSN_weights3.h5` 训练时是否见过颅骨类数据仍未查证（见前面条目的待办）。

顺带修了个 bug：基线 notebook 的 `REPO` 抄了 `notebooks/demo/` 下的 `"../.."`，
但它自己在 `notebooks/` 下，少一级，实际解析到 `/root` 导致 import 失败。
已改成从 cwd 向上搜索 `src/models/`，不再依赖 notebook 放在第几层。

### 2. 新增表面质量工具（`src/eval/mesh_viz.py` + `MSN_surface_quality.ipynb`）
起因：散点图看不清预测表面好不好，想转成 mesh 看，并用于"改训练代码前后"对比。
原仓库（MedShapeNet）翻过了，**没有任何 mesh 重建/平滑/渲染代码**，纯点云项目，
所以全部自己实现。

方案：点云 → KD-tree 距离场 → 高斯平滑 → marching cubes → 取最大连通块 → Taubin 平滑。
**没用 Poisson**：它需要逐点法向（预测点云没有，且颅骨是薄骨壳、内外表面法向互相
打架），而且天生倾向补洞 —— 缺损那个洞恰恰是要观察的对象。约 0.7s/个点云。

参数（`RECON = res 128 / radius_mm 6.0 / sigma 2.5 / taubin 60 / pad 0.14`）是
**针对本项目数据实测定的，不是任何标准**：量点间距（GT 中位 3.8mm、p95 5.1mm）→
radius=3.0 试出是海绵 → 扫 4~7mm 定出可用窗口 5~6mm（下限球要连上、上限不能糊住
约 20mm 的缺损洞）→ 加平滑消鹅卵石纹理。换数据集要重新量。

两条硬规则（已写进模块 docstring 和 notebook）：
- **重建 mesh 只能看，不能算指标**。实测原始点到重建表面中位外扩 5.3mm、p95 12.0mm，
  和模型自己的 CD_t（~7mm）同量级，在 mesh 上算指标会被重建伪影主导。
- **跨版本对比时重建参数必须锁死**。每个旋钮都改变"看起来多平滑"，逐图调参会让
  参数变化伪装成模型改进。故 `RECON` 写成模块级常量；探索单个模型用 `PRESETS`
  四档（raw/light/default/heavy）或直接传 override。

一个反直觉但实测的结论：**平滑加得越多，模型间差异反而越明显**。低平滑时 GT 和
预测都渲染成同一堆小球，重建自身纹理淹没了信号；加大平滑滤掉这层纹理，剩下的
低频起伏才是模型真正的差距。（我最初的判断正好相反，是被实验推翻的。）

### 3. ⚠️ "预测表面不够平滑、GT 应该很平滑"这个前提被数据推翻
原计划是加一个平滑惩罚项让预测更平滑。实测后**这个方向被否掉了**：

- **局部粗糙度（按点间距归一化）：GT 0.736 vs pred 0.760 —— 基本持平。**
  朴素的局部平面度惩罚项没有可测量的收益空间，压过头还会让表面比 GT 还平而损害 CD。
  （注意此指标被"颅骨是有厚度的骨壳、内外表面相距 5~7mm"污染，所以也**不能**
  反过来断言"GT 不平滑"，只能说两者没有可利用的差距。）
- **真正确凿的差距是点密度均匀性**：
  ```
  最近邻<2mm 的点占比   GT 0.0%    pred 11.2%
  间距变异系数 CV       GT 0.146   pred 0.389   (2.7 倍)
  间距范围(mm)          GT 3.56~7.30   pred 0.218~12.70
  ```
  GT 是 FPS 采样的，天然均匀、**一个近于 2mm 的点都没有**；预测有 11.2% 的点
  几乎重合，等于白白浪费、覆盖不到新表面。这是普遍现象而非几个离群点。
- **有符号偏差**：高于表面 49.3% / 低于 50.7%，对称无系统性偏移，
  |偏差| 中位 2.09mm、p95 9.54mm、std 4.30mm。这是 mesh 上那些波浪起伏的来源，
  属于精度问题（该靠 CD 压），不是"局部粗糙"。

**验证并否定的一个假设**：怀疑 6144 = 1024 seed × 6 的解码结构会让点 6 个一组扎堆。
实测第 1~7 近邻距离，pred/GT 比值平滑变化（0.82/0.94/0.96/0.96/0.94/0.93/0.93），
**第 6 近邻没有突跳**，假设不成立。扎堆不是这个架构原因。

### 4. 顺带验证了当初换 `cd_dcd` 的理由（用户问起，记录备查）
澄清一个记忆偏差：损失从 `dcd` 换成 `cd_dcd` 和"对齐"**没有关系**，是两件独立的事
（fix A = 数据预处理的配对对齐；fix C = 损失）。换损失的原因是 DCD 从随机初始化
训不动。今天补测的梯度（合成随机点云，比模型真实初始化温和，属弱化版验证）：
```
远离目标：dcd |grad|=4.6e-05   cd 1.2e-04   cd_dcd 1.6e-04
已接近：  dcd |grad|=7.5e-05   cd 1.3e-04   cd_dcd 2.0e-04
```
DCD 梯度两种情况下都最弱（约 CD 的 1/2~1/3），`cd_dcd` 都最强，方向与原结论一致。
而且对当前目标很关键：**DCD 恰恰是负责密度均匀的那一项**，要改善密度需要
**加强**它，而不是退回纯 DCD（那样连形状都拉不对）。

### 5. 下一步（优先级已按"能否跑赢噪声"重排）
1. **`dcd_weight` 暴露成 CLI 参数**（最便宜，`cd_dcd_loss` 里现在硬编码 1.0），
   扫 1/2/5/10，看 `扎堆<2mm` 能否从 11.2% 往 0% 压、`间距CV` 能否往 0.146 靠。
2. **Repulsion loss**（PU-Net 那套）—— 最对症：直接惩罚"两预测点距离小于阈值"，
   阈值可照 GT 分布定（~3.5mm）。实现约十几行，与现有损失是加法关系。
3. 正则化（dropout / weight decay）、可见区域透传、数据增强 —— 见前面条目。
4. **平滑惩罚项 —— 保留（导师提出），但排在 1、2 之后做**。第 3 节的测量只否定了
   "在 k=16 邻域这个尺度上还有便宜可捡"，**没有否定这个方向本身**。详见下一节。
5. **不做**：Uniform loss（比 repulsion 复杂、收益重叠）、后处理 FPS（绕过问题而非解决）。

### 7. 平滑惩罚项：反对与支持的理由（导师提出，保留待做）
先记结论：**值得做，但要排在 `dcd_weight` 和 repulsion 之后，且必须先想清楚怎么评判它。**

**不急着做的理由**
- 在 k=16 邻域尺度上，pred(0.760) 与 GT(0.736) 归一化粗糙度基本持平，
  这个尺度上没有可测量的差距可捡。
- 该指标被"颅骨是骨壳、内外表面相距 5~7mm"污染，**目前我们甚至没有一个可信的
  粗糙度度量**。没有度量就没法判断改动是否有效 —— 这是最实际的障碍。
- 压过头会让预测比 GT 还平，CD 反而变差（GT 就是目标，比目标"更好"在指标上仍是误差）。
- 机会成本：密度不均有 11.2% vs 0.0% 这么大且确凿的缺口，性价比明显更高。

**支持做的理由（这些不能忽视，导师的直觉有道理）**
- **我的测量只在一个尺度上。** 粗糙度是尺度相关的量，mesh 上肉眼可见的波浪起伏
  在 10~20mm 尺度，k=16 邻域（约 4~8mm 跨度）**根本看不到它**。
  换个更大的邻域去度量和惩罚，结论可能完全不同。
- **Chamfer 对表面质量出了名的宽容。** CD 只看最近邻距离，
  **不惩罚"点对称地散布在真实表面两侧"这种毛糙**（实测正是如此：高于 49.3%
  / 低于 50.7%，|偏差| p95 9.54mm）。两朵 CD 相同的点云可以观感差别很大。
  平滑先验提供的是 CD 里**没有的信息**，这是支持它最强的论据。
- **GT 本身不是完美目标。** GT 来自二值体素的 marching cubes，带阶梯状伪影。
  如果模型输出的表面比 GT 更光滑，**在解剖学上可能更正确**，即使 CD 略微变差。
  ——若走这条路，必须在论文里把"指标变差但质量更好"这件事讲清楚，
  不能只报 CD 就完事。
- 实现是**加法项 + 权重**，`weight=0` 即退回当前行为，风险有界、可随时关掉。

**若要做，建议这样设计（避免白做）**
1. 先补一个**多尺度**粗糙度度量（k=16 / 48 / 128 各测一遍），
   确认在哪个尺度上 pred 真的比 GT 差 —— 没有这个，改完无法判断成败。
2. 惩罚项形式二选一：kNN 图的 Laplacian（点到邻域质心的距离）
   或法向一致性（相邻点局部法向的夹角）。前者简单，后者更贴"表面光滑"的直觉。
3. 权重做成 CLI 参数，扫 0（对照）/ 小 / 中 / 大。
4. 评判**不能只看 CD**：同时报多尺度粗糙度、密度 CV、以及锁定参数的 mesh 图。
   预期是 CD 略升而粗糙度明显降 —— 这个 trade-off 本身就是可写进论文的结果。

### 6. 本轮 commit
`4c2e008` 早停/k-fold/OOM 修复 · `d4167dc` 预训练基线 notebook + 权重加载陷阱
`dc27a8c` 路径修复 · `bf7a857` 表面质量工具 · `9479151` 预设+平滑阶梯
`373aa97` ladder 裸字符串修复 · `2b7aa80` RECON 值写进 notebook
tag `baseline-7.08mm` 标记 7.08mm 那次的代码状态。
⚠️ 这些 commit **都还没 push**（本机无 GitHub 凭据，需手动
`git push origin main && git push origin baseline-7.08mm`）。

## 2026-08-06（实验：dcd_weight 1 → 3）

### 结果
产物在 `experiments/msn_skullfix/dcd_w3/`。
⚠️ 路径比预想的多一层：`--run-name` 是 `--out-dir` 下的子目录，
而 `--out-dir` 默认是 `experiments/msn_skullfix`，所以不是 `experiments/dcd_w3/`。
在 `MSN_surface_quality.ipynb` 的 `MODELS` 里要写 `("dcd_w3", "msn_skullfix/dcd_w3")`。

| 指标 | baseline (w=1) | dcd_w3 (w=3) | 变化 | GT |
|---|---:|---:|---|---:|
| **扎堆<2mm** | 12.6% | **9.7%** | **-2.9 个百分点** ✅ | 0.0% |
| **间距 CV** | 0.388 | **0.369** | -0.019 ✅ | 0.145 |
| \|偏差\|中位 | 2.00mm | 2.25mm | +0.25mm ❌ | — |
| \|偏差\|p95 | 6.56mm | 7.28mm | +0.72mm ❌ | — |
| val CD_t | 7.08mm | 7.41mm | +0.33mm ❌ | — |
| epochs | 133 | 77 | 早停更早 | — |

（密度指标取 8 颗验证集颅骨的均值；CD_t 取各自 run.json 的 best。）

### 解读
- **机制有效，方向没错**：扎堆 12.6%→9.7%、CV 也降，证明 `dcd_weight` 确实按预期起作用。
- **但兑换率很差**：权重 1→3（梯度占比 36%→63%，接近翻倍）只换来 2.9 个百分点，
  而离 GT 的 0.0% 还差 9.7 个百分点。按这个比例，要压到 GT 水平所需的权重会让 CD 先崩。
- **耦合问题被证实**：精度确实被牺牲（CD_t +0.33mm、偏差 p95 +0.72mm）。这印证了
  `dcd_weight` 会把 DCD 的距离因子和密度因子**一起**放大，不是干净的密度旋钮。
- ⚠️ **但这些变化都在噪声范围内**：单折 20 颗的噪声约 0.4mm，CD_t 只变了 0.33mm，
  **"变差"不能当定论**。密度那 2.9 个百分点相对可信些（密度指标不像 CD 那样受划分影响），
  但也不宜过度解读。

### 结论
不值得再往上调 weight，兑换率已证明太差。下一步二选一：
- `--dcd-lambda 2`（weight 留默认 1）：lambda 只作用于密度因子、不放大距离因子。
  若能用更小的精度代价换到同等密度改善，就证实耦合是瓶颈 —— 这个对比本身是个干净的结论。
- repulsion loss：只看预测点之间的距离，与 CD 完全正交，理论上没有这个 trade-off。

---

## 2026-08-06（实验：dcd_lambda 1 → 2）

产物在 `experiments/msn_skullfix/dcd_l2/`。weight 留默认 1，只动 lambda，
目的是验证"lambda 只作用于密度因子、不放大距离因子"这个假设。

### 三次跑的完整对比

同一份 `.npz`、同一 seed 42 划分、同一 20 颗验证颅骨；
密度/偏差取 8 颗验证颅骨均值（`mesh_viz.surface_stats`），CD_t 取各自 `run.json` 的 best。

| | weight | lambda | epochs | val CD_t | 扎堆<2mm | 间距CV | \|偏差\|中位 | \|偏差\|p95 | 偏差std | 外侧占比 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `baseline_es20` | 1 | 1 | 133 | 7.076mm | 12.6% | 0.388 | 2.00mm | 6.56mm | 3.25mm | 48.3% |
| `dcd_w3` | **3** | 1 | 77 | 7.406mm | **9.7%** | **0.369** | 2.25mm | 7.28mm | 3.63mm | 50.1% |
| `dcd_l2` | 1 | **2** | 149 | **6.945mm** | 12.1% | 0.384 | **1.92mm** | **6.18mm** | **3.08mm** | 51.4% |
| GT 参照 | — | — | — | — | 0.0% | 0.145 | 2.28mm | 3.76mm | 2.42mm | 49.3% |

### 解读：lambda 没做到它该做的事，却做成了另一件事

**两个旋钮的作用完全不同，而且都不是文档上写的那样：**

| | 密度（扎堆/CV） | 精度（CD_t/p95/std） |
|---|---|---|
| `dcd_weight` 1→3 | **明显改善** 12.6→9.7% | **变差** 7.08→7.41mm |
| `dcd_lambda` 1→2 | **几乎没动** 12.6→12.1% | **改善** 7.08→6.94mm |

lambda 名义上是密度因子的指数（`1/count^λ`），调大应该更狠地罚扎堆。
**实测密度纹丝不动**（12.6→12.1%、CV 0.388→0.384，这个量级就是噪声），
改善的全在精度上，而且是四项同向（CD_t、偏差中位、p95、std）。

**一个未验证的解释**：λ=2 让 `1/count^λ` 对严重扎堆的点衰减得太快，
密度因子趋近 0、该点损失顶到上界 1，**梯度反而死了**；
于是优化压力回流到距离因子 `exp(-α·dist)` 上，表现为精度变好、密度不变。
要证实需要测扎堆点上的梯度，尚未做。

### 噪声核查（重要）

逐 epoch 抖动比改善量还大，所以做了三项核查：

| | min(全程) | min(前133轮) | 末30轮均值 | 末30轮 std |
|---|---:|---:|---:|---:|
| baseline | 7.076 | 7.076 | 7.432 | **0.234** |
| dcd_l2 | 6.945 | 6.945 | 7.331 | **0.257** |

- **改善量 ~0.10–0.13mm < epoch 间 std 0.234mm**，单看任一项都不能算显著。
- **不是靠多跑轮数捡到的**：dcd_l2 最优在第 129 轮，落在 baseline 的 133 轮范围内，
  不存在"多跑 16 轮多摇 16 次骰子"的偏差（min-over-epochs 是有偏估计，必须查这一条）。
- **早停干净触发**：129 轮最优 + 正好 20 轮 patience = 149 停，没撞 90 分钟预算被截断。

### 结论

- **保留 λ=2 作为新默认**：四个精度指标同向改善、无一退化。全部同向比单项显著更可信，
  但幅度仍在噪声边缘，**不要在论文里写成确证**，需要 k-fold 才能定性。
- **但别把它写成"密度优化"**，它是精度微调。密度那个大坑（12.1% vs GT 0.0%）基本没动。
- `dcd_w3` 定性清楚了：weight 是**密度↔精度的兑换旋钮**，兑换比不划算，弃用。
- **DCD 两个旋钮都够不着密度问题**（一个换不起、一个不起作用）→
  印证了该换方向：不再调 DCD 超参，转 roadmap。

---

## 2026-08-06（查证原项目：论文数据、可比性、以及一个关键的正确性验证）

起因：想知道原项目是不是只用 DCD、他们的密度是多少、能不能直接对比数字。
以下每条都标了出处，可自行复核。

### 出处清单
| 内容 | 网址 |
|---|---|
| 原仓库 | https://github.com/jrHoss/MedShapeNet-Foundation-Model |
| 论文 PDF（就在仓库里，23 页） | https://github.com/jrHoss/MedShapeNet-Foundation-Model/blob/main/MedShapeNet_Foundation_Model.pdf |
| 损失函数实现 | https://github.com/jrHoss/MedShapeNet-Foundation-Model/blob/main/utils/loss.py |
| 训练脚本 | https://github.com/jrHoss/MedShapeNet-Foundation-Model/blob/main/train.py |
| 论文（ResearchGate 版） | https://www.researchgate.net/publication/384968432_A_MedShapeNet_Foundation_Model_-_Learning-Based_Multimodal_Medical_Point_Cloud_Completion |
| DCD 原论文 (Wu et al., NeurIPS 2021) | https://arxiv.org/abs/2111.12702 |

### 1. ⭐ 一个可以写进论文的正确性验证

论文 Table 1 报的 **DCD = 1.41269**（他们自己的 20,962 个验证点云）。
本项目用他们的权重在**颅骨数据**上推理，测得 **DCD = 1.42772**
（`experiments_log/pretrained_baseline/eval_val20.csv`，20 颗均值）。

**两者相差 1%。** 这同时证明三件事：
1. `msn_skullfix.calc_dcd` 是原实现的忠实移植 —— 数值层面复现了论文；
2. `MSN_weights3.h5` 加载正确（呼应 `msn_demo_arch.py` 那个静默失败的坑）；
3. 整条评测管线可信。

**这个数字应该写进论文的方法学章节**，它是"我们的复现是对的"最直接的证据。

### 2. 原项目确实只用 DCD，而且从没调过 α / λ

`train.py`：`loss = calc_dcd`，Adam `lr=1e-7`，`batch_size=8`，`epochs=500`，
ReduceLROnPlateau(factor 0.9, patience 5)。
`loss.py` 里 `calc_dcd(x, gt, alpha=1, n_lambda=1, ...)` —— **train.py 调用时不传任何覆盖值**。

含义：
- 本项目的 `baseline_es20`（w=1, λ=1）在这两个参数上**与原项目一致**；
- 原项目**没有提供任何"调 λ 有用"的先例**，`dcd_l2` 那次实验（以及"λ 改善精度而非密度"
  这个反直觉结论）是本工作新做的。

### 3. 论文报的数字

| | Table 1（全量） | Table 2（4,800 形状子集） |
|---|---:|---:|
| 训练数据 | 200,000 点云 / 240 类 | 4,800 形状 |
| 验证集 | 20,962 点云 | — |
| CD | 0.00170 | 0.002327 |
| DCD | 1.41269 | 1.60467 |
| F-score @0.05 | 0.9370 | 0.89202 |
| F-score @0.03 | 0.7408 | 0.6234 |

Table 2 是他们和 FC / PCN 的对比（PCN 输出 6,084 点、GT 6,144 点，数量不匹配所以 DCD 记 N/A）。
**Table 2 那一列是和本项目处境最接近的参照**（小数据量），但本项目目前不算 F-score，无法并排 —— 见待办。

训练配置：**6× RTX A6000（各 40GB）+ 8 CPU + 1000GB RAM，约六周**，
322 epoch 后因连续 12 轮无改善停止。对照本项目：单卡、约 20 分钟、149 epoch。
**算力差约三个数量级** —— 引用他们的绝对数字时必须交代这一点。

### 4. ⚠️ CD 不能直接跨项目比（DCD 可以）

他们的 `calc_cd` 用 `tf.norm`（**线性**距离，非平方），`cd_t = mean(d1)+mean(d2)`，
和本项目完全一致。**但按这个定义，CD=0.00170 意味着单向平均误差 0.00085 归一化单位
—— 比 6144 个点自身的间距（约 0.03）还小 35 倍，物理上不可能。**

`loss.py` 里另有一个 `chamfer_distance_loss`（调 `tfg.nn.loss.chamfer_distance`，
返回**平方**距离），0.00170 换算成 RMS 是 0.029，正好落在点间距量级。
**推测 Table 1 的 CD 来自这个函数，不是 `calc_cd`。** 仓库里没有评测脚本，无法确证。

→ **写论文时不要拿本项目的 CD_t 和他们 Table 1 的 CD 并排。**
DCD 没有这个问题（同公式、同 α=1、同 [0,2] 范围），这也正是第 1 节能对上的原因。

### 5. ⚠️ `CD_p` 量纲不成立，建议不再报告

`cd_p = (sqrt(mean(d1)) + sqrt(mean(d2)))/2`，而 `d` 已是线性距离 ——
这是**对长度开根号**。DCD 原论文的定义是 `mean(sqrt(d²))` 即平均距离，
原项目移植时写错了，本项目忠实复现了这个错误。

`eval_val20.csv` 里的 `CD_p_mm`（23.6mm）因此是无意义的。
**`CD_t_mm` 没问题**（线性距离之和 × scale_mm，量纲正确），继续作为主指标。

### 6. 论文 Eq.(4) 印错了

写成 `½(Σe^{-α‖·‖²} + Σe^{-α‖·‖²})`，**漏掉了 `1 −` 和密度权重 `1/n_y^λ`**。
紧接着的正文和代码都是对的。读的时候以 `loss.py` 为准。

### 7. 论文里**没有**的东西（本工作的空间）

- **没有任何密度/均匀性指标**。论文只说"DCD 能评估不均匀点分布"，
  从未直接测过扎堆率或间距 CV。**`mesh_viz.surface_stats` 是论文之外的东西。**
- **没有 mesh 重建/可视化代码**（之前翻仓库时已确认）。
- **没有交叉验证**，单次 90/10 划分。
- **没有 mm 单位**，全是归一化数值，临床上不可解释。

### 8. 「zero-shot」这个措辞很可能是错的（待落实）

之前挂着的待办「`MSN_weights3.h5` 训练时是否见过颅骨」，查到两条**间接**证据：
- 论文/README 称 MedShapeNet 覆盖 **240 类，含 organs / vessels / bones / instruments**；
- MedShapeNet 数据集本身部分源自 AutoImplant 挑战赛（SkullFix 的来源）。

**都不是直接证据**，但足以说明「zero-shot 迁移」大概率**站不住**。
写进论文前必须去 MedShapeNet 数据集主页核实类别清单是否含 skull/cranium。
在落实之前，对照组表述建议用中性的
**"作者发布的预训练权重直接应用于本项目颅骨数据"**，不要写 zero-shot。

---

## 2026-08-06（范围决策：体素基线暂不做，基线改用原项目预训练权重）

### 决策
**体素基线（3D U-Net 之类）暂不做。** 当前工作的对照基线是
**原 GitHub 项目（MedShapeNet Foundation Model）发布的预训练权重
`MSN_weights3.h5` 在本项目颅骨数据上的推理结果**。

| | 基线 | 本工作 |
|---|---|---|
| 来源 | 作者发布的 `MSN_weights3.h5` 直接推理 | 在 80 颗颅骨上从零训练 |
| val CD_t | 10.71 mm | **6.945 mm**（`dcd_l2`） |
| val DCD | 1.428 | **0.890** |
| 产出 | `notebooks/MSN_baseline_pretrained.ipynb`<br>`experiments_log/pretrained_baseline/eval_val20.csv` | `experiments_log/dcd_l2/` |

两者评估的是**同一批 20 颗验证颅骨**、同一套指标定义、同一份已对齐的 `.npz`，
所以这个对比是干净的、可以直接写进论文的。

### 这个基线的性质（措辞必须准确）
- 它是**"通用基础模型直接应用于颅骨" vs "颅骨专精训练"**，
  **不是**同任务下两种方法的较量。
- 论文 Table 1 报的 DCD=1.41269 与本项目实测的 1.42772 只差 1%，
  说明**他们的模型在颅骨上的表现和在自己验证集上几乎一样** ——
  基础模型并没有在颅骨上"失效"，本工作的增益来自**专精化**。
  这个措辞比"我们修好了它的缺陷"更准确、也更站得住。
- ⚠️ 仍**不要**用 "zero-shot"，理由见上一条目第 8 节。

### ⚠️ 与 README 的冲突（待处理）
`README.md` 目前仍写着项目目标是
"compare one **voxel baseline** vs one geometry-aware method"、
"a **self-built** corrupted dataset"，而实际情况是：
- `src/corruption/` 仍是 `# placeholder`，用的是 SkullFix 自带缺损；
- 没有任何体素方法的代码；
- `src/eval/` 只有 Chamfer/DCD，没有 Hausdorff/MSD/Dice。

**这个冲突需要和导师确认后再改 README** —— 是范围正式收窄（那就改 README），
还是体素基线只是延后（那就在 README 里标注"计划中/未开始"）。
在确认之前不动 `README.md`，避免自己改掉论文的主命题。

---

## 2026-08-06（实验：dropout —— 负面结果，代码已删除）

### 结论先行
**这个模型不过拟合，正则化这条路整条否掉。代码已从
`msn_skullfix.py` / `train_skullfix.py` 中删除**（只在两处留了注释说明为什么不要再加）。
记录保留在 `experiments_log/drop01_rejected/`。

### 结果
`--dropout 0.1`（注意力子层输出，16 层；weight 与 lambda 沿用 dcd_l2 的 1 / 2）：

| | dcd_l2（对照） | drop01 | |
|---|---:|---:|---|
| **best val CD_t** | **6.945 mm** | **14.36 mm** | **差 2.07 倍** ❌ |
| train CD_t（同轮） | 6.86 mm | 8.00 mm | train 也变差 |
| epochs | 149 | 92（最优在 65） | |

### ⚠️ 真正的发现：模型根本没有过拟合

| run | dropout | train CD_t | val CD_t | **val/train** |
|---|---:|---:|---:|---:|
| baseline_es20 | 0 | 7.25 | 7.86 | **1.08×** |
| dcd_w3 | 0 | 7.65 | 7.90 | **1.03×** |
| dcd_l2 | 0 | 6.86 | 7.31 | **1.07×** |
| drop01 | 0.1 | 9.98 | 71.97 | 7.21×（见下） |

**前三行 1.03~1.08× —— 泛化差距几乎为零，没有任何需要正则化去修的东西。**

drop01 那个 7.21× **不是过拟合**，是训练/推理模式差异：训练指标开着 dropout 算
（8.00mm），验证指标关掉 dropout 算（14.36mm）—— **关掉反而更差**，这就是分布偏移的
铁证。这个架构里**没有任何归一化层**（`LBR` 只是 Dense+ReLU），16 个 dropout 层在残差流里
的偏移逐层累积，最终输出又是对尺度极敏感的原始三维坐标。

### 当初为什么会提正则化，错在哪

推理是："187M 参数 / 80 颗训练颅骨 → 容量远超数据 → 会记忆化 → 上正则化"。
四个问题：

1. **最根本：推荐之前没有先量 train/val gap。** 那个数字一直躺在每个 `run.json` 的
   `final` 字段里（`cd_t_metric` 与 `val_cd_t_metric` 一除即得），十秒钟的事，
   却用经验法则代替了测量。**有实测数据时不该套启发式。**
2. **"80 个样本"低估了监督量。** 每个样本是一朵 6144×3 的点云、损失在每个点上都算，
   一个 epoch 约 50 万个点级约束。参数量 ÷ 样本数这个比值**对稠密预测任务不适用**。
3. **参数量 ≠ 有效容量。** 编码器最后一步
   `reduce_max(x, axis=1)`（`E-OUT_MaxPool`）把整颗颅骨压成**一个 4096 维全局向量**，
   解码器只能看到它。不管前面堆多少参数，信息都得从这一个向量挤过去 ——
   **物理上没法记住 80 颗颅骨的细节**。这个结构性约束比任何 dropout 都强。
4. **模型连训练集都没拟合好。** 真记忆化的话 train CD_t 该趋近 0（颅骨约 180mm 宽），
   实际 6.86mm。**是欠拟合，不是过拟合。**

### 对 roadmap 的影响
- ❌ **正则化（dropout / weight decay）—— 删除。** weight decay 也不必再试：它比 dropout
  温和，但前提同样是"存在过拟合"，而这个前提已被三轮数据否定，最好结果也只是"没影响"。
- ⚠️ **数据增强的理由要重写。** 原来写的是"防过拟合"，这个理由不成立了；
  若还要做，理由只能是"提升覆盖/多样性"，预期收益要下调。
- ⬆️ **"可见区域透传"优先级上升。** 第 3 点说明瓶颈是那个全局向量造成的信息通道过窄，
  而透传正是拓宽它 —— 现在这个方向有了实测支撑，不再只是照搬原论文。
- **总体方向从"抑制拟合"改为"提升拟合能力"。**

### 顺带记两个坑
- **早停会被第 1 轮的偶然低值卡住。** 第一次跑 drop01 时 `best_val_loss` 出现在第 1 轮
  （2.498），此后 20 轮没被超越 → 第 21 轮就早停了。加了 dropout 后早期 val 曲线摆动
  超过 1.0，**第 1 轮摇到偏低值就会提前掐断训练**。若以后再遇到，用
  `EarlyStopping(start_from_epoch=N)` 跳过早期噪声段。
- **`--run-name` 重名会静默覆盖。** 第二次跑用了同样的 `drop01`，
  上一轮的 `history.csv` 直接没了（`run.json` 也会在结束时被盖掉）。
  重跑同一配置前先改名或先备份。

---

## 2026-08-07（repulsion 生效：扎堆 12.1% → 0.9%）⭐

> ⚠️ **本条目的归因已被下一条（`lr_fix_only` 对照）修正。**
> 当时标题写的是"且不以精度为代价"、正文暗示 CD_t 的改善与 repulsion 有关 ——
> **实测证明 CD_t 的 0.628mm 全部来自学习率修复，repulsion 对精度的贡献为零。**
> 阅读下面的表格时请一并看修正条目。

### 结果 —— 目前最好的一次

`rep_w05`：`--dcd-lambda 2 --repulsion-weight 0.5`（r0=2mm, k=4），
外加同批修好的学习率衰减。密度指标取 8 颗验证颅骨均值，CD_t 取 run.json 的 best。

| | val CD_t | 扎堆<2mm | 间距CV | \|偏差\|p95 | 偏差std | epochs |
|---|---:|---:|---:|---:|---:|---:|
| baseline | 7.076 mm | 12.6% | 0.388 | 6.56mm | 3.25mm | 133 |
| dcd_w3 | 7.406 mm | 9.7% | 0.369 | 7.28mm | 3.63mm | 77 |
| dcd_l2（前最优） | 6.945 mm | 12.1% | 0.384 | 6.18mm | 3.08mm | 149 |
| rep05（权重错，无效） | 7.435 mm | 13.7% | 0.406 | 6.99mm | 3.44mm | 114 |
| **rep_w05** | **6.326 mm** | **0.9%** | **0.229** | **5.59mm** | **2.93mm** | **222** |
| GT 参照 | — | 0.0% | 0.145 | 3.76mm | 2.42mm | — |

**扎堆率从 12.1% 压到 0.9%，基本追平 GT 的 0.0%** —— 不是挪动几个百分点，是这个问题被解决了。
而且**没有付出精度代价**：CD_t、偏差 p95、偏差 std 同时创下最好成绩。
此前 `dcd_weight` 那种"拿精度换密度"的兑换关系在这里不存在。

### ⚠️ 归因：这一轮同时改了两件事，必须分开写

`rep_w05` 相对 `dcd_l2` 有两处变化：① 生效的 repulsion；② 修好的学习率衰减。

- **扎堆 12.1% → 0.9% 归因干净**：只有 repulsion 能让预测点互相散开，
  学习率衰减不具备这个机制。
- **CD_t 6.945 → 6.326 归因不干净**：衰减本轮触发 6 次
  （48/92/135/176/194/213 轮，3e-4 → 4.69e-6），训练从 149 轮延长到 222 轮，
  且每次降完 CD_t 都继续下行。光这一项就能解释相当一部分增益。
  **要拆开必须补一个「只修 LR、不加 repulsion」的对照。**

### 顺带确认：后期暴涨的问题消失了

rep05 那轮 train loss 从 1.25 飙到 2.07（见前条目），本轮全程平稳 ——
正是学习率衰减该起的作用，反过来印证了"衰减此前从未生效"这个诊断。

### 两个待观察

- **还没收敛**：val CD_t 最优在第 220 轮（共 222），扎堆最低在 196 轮（1.10%），
  曲线尚未走平。加大预算可能还有空间。
- **train/val 差距首次拉开**：train CD_t 5.31 vs val 6.33，比值 **1.19×**
  （此前一直是 1.03~1.08×）。仍不算过拟合，但若继续延长训练要盯住这个数。
  注意这也意味着前面"本模型不过拟合"的结论有了边界条件：它成立于欠训练状态。

### 下一步（按价值排序）

1. **只修 LR、不加 repulsion 的对照** —— 拆开上面那个归因，20 分钟，论文必问。
2. **`--loss cd --repulsion-weight 0.5`（去掉 DCD）** —— 关键消融。既然 repulsion
   已经解决了密度问题，而 DCD 的密度项梯度为 None（已实测），
   **"可以去掉 DCD"会是比"叠三个损失"强得多的结论。**
3. `--repulsion-weight 1.0` —— 扎堆已 0.9%，继续压空间不大，用于确认 0.5 是否最优。

---

## 2026-08-07（对照实验 `lr_fix_only`：归因拆开，结论要改）⭐⭐

### 目的
`rep_w05` 同时改了两件事（repulsion + 修好的学习率衰减），CD_t 的增益归因不明。
本轮只保留学习率修复、**关掉 repulsion**（删掉 `--repulsion-weight`，其余完全相同），
做单变量对照。

### 三方对比

| | CD_t | 扎堆<2mm | 间距CV | \|偏差\|p95 | 偏差std | epochs |
|---|---:|---:|---:|---:|---:|---:|
| `dcd_l2`（无LR修复，无rep） | 6.945 mm | 12.1% | 0.384 | 6.18mm | 3.08mm | 149 |
| **`lr_fix_only`**（有LR修复，**无rep**） | **6.317 mm** | 5.2% | 0.294 | 5.62mm | 2.96mm | 279 |
| **`rep_w05`**（有LR修复，**有rep**） | 6.326 mm | **0.9%** | **0.229** | 5.59mm | 2.93mm | 222 |
| GT 参照 | — | 0.0% | 0.145 | 3.76mm | 2.42mm | — |

### 归因结论

**① CD_t 的提升 100% 来自学习率修复，repulsion 贡献为零。**
```
6.945 -> 6.317   学习率修复      -0.628 mm
6.317 -> 6.326   加 repulsion    +0.009 mm   ← 运行间方差 0.49mm，这就是零
```
上一条目里"精度同时改善"的表述把功劳记错了对象，已在该条目顶部加了修正提示。

**② 密度改善是两者分摊，学习率占大头（这一条没预料到）。**
```
扎堆  12.1% -> 5.2%   学习率修复    -6.9pp（占总改善 62%）
       5.2% -> 0.9%   repulsion     -4.3pp（占 38%）
间距CV 0.384 -> 0.294 -> 0.229      同样的分摊结构
```
学习率衰减为什么会压扎堆：**推测**是后期步长变小，点能精细就位而不再抖动。**未验证。**

**③ 偏差 p95 / std 也基本都是学习率的功劳**（5.62 vs 5.59、2.96 vs 2.93，都在噪声内）。

### repulsion 还值得留吗 —— 值得，但要重新表述

它的价值很窄但很硬：**把扎堆从 5.2% 压到 0.9%、CV 从 0.294 压到 0.229，
而 CD_t 无可测变化。** 没有兑换、没有代价，而且**只有它能把密度推到接近 GT 的水平**
——学习率修复止步于 5.2%，仍是 GT（0.0%）的五倍多。

论文里应把它定位成**目标明确的组件**，而不是"提升整体性能"的改动。
可写的消融陈述：*"加入 repulsion 使扎堆率由 5.2% 降至 0.9%（GT 0.0%），
Chamfer 距离无可测变化。"*

### ⚠️ 教训：最大的单项收益来自修一个配置 bug

> ⛔ **本节标题与定性已作废，见本日「重划分」条目。** 那个 bug 是**本项目训练
> 脚本自己引入的**（原项目训练 demo 只有 `ReduceLROnPlateau(patience=5)`、
> 没有 `EarlyStopping`）。修好自己写错的配置**不是研究发现**，
> **不写进论文**；它的唯一后果是把本条目之前的四轮划为⛔错误性实验。

**0.628mm 超过此前所有改动的总和，而它来自把 `ReduceLROnPlateau` 的 patience
从 40 改成 10。** 不是损失设计、不是架构，是一行数字写错了，而且错得很安静
（它比早停的 20 大，所以在此前每一次实验里都从未运行过）。

**连带影响：`baseline` / `dcd_w3` / `dcd_l2` 全部是在"没有学习率衰减"这个次优配置下跑的。**
它们之间的横向比较仍然内部一致，但整体处在一个被拖累的基线上。
**`dcd_lambda=2` 那 0.13mm 的结论现在更应按噪声处理。**

### 其它
- 两轮都没撞 `--epochs 300` 上限（279 / 222），是自行早停，对比公平。
  但 279 已经不远，**后续若再延长训练要先调高 `--epochs`**，否则会被静默截断。
- `lr_fix_only` 的 train/val 比值同样是 1.19×，与 `rep_w05` 一致 ——
  说明这个比值上升是训练更充分带来的，与 repulsion 无关。

---

## 2026-08-07（DCD 消融 + 一个推翻此前多处判断的发现）⭐⭐⭐

### 一、结论：DCD 可以去掉

`--loss cd --repulsion-weight 0.5`，与 `rep_w05` 只差 DCD 这一项。跑了两次。

| | CD_t | HD95 | F1@0.05 | F1@0.03 | DCD | 扎堆<2mm | 间距CV | epochs |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `rep_w05`（CD+**DCD**+rep） | 6.409 | 6.205 | 0.928 | 0.556 | **0.676** | 1.36% | **0.238** | 222 |
| `cd_rep05_full`（CD+rep） | **6.355** | 6.103 | 0.930 | **0.560** | 0.685 | **1.24%** | 0.254 | 256 |
| `cd_rep05_r2`（同上，重复） | 6.359 | **6.034** | 0.929 | 0.557 | 0.685 | 1.45% | 0.251 | 249 |

去掉 DCD 后 CD_t 与 HD95 **变好**，两个 F1 持平，扎堆率持平，只有间距 CV 略退。
（DCD 这个指标本身略退不构成证据 —— 用 DCD 训练的模型在 DCD 上占优是同义反复。）

**三个损失可以砍成两个：CD + repulsion。** 与此前那个梯度分析一致 ——
DCD 的密度项梯度为 None，而密度已由 repulsion 接管。

### 二、⚠️ 重大发现：`0.49mm 运行间方差` 这个数字是错的

同配置重复两次的实测差异：

```
CD_t      6.3547  vs  6.3587    差 0.0040 mm     ← 不是 0.49
HD95      6.1028  vs  6.0337    差 0.0691
F1@0.05   0.9297  vs  0.9294    差 0.0004
DCD       0.6853  vs  0.6849    差 0.0004
扎堆%     1.236   vs  1.447     差 0.211
```

**CD_t 只差 0.004mm，比先前采信的 0.49mm 小 120 倍。**

原因找到了，是学习率调度：

| run | LR 衰减次数 | 末 30 轮 CD_t 的 std |
|---|---:|---:|
| baseline | **0** | **0.234** |
| dcd_l2 | **0** | **0.257** |
| rep_w05 | 6 | 0.010 |
| cd_rep05_full | 9 | **0.003** |
| cd_rep05_r2 | 7 | 0.010 |

**恒定学习率下验证曲线在平台上来回弹，而指标取的是「全程最优的一轮」——
那本质是在随机游走上取最小值，谁弹得低谁赢。** 学习率衰减修好之后曲线真正落下
并稳住，最小值就成了一个确定的数。

**0.49mm 不是本方法的固有噪声，是坏掉的学习率调度产生的伪影。**

### 三、这推翻了什么

- ❌ **「运行间方差 0.49mm」作废**。此前多处「这个差异在噪声内」的判断需要重读：
  凡是拿 0.49mm 当噪声底线下的结论，其阈值都设得太宽。
- ⚠️ **但前四轮（baseline / dcd_w3 / dcd_l2 / rep05_void）本身仍然只能在 ~0.25mm
  精度下读** —— 它们跑在没有学习率衰减的条件下，其「最优值」确实是彩票。
  `dcd_lambda=2` 那 0.13mm 按噪声处理的结论**依然成立**。
- ✅ **修好之后测量精度提升约两个数量级**，0.05mm 量级的差异现在是可分辨的。
  这也是上面消融结论敢说「变好」而不是「无可测差异」的依据。

⚠️ **硬伤**：对照组 `rep_w05` 只跑过一次，现在是「去 DCD 的两次均值」对
「带 DCD 的一次采样」。0.004mm 那个重复方差是在 CD+rep 配置上测的，
`rep_w05` 自身的重复性未验证（其末 30 轮 std 为 0.010，同样很稳，但不是直接证据）。
**按计划这些都会由 k 折统一重做，故本轮不补跑。**

### 四、`--seed` 不保证可复现（需在论文中写明）

`cd_rep05` 与 `cd_rep05_full` 同 seed 同配置，**从第 1 个 epoch 就分岔**
（val_loss 0.5703 vs 0.5602），一个跑到 300 轮仍未收敛、另一个 256 轮早停。

排查了两个嫌疑：
- ❌ **不是 `clump_metric` 扰动随机流**。怀疑其 `tf.random.shuffle` 会消耗
  `PointSampler` 的抽样流，**实测排除**：加不加 shuffle，后续三次
  `tf.random.uniform(seed=42)` 完全一致。
- ✅ **是 GPU 不确定性**。代码没有开
  `tf.config.experimental.enable_op_determinism()`，TF 在 GPU 上默认不保证确定性
  （规约用原子操作、cuDNN 自动挑算法）。187M 参数模型对浮点末位极敏感。

`tf.keras.utils.set_random_seed(42)` 只固定了**权重初始化与数据划分**。
`run.json` 里的 `"seed": 42` 因此**暗示了一个并不存在的可复现性**，措辞要修正。

**决定不开 `enable_op_determinism()`**：会慢 1.5~2 倍且此前实验全需重跑，而论文
本来就该报 k 折的均值±标准差，逐位可复现没有加分。

### 五、两个顺带修掉的问题

- **`ReduceLROnPlateau` 的 `min_delta` 改为 0**（Keras 默认 1e-4 是绝对阈值）。
  `cd_dcd` 的 val_loss 在 1.0 附近、末段每轮改善 2~4e-4，勉强过线；但纯 `cd` 的
  val_loss 只有 0.07、末段改善约 1.5e-5，**会被判定为每轮都没进步、学习率一路砍到
  min_lr** —— 任何改 `--loss` 的实验都会悄悄跑废。0 是唯一与量级无关的取值，
  且 EarlyStopping 一直用的就是 0。
  ⚠️ 这使得今天之后的 `cd_dcd` 训练与存档的 `rep_w05` 不再逐位一致（更宽松、
  衰减略晚）。差异很小且方向安全，未重跑前面各轮。
- **`make_loss` 会污染全局函数名**。`repulsion_weight=0` 时它直接返回模块级的
  `chamfer_loss` / `dcd_loss` 并赋值 `__name__`，把全局函数永久改名，同一进程里
  后续调用会继承错误标签。改为 `functools.wraps` 包一层。不影响任何数值。

### 六、DCD 的出处与设计意图 —— 它本来是一把尺子，不是损失

查了原论文，这解释了前面所有的观察。

> **Wu, T., Pan, L., Zhang, J., Wang, T., Liu, Z., Lin, D.**
> *Density-aware Chamfer Distance as a Comprehensive **Metric** for Point Cloud Completion*
> **NeurIPS 2021**，arXiv:2111.12702

**它是被设计成评估指标的，标题里那个词就是 "Metric"。** 动机是当时两个常用指标都有毛病：

| | 问题 |
|---|---|
| Chamfer 距离 | **对密度/分布差异不敏感** —— 一朵观感很差的点云也可能拿到很低的 CD |
| EMD | 对密度敏感，但**要求点数相等**，且**计算极其昂贵**，近似算法也慢 |

DCD 就是来填这个空的：能察觉密度分布差异（CD 做不到）、比 EMD 便宜得多、
且**有界**因而评估更稳定可比。

**作为指标它是成功的，本项目全程在用**：预训练权重 1.428 vs 本工作 0.676，
它确实把两者区分开了；而论文报的 1.41269 与本项目实测的 1.42772 只差 1%，
说明它作为一把尺子可靠且可复现。

**问题出在它被当成训练损失。** 原论文主张的是「这是一把更好的尺子」；
MedShapeNet 那个项目把它拿来当损失训练（`train.py` 里 `loss = calc_dcd`）；
本项目继承了这个用法。而**尺子不需要可导，损失必须可导** ——
这正是「密度项梯度为 None」那个发现的意义所在。

> ⚠️ **措辞修正（见第七节）**：不可导 ≠ 无效。实测 DCD 单独作用时仍把扎堆从
> 13.6% 压到 5.6%，靠的是对 Chamfer 梯度重新加权这条间接路径。它不能产生
> **分离两点的直接作用力**，但不是「什么都没做」。

**结论的正确表述**（论文里应该这样写）：
> DCD 作为评估指标是有效的，本工作全程采用；但作为密度的训练信号，
> 其密度项不可导，因此在已有显式排斥项的情况下是冗余的。

比「DCD 没用」准确得多，也守得住 —— 这不是在说某个方法很烂，
而是一个**工具被用在设计意图之外**的案例，且证据链完整：
原论文定位 → 下游当损失用 → 梯度为 None → 有 repulsion 时去掉无损失 → 但作指标依然好用。

### 七、消融 2×2 已补齐（`cd_only`，2026-08-20）—— 并修正第六节一处说法

补上「CD 单独，无 DCD 无 repulsion」这一格，四格齐了：

```
                    无 repulsion      有 repulsion
CD_t (mm)   有DCD       6.400            6.409
            无DCD       6.429          **6.357**  ← 最好
扎堆<2mm    有DCD       5.605%           1.361%
            无DCD      13.602%           1.342%
间距CV      有DCD       0.297          **0.238**
            无DCD       0.399            0.252
HD95 (mm)   有DCD       6.205            6.205
            无DCD       6.114          **6.068**  ← 最好
```
（右下角为 `cd_rep05_full` 与 `cd_rep05_r2` 两次的均值；`cd_only` 跑 305 轮早停，未被截断。）

**`cd_only` 除 HD95 外每一项都是最差的**，扎堆率 13.6% 甚至高于 baseline 的 12.85%。

#### ⚠️ 这一格推翻了第六节里一个说法

第六节写过「DCD 的密度项梯度为 None，所以它结构上无法改善密度」。**数据不支持这句。**
没有 repulsion 时，DCD 把扎堆从 **13.6% 压到 5.6%（砍掉 59%）** —— 这是实实在在的密度改善。

正确的机制解释是：密度因子本身确实不可导，但它**给 Chamfer 的梯度重新加权** ——
被过度认领的点，其距离项权重被压低，优化力气因此重新分配。**这个间接机制有效**，
只是远弱于一个直接的排斥力（5.6% vs 1.3%）。

**论文里应该这样写**：
> DCD 通过重新加权 Chamfer 梯度间接改善密度（扎堆率 13.6% → 5.6%），但其密度因子
> 本身不可导，无法产生分离两点的直接作用力。当存在显式排斥项时其贡献被完全覆盖
> （1.36% vs 1.34%），因而是冗余的。

这比原来那句准确，信息量也更大 —— 既证明了 DCD 有用，又证明了它可被更好的机制替代。

#### repulsion 也改善精度（此前未观察到）

```
无 DCD:  cd_only 6.429  ->  加 repulsion 6.357    -0.072 mm
有 DCD:  lr_fix  6.400  ->  加 repulsion 6.409    +0.009 mm（无变化）
```

机制很直接：**扎堆的点是浪费的**，覆盖不到新表面。Chamfer 有一个方向是「每个 GT 点到
最近预测点的距离」，点铺得越开这一项越小。所以把点推开本来就应该改善 CD_t。
有 DCD 时看不到这个收益，是因为 DCD 已经先把扎堆压到 5.6%，repulsion 能捡的便宜少了。

0.072mm 是同配置重复方差（0.004mm）的 **18 倍**。⚠️ 但 `cd_only` 只跑过一次，
其自身重复性未验证。

#### 结论：最优配置是 CD + repulsion（不要 DCD）

CD_t / HD95 / F1@0.05 / F1@0.03 四项均为四格最优，扎堆率与带 DCD 持平，
**只有间距 CV 一项带 DCD 略好**（0.238 vs 0.252）。

⚠️ 待补：`cd_only` / `lr_fix` / `rep_w05` 各只跑过一次。由 k 折统一解决，本轮不补跑。

### 八、作废与保留

- `cd_rep05_truncated` —— 撞 `--epochs 300` 上限被截断（最优值落在最后一轮），
  **不可引用**，仅留作「上限会静默截断」的证据。
- 训练 notebook 现在会打印每个 run 的停止原因（EarlyStopping vs 被上限截断），
  这个坑不该再靠事后人眼发现。

---

## 2026-08-20（缺损区限定指标：结果分析）⭐⭐

纯评测侧改动，不需要重训，实现见 `src/eval/report.py` 的 `_defect_metrics`。
判定规则：某点到最近的**输入点**超过 5mm 即算在缺损区，**GT 与预测用同一条规则**。

### 结果（20 颗验证颅骨）

| run | 缺损覆盖 | 缺损HD95 | 缺损精度 | 缺损F1@.05 | 缺损点数 | 全CD_t | 全F1@.05 |
|---|---:|---:|---:|---:|---:|---:|---:|
| baseline | 3.911 | 6.610 | 5.416 | 0.631 | 1261 | 7.220 | 0.862 |
| dcd_l2 | 3.852 | 6.456 | 5.193 | 0.652 | 1130 | 7.031 | 0.876 |
| lr_fix（有DCD 无rep） | **3.248** | **5.227** | 4.441 | 0.794 | 932 | 6.400 | 0.928 |
| rep_w05（有DCD 有rep） | 3.414 | 5.539 | 4.524 | 0.788 | 934 | 6.409 | 0.928 |
| cd_only（无DCD 无rep） | 3.439 | 5.500 | 4.488 | 0.791 | 865 | 6.429 | 0.927 |
| **cd_rep05_full（无DCD 有rep）** | **3.236** | 5.312 | **4.374** | **0.807** | 933 | **6.355** | **0.930** |
| cd_rep05_r2（同配置重复） | 3.242 | 5.265 | 4.436 | 0.801 | 929 | 6.359 | 0.929 |

同配置重复的差异（噪声底线）：缺损覆盖 **0.0065mm**、缺损F1 **0.0062**、
缺损HD95 0.046mm、缺损精度 0.062mm。

### 一、⭐ 缺损区的改善幅度**大于**全点云 —— 这是最重要的一条

相对 baseline：

```
                  缺损覆盖    全CD_t   |   缺损F1     全F1
cd_rep05_full     -17.3%     -12.0%   |   +27.9%    +7.9%
lr_fix            -16.9%     -11.4%   |   +25.9%    +7.6%
```

**如果反过来（缺损区改善小于全点云），就说明这些改动只是把"复现可见区域"做得更好，
真正的补全没进步。** 现在是相反的方向，所以本项目至今的改进**确实作用在难的那部分**。

⚠️ F1 那一列的相对幅度要打点折：baseline 的缺损 F1 基数低（0.631），
从低基数出发相对改善天然更大。覆盖那一列（-17.3% vs -12.0%）没有这个问题，更可信。

### 二、⚠️ 2×2 在缺损区上出现了交互效应 —— 和全点云的形状不同

```
缺损覆盖 (mm)        无 repulsion    有 repulsion
        有 DCD          3.248           3.414      <- repulsion 让它变差
        无 DCD          3.439           3.239      <- repulsion 让它变好
```

**两个机制单独用都好（3.24 / 3.24），一起用反而差（3.41），都不用最差（3.44）。**

差值 0.166mm 是重复噪声（0.0065mm）的 **25 倍**，不是抖动。

这比之前"有 repulsion 时 DCD 冗余"的说法更强一层：在缺损区，**DCD 和 repulsion 会互相
干扰**，不只是冗余。机制上的解释尚未验证 —— 一个猜测是两者都在推点，叠加后把点过度
铺开、从缺损区挤出去，但 `defect_n_pred` 并不支持这个（932 vs 934，几乎没变）。
**先记录现象，不下机制结论。**

全点云 CD_t 上看不到这个交互（6.400 / 6.409 / 6.429 / 6.357），**是缺损区指标才把它暴露
出来的** —— 这本身就说明补这套指标是值得的。

### 三、~~精度比覆盖差，且模型在过量填充~~ ❌ **本节结论已撤回，见 2026-08-20 修正**

当时写的是：模型往缺损区放了 2.4 倍于 GT 的点（930 vs 395），且这些点离真实
表面更远（4.37mm vs 覆盖 3.24mm），读作"填得过量且不够准"。

**两个结论都是错的**，源头是当时就标了 ⚠️ 的那个隐患真的主导了数字 ——
预测侧的掩码用"离输入点 > 5mm"判定，把洞周围一圈漂移的点也圈了进来。
实测（见下面的修正条目）：真正落在洞里的预测点是 473 个而非 930，密度与 GT
基本相同；洞内精度 2.96mm，比洞外的 3.23mm 还好。

### 四、采样地板同样适用

早先测过（同一张网格两次独立采样，模型误差为零）：6144 点下 CD_t 地板 4.43mm，
即**单向约 2.2mm**。缺损覆盖是单向指标，所以 3.24mm 里**约 2.2mm 是采样分辨率**，
模型自身的贡献只有约 1mm 量级。

**这意味着缺损区覆盖这一项的改进空间已经不大了** —— 除非提高输出点数（见
2026-08-20 关于点数与显存的记录：位置编码 `tf.eye` 按点数平方增长，代价很高）。
相比之下缺损精度（4.37mm）离地板更远，还有空间。

### 五、结论

- **`cd_rep05_full`（CD + repulsion，无 DCD）在缺损区指标上同样最优** ——
  与全点云的结论一致，两套独立指标指向同一配置。
- **论文应以缺损区指标为主指标**，全点云指标作为辅助 —— 后者 93.6% 的权重落在
  "复现输入"上。
- ⚠️ `lr_fix` / `rep_w05` / `cd_only` 各只跑过一次，噪声底线只在 CD+repulsion 上
  验证过。第二节那个交互效应虽然是噪声的 25 倍，**仍需 k 折确认**。

---

## 2026-08-20（修正：缺损区掩码定义错了，「过量填充/填不准」两个结论撤回）⚠️

### 起因
上一条目第三节的结论建立在一个被污染的掩码上。当时就标了 ⚠️，但没去验证，
结果它确实主导了数字。

### 掩码错在哪

预测侧原本沿用了 GT 侧的规则：「到最近输入点 > 5mm 即算在缺损区」。
**问题是根本性的：一个飘在完好骨头上方 6mm 的预测点也满足这个条件。**
所以那一列实际在量"预测点飘离表面多远" —— 而这已经被 CD_t / HD95 覆盖了 ——
只是套了个缺损区的壳。

实测（`cd_rep05_full`，10 颗验证颅骨）：

```
GT 在缺损区的点数              396
预测「离输入远」的点数          974   <- 旧掩码
预测「真的落在洞里」的点数       473   <- 距某个 GT 缺损点 5mm 内
```

约 500 个是洞周围的边界漂移点。按位置拆开预测点到真实表面的距离：

```
洞内   2.96 mm      洞外   3.23 mm      壳层   5.21 mm      全部   3.21 mm
```

**洞内比洞外还准。** 旧的 4.37mm 是被壳层那 5.21mm 拉上去的。

密度也没有过量：

```
                  GT       预测
缺损区内点间距   4.42mm   4.60mm   <- 预测反而略稀
缺损区外点间距   4.02mm   4.07mm
区外/区内密度比   0.91     0.89    <- 几乎一致
```

### 阈值 5mm 的依据（补上，此前只是拍的）

GT 点到最近输入点的距离是**清晰的双峰分布**（20 颗颅骨共 12.3 万点）：

```
  0-1mm  6.22%   1-2mm 19.97%   2-3mm 31.78%  <- 共享表面的峰
  3-4mm 25.99%   4-5mm  9.35%   5-6mm  1.74%  <- 谷底
  6-8mm  0.78%   ...            15+mm  2.46%  <- 真正的洞
```

设定这个尺度的是**采样间距**：GT 点间距 4.03mm、输入点间距 4.96mm。
两朵独立采样同一表面的点云，彼此相距约半个间距 —— 与 2-3mm 那个峰吻合。

阈值敏感性证实 5mm 在谷底：3mm→42.03%，4mm→16.05%，**5mm→6.70%**，
6mm→4.97%，8mm→4.18%。过了 5mm 曲线就平了。

### 修正后的定义与结果

两侧用**不同**的规则，因为要问的问题不同：
- GT 侧：到最近**输入点** > 5mm（"这块表面输入里没有"）
- 预测侧：到最近的**缺损 GT 点** < 5mm（"这个点落在洞里"）

⚠️ 我起初以为后者有循环性而拒绝了它，**这个顾虑站不住**：区域是关于
(输入, GT) 的事实，预测从不参与其定义。用 GT 划出一条带再评估带内的预测，
正是 boundary DSC 的做法。

```
run              缺损覆盖  缺损HD95  缺损精度  缺损F1@.05  缺损点数
baseline           3.911     6.610     3.006      0.894      412
dcd_l2             3.852     6.456     2.974      0.909      387
lr_fix             3.248     5.227     2.890      0.966      462
rep_w05            3.414     5.539     2.958      0.957      451
cd_only            3.439     5.500     2.928      0.962      425
cd_rep05_full      3.236     5.312     2.913      0.965      492
cd_rep05_r2        3.242     5.265     2.923      0.966      478
```

（GT 在缺损区约 395 点，模型放 387~492 个 —— 数量基本正确。）

### 一条更重要的结论：这一列不区分模型

修正后所有配置的缺损精度都落在 **2.89~3.01mm**，连 baseline 都是 3.006。
旧定义下 4.37~5.42 的那个"差距"几乎全是壳层伪影 —— baseline 的预测更飘，
被误圈进来的壳层点更多（1261 vs 933），指标就被拉得更高。

**所以：各配置在"填得准不准"上没有差别；它们的差别在"覆盖得全不全"。**
`defect_cov_mm` 3.24~3.91mm，跨度是重复噪声（0.007mm）的 90 倍以上，
**那才是缺损区里有判别力的那一列。**

### 方法学备注：换定义前做了排名检验

改指标之前先验证了它不是在给自家配置注水：换定义后 `lr_fix`(2.890) 反超了
`cd_rep05_full`(2.913)，**排名对我们偏好的配置不利**。这是这个改动可信的最强
证据 —— 若换定义刚好让自家模型赢，那才该怀疑。
（不过 2.890/2.913/2.923 三者只差 0.01~0.02mm，重复噪声就是 0.010mm，
这个"反超"本身也在噪声内。）

---

## 2026-08-21（实验：`--no-text` 去掉文本分支）⚠️ **待确认 —— 暂不写入进度报告**

> **状态：单次运行，结论未复核。** 最强的那条差异（缺损覆盖）是 26 倍噪声，
> 按经验足够可信，但噪声底线是从**另一组配置**的重复实验估来的，本配置本身
> 没有重复。**在 `notext_r2` 跑出来之前，本条不进任何对外材料（进度报告、
> PPT、论文）。** 它现在的用途只有一个：给"逐点交叉注意力怎么改"定方案。

### 起因：这条分支到底还是不是活的

原项目 MSN 是**多类别、文本引导**的补全，文本向量对不同器官不同。本项目只有
颅骨一类，于是 `encode_class_name("skull")` 的输出被缓存进 `bert_skull.npy`，
**每个样本、每个 batch、每个 epoch 都是同一个常数**。BERT 早已不参与训练，
连推理都不跑。

但它不等于"没作用"。`_offset_attention` 里 Q/K/V 全是 `use_bias=False`，
那个常数过完 `text_proj` 之后加在全局向量上、再复制给每个种子点，
**恰好扮演了 D1 四个交叉注意力块的 K/V 本来缺的偏置**。
3,149,824 个参数（768×4096 + 4096）合起来只表达 4096 个数。

> ⚠️ **这句话不准确，见本日第二条（机制分析）。** 后继的 `LBR` 自带 bias，
> 那个常数项可被它完全吸收 —— 文本分支**不提供任何额外的表达能力**，
> 它的实际作用是把该偏置方向的有效学习率放大约 ||t||² ≈ 228 倍。

所以预期是「指标不动，证明它是死重量」。**这个预期错了。**

### 配置

```
python -m src.models.train_skullfix --loss cd --repulsion-weight 0.5 \
       --no-text --run-name notext
```

除 `--no-text` 外与 `cd_rep05_full` 逐项相同（`loss=cd`、`repulsion_weight=0.5`、
`r0=2.0`、`k=4`、`lr=3e-4`、`batch=4`、`seed=42`、`patience=20`）。**只动一个变量。**

- 参数 187,462,503 → 184,312,679，差正好 3,149,824 ✓
- 权重文件 750,221,728 → 737,615,952 字节，差 12,605,776 ≈ 3,149,824×4 ✓
- 380 轮，最优第 360 轮，之后 20 轮 → **EarlyStopping 停的**，没撞天花板
- LR 下降 9 次

### 结果（20 颗验证颅骨）

噪声按**每项指标各自**从 `cd_rep05_full` vs `cd_rep05_r2` 那对重复实验实测，
不用统一的经验值 —— HD95 和扎堆率的噪声比 CD_t 大一到两个数量级。

| 指标 | 对照均值 | notext | Δ | 噪声 | 倍数 | 判读 |
|---|---:|---:|---:|---:|---:|---|
| CD_t (mm) | 6.3567 | 6.3171 | −0.0396 | 0.0040 | 9.8× | ✅ 更好 |
| F1@0.05 | 0.9296 | 0.9330 | +0.0035 | 0.0004 | 9.9× | ✅ 更好 |
| HD95 (mm) | 6.0683 | 5.9472 | −0.1211 | 0.0691 | 1.8× | 噪声内 |
| F1@0.03 | 0.5588 | 0.5627 | +0.0039 | 0.0032 | 1.2× | 噪声内 |
| DCD | 0.6851 | 0.6854 | +0.0003 | 0.0004 | 0.7× | 噪声内 |
| 扎堆 % | 1.3416 | 1.3932 | +0.0517 | 0.2108 | 0.2× | 噪声内 |
| 间距 CV | 0.2525 | 0.2571 | +0.0046 | 0.0022 | 2.1× | ❌ 更差 |
| **缺损覆盖 (mm)** | **3.2388** | **3.4107** | **+0.1719** | **0.0065** | **26.4×** | ❌ **更差** |
| 缺损 HD95 (mm) | 5.2886 | 5.4256 | +0.1371 | 0.0464 | 3.0× | ❌ 更差 |
| 缺损精度 (mm) | 2.9180 | 2.9535 | +0.0355 | 0.0100 | 3.6× | ❌ 更差 |
| 缺损 F1@0.05 | 0.9657 | 0.9608 | −0.0049 | 0.0011 | 4.3× | ❌ 更差 |
| 缺损内点数 | 485.2 | 454.0 | −31.2 | 14.1 | 2.2× | ❌ 更差 |

（`defect_gt_%` 两边完全相同 —— 它是数据的属性不是模型的，无从比较。）

### 一、⭐ 全点云变好、缺损区变差，方向相反

而且**缺损区那一侧的信号更强**：缺损覆盖 26.4 倍噪声，全点云 CD_t 只有 9.8 倍。

这和 2026-08-20 那条「改进作用在难的那部分」的规律**方向相反**。此前所有有效
改动（学习率修复、repulsion、去 DCD）都是全点云和缺损区同向变好，这是第一次
出现分叉。

### 二、机制：去掉偏置让模型变保守

`defect_n_pred` 从 485 掉到 454，**少往洞里放了 31 个点**。合起来看：

- **有输入数据的地方**贴得更紧 → 全点云 CD_t / F1 变好
- **必须凭空外推的洞里**不敢伸展 → 缺损覆盖变差

一个与输入无关的恒定偏置，给解码器提供的正是「跟当前样本无关的形状先验」——
**而填洞恰恰是唯一需要脱离输入去外推的地方。**

第三个佐证：**收敛慢了 50%**（380 轮 vs 对照的 256/249 轮）。去掉那个偏置
让优化更难。

### 三、这一跑真正的用处：把「逐点交叉注意力」的方案之争结掉了

D1 的四个交叉注意力块目前是退化的 —— `m1 = tile(全局向量)`，实测各行差异
0.000e+00，注意力权重恒为 1/1024。（⚠️ 更正此前一处口误：**只有 D1 的四块退化，
D2 的四块不退化** —— `m2` 来自 `copy_and_mapping`，实测行间差异 6.546。）

要把 D1 的 key 换成编码器的 2048 个逐点特征，有三种接法：

| | `m1` = | 判定 |
|---|---|---|
| A | 只用逐点特征 | ❌ **本轮排除** |
| **B** | `concat([全局(含文本), 逐点特征], axis=1)` → 1+2048 行 | ✅ **采用** |
| C | 逐点特征 + 广播加全局 | 会改掉逐点特征的含义 |

关键在于：**`feats` 进入解码器只有 `m1` 这一条路**（`D1-eye` / `D2-eye` 只用
`tf.shape(t)[0]` 取 batch size，无数据依赖）。所以 A 方案会同时切断全局向量
和文本分支 —— 而本轮实测那条通路有 26 倍噪声的作用，**不能切**。

**B 保留全局 token、在旁边加 2048 个逐点 key，现有行为是它的严格子集。**

### 四、顺带测到的一个数：墙钟预算余量是 2 倍，不是紧的

冒烟测得稳态 **9 s/epoch**（首轮 20s 含 XLA 编译）。300 轮约 45 分钟，
默认 `--minutes 90` 有 2 倍余量。此前担心「改逐点注意力会撞墙钟」是估错了 ——
**要慢一倍才会撞到。**

（另：七个对照 run 全部由 EarlyStopping 停止，总轮数 − 最优轮数 == 20 逐个验过。
所以 `--epochs 500 --minutes 150` 这类更高的天花板不构成不公平：从未生效过的
上限抬高不改变任何东西，而低天花板会**截断**新 run，那才是不公平的方向。）

### 五、配套的基础设施改动（无行为变化）

本轮暴露出一个静默失效面，一并堵上了：

**`load_weights` 在拓扑维度上不设防。** 改动解码器 key 的来源**不改变任何权重
的形状**（变的是 key 的序列长度，不是每行的通道数），所以旧 checkpoint 会被
静默读进新拓扑、不报错，并给出一份属于「从未训练过的网络」的指标。
已用最小复现验证：两个只有 key 行数不同的模型，8 组权重形状全部相同，
`load_weights` 成功返回、无任何警告。这与 `msn_skullfix.py` 开头记录的
`MSN_weights3.h5` 事故是同一失效模式的第二个入口。

| 位置 | 改动 |
|---|---|
| `train_skullfix.py` | `run.json` 新增 `use_text`；顺带补 `epochs` / `minutes`（此前只能靠对 `history.csv` 做算术才能判断是否被截断） |
| `report.py` | `Run.arch_key` = (`config`, `per_point_attn`, `use_text`)；缺字段按「该字段出现前生效的默认值」读，旧 run.json 保持可读 |
| `report.py` | `eval_runs` 按架构分组建模型，不再 `assert` 只有一种；跨架构会打印清单 |
| `report.py` | `arch_config()` 反向保护：用旧代码评估新架构的 run 直接报错，不静默退回 |
| `report.py` / `make_report_figures.py` | 模型输入按 `cfg.use_text` 决定，不再硬编码 `[points, text]` |
| 两个 notebook | 同上；`MSN_surface_quality` 复用一个模型，跨架构直接 assert |
| `experiments_log/README.md` | 新增「架构」列。顺带发现 `pretrained_baseline` 本来就是另一套拓扑（`msn_demo_arch`），已标注 |

**跨架构对比本身是成立的** —— 只要每套架构各自从头训练、拿自己的权重评估。
不成立的只有「拿旧权重灌进新拓扑」，那不是对比，是 bug。而且这次的对比比
以往更干净：**逐点注意力不增加任何参数**（删掉的 `tile` 是 Lambda，逐点特征
本来就算好了只是被 max-pool 扔掉，K/V 的输入仍是 4096 通道），
所以不存在「是结构好还是单纯变大了」的混淆。

### 六、待确认与结论

**暂定结论：文本分支保留，`--no-text` 不采用。** 全点云 CD_t 好 0.04mm 换缺损
覆盖差 0.17mm，而缺损区才是本项目要考核的部分。

**确认本条需要：**
1. `notext_r2`（同配置重复一次，约 45 分钟）—— 用本配置自己的噪声底线复核
   缺损覆盖那 26 倍
2. 理想情况下并入 k 折，与 2×2 一起报均值±标准差

**在 1 完成之前，本条不写进进度报告 / PPT / 论文。**

---

## 2026-08-21（机制分析：文本分支为什么关掉会变差 —— 它是个隐式的「偏置学习率」技巧）⚠️ **待确认**

> 承接上一条。同样**不进进度报告 / PPT / 论文**，直到下面第五节那个判决性实验跑过。
> 本条是解释，不是新证据 —— 它对 `notext` 的可信度没有任何提升作用。

### 一、⚠️ 更正：它在数学上是完全多余的

上一条把文本分支描述成「D1 的 K/V 本来缺的偏置」，暗示它提供了额外的自由度。
**查 `LBR` 的定义后发现这个说法不成立。**

`LBR = Dense(use_bias=True) + ReLU`，没有 BatchNorm。展开 `_offset_attention`：

```
r  = x − V(g) − V(c)                  c 是文本常数；V/Q/K 都是 use_bias=False
LBR:  W·r + b  =  W·(x − V(g))  −  W·V(c)  +  b
                                   └────────┘
                              常数项，可被 b 完全吸收
```

**文本分支不增加任何表达能力。** 带文本能表示的函数，不带文本也能表示 ——
只要那 4 个 `D1-STA{i}_LBR_lin` 的 bias 学到对的值。那 3,149,824 个参数是
**同一个偏置的另一种参数化**。

所以 `notext` 变差**不可能**是「少了一个自由度」。差异只能在**优化**上：
同一个函数类，梯度下降实际走到了不同的地方。

### 二、实测：无文本的模型只走到了三分之一

直接从 `.h5` 读权重算的（不建模型 —— 当时 GPU 被 `pp_attn` 训练占着）。
`c = relu(t @ W_text + b_text)`，文本注入到第 i 块的等效偏置是 `−(c @ V_i) @ W_LBR_i`。

| | 文本等效偏置 | LBR bias | 合计 | 无文本时的 LBR bias |
|---|---:|---:|---:|---:|
| D1-STA1 | 0.202 | 0.081 | **0.283** | 0.098 |
| D1-STA2 | 0.172 | 0.084 | **0.256** | 0.112 |
| D1-STA3 | 0.141 | 0.076 | **0.217** | 0.103 |
| D1-STA4 | 0.144 | 0.065 | **0.209** | 0.086 |

两点：

1. **无文本时 bias 确实变大了（约 +30%）** —— 网络在试图补偿，路径是通的。
2. **但补偿远远不够：0.098 vs 0.283，只有约三分之一。**

另一个细节：`||c|| = 0.20`，而且 **4096 维里只有 18 维非零**（ReLU 砍掉其余）。
那 315 万参数实际只在表达一个 **18 维**的方向 —— 它不是在提供信息，
是在提供一条**走得快的通道**。

### 三、机制：这是个杠杆（隐式学习率放大）

```
BERT 常数 t：768 维，||t|| = 15.10，||t||² ≈ 228
```

- **普通 bias**：1024 个数，梯度直接推它
- **文本这条路**：同样的梯度先摊到 `768 × 4096` 个权重上，再乘回常数 `t`；
  对「等效偏置」的推动被放大约 **||t||² ≈ 228 倍**

同一扇门，一个用手推，一个踩在长杠杆上推。**终点相同，一步走的距离差两个数量级。**

这解释了两个观察到的现象：
- 带文本的几十个 epoch 就把偏置推到位；无文本的爬了 **380 轮**（对照 256/249，慢 50%）
- 爬了这么久，**最后仍只到三分之一**

### 四、为什么偏偏是缺损区

**那个常数偏置是解码器里唯一与输入无关的东西。**

- **完好区域**：输入点就在旁边，模型照着贴即可，用不上先验
- **缺损区域**：一个输入点都没有，只能靠「颅骨一般长什么样」

而「颅骨一般长什么样」正是与当前样本无关的常数，**只能存在偏置里**。

证据链（四项独立测量，方向一致）：

```
偏置只到 1/3  →  收敛慢 50%  →  洞里少放 31 个点（485→454）  →  缺损覆盖 +0.17mm
```

这也解释了上一条里那个反常现象：**为什么全点云更好、缺损区更差**。
去掉杠杆后模型更保守 —— 有数据的地方贴得更紧（CD_t/F1 变好），
必须外推的地方不敢伸展（缺损覆盖变差）。

### 五、⚠️ 判决性实验（未做）

以上是**一个与数据一致的解释，不是证明**。三处薄弱：

1. `notext` 只跑了一次
2. 「偏置大小」与「敢不敢外推」之间是**相关**，没有直接验证因果
3. 偏置范数是个粗糙的代理量，方向可能比大小更重要

**最直接的检验：给那 4 个 `D1-STA{i}_LBR_lin` 的 bias 单独调大学习率，
跑一轮无文本的。** 约 45 分钟。

- **若能追平带文本的效果** → 杠杆假说坐实
- **若追不平** → 还有别的机制，本条要重写

⚠️ 实现上要注意：Keras 的 `Adam` 是逐参数自适应的，本来就会在一定程度上
补偿梯度尺度差异 —— 所以「杠杆」的效果在 Adam 下应当被削弱而非放大。
**这一点与观测到的现象存在张力，是本假说最需要被检验的地方。**
（可能的调和：Adam 的自适应是按参数的历史二阶矩，而杠杆改变的是
**同一个等效方向上的有效步长与该方向的曲率之比**，两者不是一回事。
但这需要验证，不能当成已知。）

### 六、如果成立，这是个可写的方法学观察

不是「我们保留了文本分支」，而是：

> 原架构的文本条件分支在**单类别**任务下退化为一个常数偏置，其 3.15M 参数
> 不提供任何表达能力（可被后继 Dense 的 bias 完全吸收），实际作用是把该偏置
> 方向的有效学习率放大约 ||t||² 倍。移除它会使模型在**必须外推的缺损区域**
> 变得保守（缺损覆盖 3.24 → 3.41 mm），而在有输入支撑的区域反而更准。

这比「保留一个模块」强得多 —— 它是对原架构的一处**结构性观察**，
而且直接连到本项目的核心问题（缺损区的外推能力）。

**但必须先做第五节那个实验。** 在那之前本条只是假说。

---
## 2026-08-21（实验：逐点交叉注意力 —— ❌ 阴性结果，注意力从未启动）⭐⭐

> ❌ **决定：不写进论文**（2026-08-21 用户拍板，见本日「重划分」条目）。本次进度报告也不写。
> 结论本身是硬的（72 倍噪声的退化 + 直接测到的坍缩证据），不是"待确认"。
> 待定的是**要不要把一个阴性结果放进论文**，那是叙事取舍，不是证据问题。

### 起因

D1 的四个交叉注意力块是退化的：`m1 = tile(全局向量, [1, 1024, 1])`，各行差异
实测 0.000e+00，注意力权重恒为 1/1024。假设是「`E-OUT_MaxPool` 把 2048 个逐点
特征压成一个向量，这个信息瓶颈限制了性能」。

（⚠️ 此前一处口误已更正：**只有 D1 的四块退化**，D2 的四块不退化 ——
`m2` 来自 `copy_and_mapping`，实测行间差异 6.546。）

### 实现：B 方案

```python
m1 = L.Concatenate(axis=1, name="D-IN_keys")([feats, per_point])   # 1 + 2048 行
```

全局向量保留在第 0 行（它是文本分支进入解码器的**唯一**通路，
`D1-eye`/`D2-eye` 只用 `tf.shape(t)[0]` 取 batch size）。逐点特征来自
`E-OUT_LBR1`，**本来就算好了**，只是被 max-pool 扔掉。

三项前置实测：

| | `per_point_attn=False` | `True` |
|---|---|---|
| 参数量 | 187,462,503 | **187,462,503**（一字不差） |
| D1 的 key 形状 | (1024, 4096) | (2049, 4096) |
| 注意力取值（随机初始化） | [9.766e-04, 9.766e-04] 恒定 | [1.724e-04, 1.262e-03] |
| 峰值显存（TF 计数器，batch 4） | 16.28 GiB | 18.83 GiB |
| 稳态耗时 | 409 ms/step | 458 ms/step（+12%） |

⚠️ 显存那条我先估成 +300MB，实际 **+2.55 GiB，估错 8 倍**。
第一次还用 `nvidia-smi` 测，两边都是 22576 MiB —— 那是 TF 预留的上限不是真实
占用，测法无效，换 `tf.config.experimental.get_memory_info` 才拿到真数。

**参数量完全相同**这一点值得单独说：架构消融很少能做到，通常一改结构参数量
就变，分不清是结构好还是单纯变大。**这次分得清。**

### 结果（20 颗验证颅骨；355 轮，最优第 335 轮，EarlyStopping，LR 降 9 次）

| 指标 | 对照均值 | pp_attn | Δ | 噪声 | 倍数 | |
|---|---:|---:|---:|---:|---:|---|
| **DCD** | 0.6851 | 0.7470 | +0.0618 | 0.0004 | **153.9×** | ❌ |
| **CD_t (mm)** | 6.3567 | 6.6482 | +0.2915 | 0.0040 | **72.4×** | ❌ |
| **F1@0.05** | 0.9296 | 0.9071 | −0.0224 | 0.0004 | **64.0×** | ❌ |
| F1@0.03 | 0.5588 | 0.5217 | −0.0372 | 0.0032 | 11.5× | ❌ |
| 间距 CV | 0.2525 | 0.2721 | +0.0196 | 0.0022 | 8.8× | ❌ |
| HD95 (mm) | 6.0683 | 6.4902 | +0.4219 | 0.0691 | 6.1× | ❌ |
| 扎堆 % | 1.3416 | 1.1776 | −0.1640 | 0.2108 | 0.8× | 噪声内 |
| 缺损覆盖 (mm) | 3.2388 | 3.3277 | +0.0889 | 0.0065 | 13.7× | ❌ |
| 缺损 HD95 (mm) | 5.2886 | 5.4746 | +0.1860 | 0.0464 | 4.0× | ❌ |
| 缺损精度 (mm) | 2.9180 | 2.9265 | +0.0085 | 0.0100 | 0.9× | 噪声内 |
| 缺损内点数 | 485.2 | 448.4 | −36.8 | 14.1 | 2.6× | ❌ |

**全面变差。** 唯一没变差的是扎堆率（噪声内）。

### ⭐ 原因：注意力从未启动 —— 这才是本条的价值

把训练完的模型的 D1 注意力掏出来：

```
块          全局token权重     均匀值      倍数     有效key数(exp熵)    energy 标准差
D1-STA1      4.852e-04    4.880e-04    0.99x      2048.6 / 2049        0.0204
D1-STA2      4.866e-04    4.880e-04    1.00x      2048.7 / 2049        0.0204
D1-STA3      4.869e-04    4.880e-04    1.00x      2048.7 / 2049        0.0224
D1-STA4      4.873e-04    4.880e-04    1.00x      2048.7 / 2049        0.0218
```

**有效 key 数 2048.7 / 2049 —— 训练完还是几乎完全均匀。** 连"多看一眼全局
token"都没学会（0.99× 均匀值）。

`energy` 的标准差只有 **0.02**：`q·k` 在 2049 个 key 上几乎完全相同。

**排除了「softmax 轴选错」这个解释**：拿同一批 energy 换成标准的
`softmax(QKᵀ/√d, axis=-1)`（按 key 轴 + 缩放）重算，有效 key 数仍是
**2048.4~2049.0**。所以问题不在归一化方式，**在 Q/K 本身没有分化**。

这是经典的**注意力坍缩**：权重近均匀 → 输出是个平均 → 回传给 Q/K 的梯度极小
→ 更学不出区分度。而 80 颗训练颅骨对"学会该看哪儿"这类参数来说太少了。

### 于是这一改实际干了什么

```
改之前:  m1 = 全局向量 × 1024 份
        注意力被结构强制均匀 → 输出 = V(g)
        → 全局条件化以【满强度】到达

改之后:  m1 = [全局向量, 2048 个逐点特征]
        注意力学完仍均匀 → 输出 = 2049 个的平均
        → 全局向量只占 4.85e-4，被【稀释约 2000 倍】
        → 剩下 99.95% 是逐点特征的【平均】
```

**没有加上注意力。实际是把「max-pool 的全局条件」换成了「mean-pool 的」，
顺带把全局与文本那条路稀释了约 2000 倍。** 这完整解释了退化，也和上一条
（文本分支）的机制连着 —— 文本注入的那个偏置现在也只剩 1/2049。

### ⚠️ 措辞：两句话在论文里的分量完全不同

- ❌ 「逐点交叉注意力对本任务没用」
- ✅ 「**PCT 的 offset-attention 块在本数据规模下没有发展出 key 选择性，
  因此本次实验并未检验到『max-pool 是信息瓶颈』这一假设**」

**后者才是实际发生的事。** 那个瓶颈假设**仍然成立、仍然未被验证**。

### 对后续的影响

1. **可见区域透传不受此结果影响，反而被它抬高了优先级。** 杀死本轮的是
   "要从 80 个样本里学会该看哪儿"；**透传的路由是写死的，不需要学习**。
   它是攻击同一个瓶颈的**非学习**路径
2. ⭐ **新增自查项：编码器那 8 个 `E-SA1..8` 和 D2 的四块是不是也坍缩了？
   我没查过。** 若整个模型的注意力都是均匀的，那对论文的分量完全不同
   （"我们用了 PCT" vs "PCT 的注意力在这个规模下没工作"）。半小时的事
3. 代码保留，默认 `per_point_attn=False`，行为与改动前逐位相同
   （实测：`baseline` 8.8170 / `cd_rep05_full` 7.7282，与改动前一致）

### 这不是"transformer 不适合点云补全"的证据

PoinTr / SnowflakeNet / SeedFormer / AdaPoinTr 都是 transformer 且都有效 ——
但它们训练在 PCN/ShapeNet 的**几万个形状**上。这里是 **80 颗**。
坍缩是优化与数据规模的现象，不是架构判决。

---

## 2026-08-21（重划分：前四轮列为⛔错误性实验；学习率那条不是研究发现）⚠️ **定性更正**

### 查证结果

原项目的训练 demo（`notebooks/demo/MSN_model_training_Demo.ipynb` cell 22/24）：

```python
reduce_lr = ReduceLROnPlateau(monitor='val_loss', factor=0.9,
                              patience=5, min_lr=1e-20)
AE.fit(..., callbacks=[reduce_lr, checkpoint_callback])
```

**只有 `ReduceLROnPlateau(patience=5)` 和 `ModelCheckpoint`，根本没有
`EarlyStopping`。** 没有 EarlyStopping 就不存在 patience 冲突，衰减一定触发。
cell 23 里那句 "After 322 epochs, training was stopped due to no performance
improvements for 12 consecutive epochs" 是人工描述的停止过程，不是代码里的回调。

本项目这边：

```
train_skullfix.py  首次提交 4631cd6  2026-07-28
  第 200 行: ReduceLROnPlateau(..., patience=40, ...)
```

**`patience=40` 是本项目训练脚本自己写的，后加的 `EarlyStopping(patience=20)`
比它小，导致衰减永不触发。这是本项目引入的配置错误，与原实现无关。**

### ⚠️ 定性更正

此前多处把它记作"本项目至今最大的单项收益（6.945 → 6.317）"、
"⚠️ 教训：最大的单项收益来自修一个配置 bug"、
"证明 CD_t 的增益全部来自学习率修复"。

**这个定性是错的。** 修好自己写错的配置**不是研究结果**，它没有回答任何关于
方法或模型的问题。正确的表述只有一句：**它划出了一条数据有效性的分界线。**

### 有效性分界

| | 轮次 | 状态 |
|---|---|---|
| **修复前** | `baseline_es20`、`dcd_w3`、`dcd_l2`、`rep05_void` | ⛔ **错误性实验** |
| **修复后** | `lr_fix_only`、`rep_w05`、`cd_only`、`cd_rep05_full`、`cd_rep05_r2`、`notext`、`pp_attn` | ✅ 有效 |

恒定学习率下验证曲线在平台上来回弹，而指标取"全程最优的一轮"——那是在随机
游走上取最小值。末 30 轮 std 约 **0.25mm**，而这四轮彼此的差异只有 0.13mm 量级。

**后果：**
- 这四轮之间的任何比较**不可读**；它们与后续各轮的差值**不可归因**
- `dcd_weight` / `dcd_lambda` 的调参结论建立在其上，**一并作废**
- 第 18 项那个"λ=2 梯度饱和"假说也建立在其上，**建议直接撤掉**
- **方法层面的一切比较，基线改用 `lr_fix_only`（6.317mm），不是 `baseline_es20`（7.076mm）**

✅ **完整的 DCD 2×2（`lr_fix_only` / `rep_w05` / `cd_only` / `cd_rep05_full`）
四格全部在修复之后，是干净的，不受本次重划分影响。** 这是目前最硬的一组结果。

⚠️ 顺带修正一处受污染的陈述：密度改善此前写作"12.85% → 1.34%"，跨越了分界线。
**改用两个修复后的轮次**：`cd_only` 13.60% → `cd_rep05_full` 1.34%
（唯一差异是 repulsion）。量级相近，但归因干净。

### 论文范围收口（用户拍板）

| 内容 | 进论文 | 理由 |
|---|---|---|
| 注意力坍缩全模型自查 | ✅ | 实测、原创、可复现 |
| 重写 vs 原实现的移植审计 | ✅ | 必答题 |
| 训练配置与原实现的差异表 | ✅ | 必答题（新立为 TODO 第 16 项） |
| 采样地板与跨域不可比性 | ✅ | 直接测量 |
| 缺损区限定评测 + DCD 2×2 | ✅ | 四格干净 |
| **EarlyStopping / LR 冲突** | ❌ | **自己引入的错误，不是研究发现** |
| **前四轮及其调参结论** | ❌ | ⛔ 错误性实验 |
| **逐点交叉注意力阴性结果** | ❌ | 用户决定不写 |
| **缺损区掩码定义错误、repulsion 未无量纲化等** | ❌ | 同为自己引入的错误 |
| `--no-text` | ❌ | n=1，未复核 |

**原则：自己引入并修好的错误留在 devlog 作为工作记录，不进论文。
devlog 不删任何东西——它是私人档案；论文范围由本节约束。**

---

## 2026-08-21（实验：恢复原实现的 Q/K 初始绑定 `--tie-qk-init`）⭐⭐

> 承接本日的移植审计。这是审计查出的**唯一一处未记录差异**，跑一轮把它闭环。
> ⚠️ 结论正面，但**归因不干净**（见第四节），措辞要小心。

### 起因

原 demo 的 `Self_Attention`（`MSN_model_inference_demo.ipynb` cell 4）里有一行：

```python
W_k.set_weights(W_q.get_weights())
```

把 K 的初始权重复制成 Q 的。只影响初始化——两组权重之后独立训练。
**本项目的重写版从 2026-07-28 建文件起就没有这一行**（`git log -S "set_weights"`
在 `msn_skullfix.py` 上零条记录），所有 12 次训练都跑在没有绑定的代码上，
所以它不影响历次实验之间的可比性，是共同底色。交叉注意力两边都不绑定
（Q/K 输入宽度不同，绑不了），所以只作用于编码器的 8 个自注意力块。

### 配置

```
--loss cd --repulsion-weight 0.5 --tie-qk-init --epochs 500 --minutes 150
```

用 `parse_args` 逐字段与 `cd_rep05_full` 核对过：loss / repulsion 权重 / r0 / k /
lr / batch / seed / patience / 划分全部一致，**唯一差异是 `tie_qk_init`**。
411 轮，最优第 391 轮，之后 20 轮 → EarlyStopping，LR 降 9 次，没撞天花板。

### 结果（20 颗验证颅骨；噪声取 `cd_rep05_full` vs `cd_rep05_r2`）

| 指标 | 对照均值 | tie_qk | Δ | 噪声 | 倍数 | |
|---|---:|---:|---:|---:|---:|---|
| **CD_t (mm)** | 6.3567 | **6.2618** | −0.0949 | 0.0040 | **23.6×** | ✅ |
| **F1@0.05** | 0.9296 | **0.9363** | +0.0067 | 0.0004 | **19.1×** | ✅ |
| DCD | 0.6851 | 0.6823 | −0.0028 | 0.0004 | 7.1× | ✅ |
| HD95 (mm) | 6.0683 | 5.8597 | −0.2086 | 0.0691 | 3.0× | ✅ |
| **缺损覆盖 (mm)** | 3.2388 | **3.1700** | −0.0688 | 0.0065 | **10.6×** | ✅ |
| 缺损精度 (mm) | 2.9180 | 2.8669 | −0.0511 | 0.0100 | 5.1× | ✅ |
| 缺损 HD95 (mm) | 5.2886 | 5.1011 | −0.1875 | 0.0464 | 4.0× | ✅ |
| 缺损 F1@0.05 | 0.9657 | 0.9713 | +0.0056 | 0.0011 | 4.9× | ✅ |
| 间距 CV | 0.2525 | 0.2699 | +0.0174 | 0.0022 | 7.8× | ❌ |
| 扎堆 % | 1.3416 | 2.1419 | +0.8004 | 0.2108 | 3.8× | ❌ |

**缺损覆盖 3.170 是本项目至今最好的。**

⭐ 另一件值得记的：**缺损精度动了**。此前所有配置的缺损精度都落在 2.89~3.01mm，
据此判定"精度不区分模型"（见 2026-08-20 条目）。这是第一次有配置把它推到 2.867，
**5.1 倍噪声**。那条判定要改成"在损失函数这一维度上不区分模型"——
换个维度（初始化）它是会动的。

### 一、⚠️ 归因不干净：它多跑了 160 轮

| 轮次 | cd_rep05_full | cd_rep05_r2 | tie_qk |
|---:|---:|---:|---:|
| 100 | 6.629 | 6.798 | 6.983 |
| 150 | 6.368 | 6.476 | 6.502 |
| 200 | 6.288 | 6.316 | 6.315 |
| 249 | 6.267 | **6.274（停）** | 6.247 |
| 256 | **6.267（停）** | — | 6.234 |
| 400 | — | — | 6.198 |

**在两个对照停下来的轮次上，tie_qk 只领先 0.02~0.03mm。最终那 0.095mm 里，
大部分是它后面多跑的 155 轮挣来的。** 而且它**前 150 轮是落后的**。

EarlyStopping 是自己停的，"多跑 160 轮"不算作弊——那是这个配置的性质。
**但把 0.095mm 全算到绑定头上会误导。** 准确表述：

> 恢复 Q/K 初始绑定后，模型**收敛更慢但最终解更好**（411 轮 vs 256/249）。
> 在同等轮数下优势仅 0.02~0.03mm，接近可分辨下限。

### 二、绑定**不能**解除注意力坍缩

> ⚠️ **本节的数字与最后一段的定性已被 2026-08-26 的正式测量更正**（`src/eval/attention_collapse.py`
> → `experiments_log/attention_collapse.csv`）。本表来自一个从未入库的临时脚本。
> 三点：① `std` 一列系统性偏高 1.2~1.5 倍（定义不同，**结论不变**）；
> ② `D2-STA4` 那两个有效 key（39.7% / 3.7%）**复现不了，已作废**；
> ③ 最后一段"D2-STA4 注意力大幅收窄"**块号记错、机制也不是收窄** ——
> 真正剧变的是 `D2-STA2`/`D2-STA3`，且实为 92% 的 query 行被该块自己的 1e-9 epsilon 关掉。
> **"编码器八块照样 100.0% 均匀"这条核心结论不受影响，已复现。**
> 本节原样保留，它记录的是当时怎么想的。

| 块 | cd_rep05_full std | tie_qk std | cd_rep05 有效key | tie_qk 有效key |
|---|---:|---:|---:|---:|
| E-SA1 | 0.00030 | 0.00165 | 100.0% | 100.0% |
| E-SA4 | 0.00038 | 0.00212 | 100.0% | 100.0% |
| E-SA5 | 0.00010 | 0.00094 | 100.0% | 100.0% |
| E-SA8 | 0.00018 | 0.00275 | 100.0% | 100.0% |
| D2-STA4 | 3.49701 | 6.95931 | 39.7% | **3.7%** |

**编码器八个块照样 100.0% 均匀。** energy 标准差虽然大了 5~15 倍，绝对值仍是
0.002 量级，不足以产生任何选择性。这与 40 轮诊断一致（初始 10 倍尺度优势被抹平）。

**所以增益的机制未明** —— 它不是"注意力被激活了"。

⚠️ 意外观测：**`D2-STA4` 的有效 key 从 39.7% 掉到 3.7%，注意力大幅收窄。**
而绑定只作用于自注意力、根本没碰交叉注意力，所以这是间接效应：编码器的特征分布
变了，传下去让 D2 的打分更极端。**只是观测，没有解释。**

### 三、代价：精度换密度

扎堆率 1.34% → 2.14%（3.8× 噪声）、间距 CV 0.2525 → 0.2699（7.8×）。
**方向与 repulsion 正好相反**——repulsion 是"密度大改、精度不动"，这里是
"精度全改、密度变差"。

### 四、决定：**先不改默认值**（`tie_qk_init` 保持 False）

三条理由：

1. **归因被轮数混淆**。k 折会自然解决——五折给出均值±标准差，而不是单次的"谁跑得久"
2. **与 repulsion 的作用方向冲突**（精度↑密度↓），而 repulsion 是论文第 5 章的核心。
   现在把它并进去会让那一章的故事变浑
3. **但 k 折时应把它作为一个配置纳入** —— 现在有实测理由了

### 五、对论文的影响

审计那一章（第 3 章）现在闭环了，可以这样写：

> 本文的重写相对原实现存在一处未记录的差异：编码器自注意力中 K 的初始权重
> 未复制自 Q。恢复该行后模型收敛更慢但最终解更优（CD_t 6.36 → 6.26 mm，
> 缺损覆盖 3.24 → 3.17 mm），代价是点密度变差；该差异**不改变注意力的坍缩状态**。

**注意：这一条是关于"我们的实现与原实现的差异"，不是"我们发现了原方法的缺陷"，
也不是"我们改进了模型"。** 措辞上三者不能混。

---

## 2026-08-24（仓库整理：权重裁剪，以及一个被静默覆盖的 checkpoint）⚠️⭐

起因是整理磁盘（40G 盘只剩 8.2G），但过程中翻出一件比清理本身重要的事。

### 一、⚠️ `cd_only` 的 checkpoint 曾被覆盖，而且不报错

`experiments/msn_skullfix/cd_only/` 里三个文件的时间戳对不上：

```
best.h5      08-20 23:41   ← 被覆盖
history.csv  08-20 23:41   ← 被覆盖，只剩 57 行（归档里是 305 行）
last.h5      08-20 00:31   ← 原始那次训练结束时写的
run.json     08-20 00:31   ← 原始那次，仍写着 epochs_run = 305
```

**机制**：`ModelCheckpoint` 和 `CSVLogger` 都从第 1 轮就开始写，而 `run.json`
只在训练结束时写。所以一次**中途放弃**的重跑（同名 `--run-name cd_only`，跑到
57 轮被掐）会替换掉 `best.h5` 和 `history.csv`，却留下上一次的 `run.json` ——
**记录和权重从此描述两次不同的训练，没有任何东西会报错**。
（`last.h5` 逃过一劫纯属运气：它只在 `fit()` 正常返回后才写，中断的那次没走到。）

**证据**：拿覆盖发生**之前**（08-20 01:52）写的 `eval_all_runs.csv` 做参照，
两个 checkpoint 各评 3 颗验证颅骨：

```
skull    归档 CSV   磁盘 best.h5   磁盘 last.h5
083       7.623      8.286 ❌      7.623 ✅
053       8.669      9.330 ❌      8.669 ✅
070       6.280      7.137 ❌      6.280 ✅
```

`last.h5` 逐位对上，`best.h5` 是那个 57 轮的半成品（LR 还停在 3e-4，val_loss
0.0694，而归档是 0.0612）。

**影响面：已发表的数字没有受影响。** `cd_only` 出现在 2×2 消融、缺损区表、
第二次进度汇报里，那些评估全部跑在 08-20 01:52，早于 23:41 的覆盖。
**真正的风险是未来**：今天之后任何人重跑一次 `eval_runs`（比如为了刷新 CSV）
都会静默拿到那个错模型，而且它比真的差 0.5~0.9mm —— 刚好大到能改变结论、
又不至于离谱到被一眼看出。

**已修复**：`last.h5` 验证为正确权重后改名顶上，`history.csv` 从
`experiments_log/cd_only/` 还原。修完 `run.json` 的 `epochs_run` 与 history 行数
一致（305 = 305）。

这个坑 2026-08-06 就记过一次（"`--run-name` 重名会静默覆盖"），当时只写进 devlog
没改代码，于是它又发生了一次 —— **只是这次覆盖的是一个已经进了汇报的结果。**

### 二、根因已堵上：`--run-name` 覆盖保护

`train_skullfix.py` 新增 `guard_out_dir()`，在 import TensorFlow **之前**执行
（撞名 1 秒内失败，不用等十几秒 CUDA 起来）：目标目录里已存在
`run.json` / `best.h5` / `last.h5` / `history.csv` 中任何一个就拒绝启动，
并打印那个目录里装的是什么（轮数、loss、best val_loss），
没有 `run.json` 时额外提示"这多半是一次中断的运行"。要覆盖必须显式
`--overwrite`。

实测三种情况：撞已有 run ✅ 拒绝并报出 "256 epochs, loss=cd, best val_loss
0.06046"；撞只剩 `best.h5` 的残留目录 ✅ 拒绝并提示 ABORTED；全新名字 ✅ 放行。

k 折要跑 20+ 个 `--run-name`，全靠手写，这个保护现在是必需品而不是锦上添花。

### 三、`last.h5` 是 `best.h5` 的逐字节副本（9.1 GiB 纯冗余）

14 个 run 逐个 md5：**13 个的 `last.h5` 与 `best.h5` 完全相同**。
原因是 `EarlyStopping(restore_best_weights=True)` —— `fit()` 返回时模型里装的
已经是最优轮的权重，随后的 `model.save_weights("last.h5")` 存的是同一份。
唯一的例外正是 `cd_only`，而且方向相反（对的那份是 `last.h5`）。

也就是说 `save_weights(last.h5)` 这行在早停生效时**从来没提供过新信息**。
留着它唯一的价值是"训练被 `--minutes` 或 `--epochs` 截断时能拿到最后状态"，
而截断本身就是要作废的（`cd_rep05_truncated`）。已全部删除。

### 四、裁剪前先把指标冻住（顺带验证了评估是完全确定性的）

**删权重是单向的** —— 训练在 GPU 上不可逐位复现（2026-08-07 记过），删掉的
checkpoint 无法重建。所以顺序必须是"先冻结、再删除"：

- `eval_all_runs.csv`：7 → **10 个 run**（补上 `tie_qk` / `notext` / `pp_attn`，
  它们的逐颅骨指标此前只存在于 devlog 的表格里）
- `surface_quality.csv`：3 → **9 行**（口径不变：`ids` 顺序前 8 颗验证颅骨）

三处独立核对，**偏差全部是 0.00e+00**：

1. 与归档 CSV 重叠的 5 个 run × 20 颗 × 7 项指标，逐颅骨最大偏差 0
2. `tie_qk` / `notext` / `pp_attn` 的均值与 devlog 2026-08-21 三张表逐项相符
3. 修复后的 `cd_only` 精确复现它自己的归档行

**顺带确认了一件以前没正式验证过的事：评估是完全确定性的。**
`PointSampler` 在 `training=False` 时走 `stateless_uniform(seed=[42,0])`，
所以只要权重还在，任何一行指标都能逐位重算。这与训练的不可复现形成对照，
两件事在论文里要分开写：**训练不可复现，评估可复现。**

### 五、删了什么

| | 回收 |
|---|---:|
| 13 个重复的 `last.h5` | 9.1 GiB |
| ⛔ 错误性/作废轮次的权重（`baseline_es20`、`dcd_w3`、`dcd_l2`、`rep05_void`、`drop01_rejected`、`cd_rep05_truncated`） | 4.2 GiB |
| `cd_only` 那个被覆盖的 `best.h5` | 0.7 GiB |
| `point_clouds_old_backup_no_fix/`（未修对齐的旧 .ply 路线） | 20 MB |
| pip 缓存 | 2.5 GB |

`experiments/` 19G → **5.6G**（8 个有效 run，各一个 `best.h5`），
`/root` 可用 8.9G → **25G**。**`experiments_log/` 一行没动** —— 记录是证据，
权重只是产物；⛔ 那四轮的 `run.json` + `history.csv` 恰恰是"有效性分界"
这个论证本身，必须留。

保留 `notext` 与 `pp_attn` 的权重：前者要和还没跑的 `notext_r2` 对比，
后者留到注意力自查脚本正式写出来之前（那两个数字目前同样来自临时脚本）。

### 六、连带影响（都已处理）

- **`make_report_figures.py` 的 `RUNS` 依赖已删的三轮**，第一次汇报的图
  再也生成不出来。而 `.gitignore` 忽略 `reports/figures/*.png` + `reports/*.pptx`
  的理由**正是**"能重新生成"。→ 已用 `git add -f` 把交付物本身入库（约 5MB），
  `.gitignore` 的注释改写成实情，脚本顶部加了醒目说明和"改用哪些 run"的指引。
  用 5MB 换掉 2.1GB 作废权重。
- **`MSN_surface_quality.ipynb` 的存档 cell 是覆盖写**（`df.to_csv`，只写它
  `MODELS` 里那三个），现在直接跑一遍会把 9 行的表打回 3 行。已在
  `experiments_log/README.md` 标注。

### 七、没动的

`TODO.md` 没有新增小节 —— 本次没有完成清单上的任何一项，也没有改变优先级
（清理不是待办项）。当前有效清单仍是 2026-08-21 那节。
---

## 2026-08-24（判读方法学：三把尺子分开；⚠️ `tie_qk` 的措辞更正；notebook 重排）⭐⭐

起因是用户问"tie_qk 变好了，有没有影响之前的结论"。查下去发现问题不在 tie_qk，
在**判读方法**：此前只有一把尺子，而它回答的不是要问的那个问题。

### 一、⚠️ 三种"噪声"被混为一谈了

| 尺子 | 怎么量 | 回答什么 |
|---|---|---|
| ① 同配置重跑 | 同配置训两次比均值 | "重训一次数字会不会变" = **训练随机性** |
| ② 逐颅骨配对检验 | 20 颗逐颗配对 | "换一批颅骨还成不成立" = **可推广性** |
| ③ 末段 epoch 抖动 | 末 30 轮 std | "报告的最优值本身抖多少" |

此前只用 ①（0.004mm），并拿它去判定所有比较。**但 ① 不回答 ②。** 而且 ① 本身很脆：
只有**一对**样本、**一个**配置，且**随轮数变** —— 那对重复实验截到 150 轮时差 **0.108mm**，
退火后才 0.007mm。它不是万能底线。

### 二、⚠️ `tie_qk` 的措辞更正（本条是本次最重要的产出）

两条独立检查都削弱了它：

**① 同轮次**（拿归档 history 重算"跑到第 N 轮时的全程最优"）

| run | 轮数 | 报告值 | @249 | @222 |
|---|---:|---:|---:|---:|
| `cd_rep05_full` | 256 | 6.267 | 6.267 | 6.280 |
| `cd_rep05_r2` | 249 | 6.274 | 6.274 | 6.286 |
| `tie_qk` | **411** | **6.198** | 6.247 | 6.273 |

它多跑的 160 轮自己就买了 0.049mm。**同轮次上领先只剩 0.020~0.027mm**，
即报告的 0.095mm 里**约三分之二是训练长度**。

**② 逐颅骨配对**（20 颗配对，去掉"这颗颅骨本身难"）

```
tie_qk vs cd_rep05_full     Δ均值    配对SE   改善颅骨   Wilcoxon p
CD_t (mm)                  -0.093    0.059    15/20      0.027
缺损覆盖 (mm) ⭐主指标       -0.066    0.070    10/20      0.76    ← 抛硬币
扎堆率 (%)                  +0.906    0.169     2/20      0.0002  ← 明确变差
```

**主指标上 10/20，完全不可分辨。** 而 devlog 8/21 写的是"10.6× 噪声、本项目至今最好"
—— 那用的是尺子 ①，它只说明"重训一次这个数不会漂"，**不说明换一批颅骨还成立**。

**更正后的准确表述**：
> 恢复 Q/K 初始绑定后，模型收敛更慢（411 轮 vs 256/249）而最终解在全点云 CD_t 上略优
> （15/20 颅骨，p=0.027，未过多重比较阈值），但在缺损区覆盖上不可分辨（10/20），
> 且点密度明确变差（扎堆率 1.34% → 2.14%，2/20，p=0.0002）。同轮次比较下优势仅
> 0.02~0.03mm。

「本项目至今最好」**撤回**。`experiments_log/README.md` 对应行已就地更正为 ⚠️ 待复核。

### 三、⭐ 好消息：2×2 在配对检验下**全部显著**，而且是干净的交叉交互

```
缺损覆盖（20 颗配对）           Δ均值    改善     Wilcoxon p
+ repulsion（无 DCD）         -0.204   18/20     0.0010
+ repulsion（有 DCD）         +0.167    5/20     0.0042
+ DCD（无 rep）               -0.191   17/20     0.0014
+ DCD（有 rep）               +0.179    3/20     0.0004
重复同配置（零对照）            +0.007    9/20     0.43    ✅ 干净
```

**两个机制单独都显著有效、一起用都显著变差**，零对照落在零上。
这比"25 倍噪声"强得多——它是跨颅骨一致的效应，不依赖那个只有一对样本的噪声估计。

**而同样四格在全点云 CD_t 上全部不显著**（置信区间都跨零）。
→ **"论文以缺损区覆盖为主指标"从偏好升级成了实测依据**：全点云指标分辨不出这些改动。

⚠️ 一个反例要记住：**扎堆率的零对照没过**（同配置两次重跑 5/20、p=0.015），
说明扎堆率有真实的运行间漂移，**0.2pp 以下的扎堆差异不可信**。

### 四、其它几条也重读了一遍

- **`pp_attn` 的否决不受影响**：同轮次上仍差 0.33mm，配对 1/20、p=0.0007。**不是运气。**
- **`notext` 的缺损覆盖劣化反而更硬了**：+0.175mm、**1/20 颅骨改善**、p=0.0003。
  跨颅骨极其一致。仍缺重复实验（尺子①），所以仍不进论文。
- **`cd_only` 曾被误报成"被上限截断"**：它的 `run.json` 早于 `epochs` 字段，回退值 300 < 实际 305 轮。
  停止原因判定改为**先判早停**（决定性），再判上限。

### 五、把这套判读固化进代码（不再是临时脚本）

`report.py` 新增两个函数 —— 这些数字此前只存在于对话里，和"粗糙度那两个数"是同一个问题：

- `epoch_matched(runs, at=...)` —— 各 run 在**共同轮次**上的全程最优，去掉训练长度这个
  **系统性混淆**（不是噪声）
- `paired_stats(df, base, other)` / `format_paired(...)` —— 逐颅骨配对：Δ均值、配对 SE、
  95% CI、改善颅骨数、符号检验、Wilcoxon
- 模块里写清了三把尺子各自回答什么，以及**它们互不替代**

`train_skullfix.py` 新增 `--from-run`：照抄另一个 run 的 `run.json` 里记的每一个超参
（实测 18 个字段），显式指定的 flag 仍然优先并打印"这不再是严格重复"的警告。
**重复实验的价值全在"两次真的是同一个配置"上**，而手抄 8 个 flag 正是它悄悄失效的地方
（`notext` 与对照只差 9 个 flag 里的 1 个，当时是靠事后逐字段核对才确认的）。

### 六、notebook 重排（用户要求：以后训练都由用户自己跑）

原 `notebooks/demo/MSN_train_skullfix.ipynb` 一个 notebook 既训练又评估又可视化，
而且 markdown 里**挂着至少 4 处已被推翻的说法**（0.49mm 噪声、"最大单项收益是修 LR"、
缺损区掩码的**旧定义**、`defect_n_pred≈930`）—— 它是干活时的参照，这比 devlog 里
留着旧说法危险得多。

拆成两个，并从 `demo/`（vendor 目录）移到 `notebooks/`：

| notebook | 职责 |
|---|---|
| `MSN_train_skullfix.ipynb` | **只发起训练**：控制面板（每次只改这里）→ 预检 → 训练 → 本轮自检 → 存档。附录里保留数据准备和"相对原 demo 改了什么"A~E |
| `MSN_compare_runs.ipynb` | **只判读**：指标词典（主指标是谁、什么时候盯哪个）、三把尺子、判决清单、曲线、同轮次表、主表、配对检验、分布图、可视化、对照组 |

判决清单（一个改动要同时满足才算成立）：同轮次上仍领先 · 配对检验主指标 p<0.002 或
≥17/20 · 有同配置重复且差异小于声称的效应 · 方向与机制解释一致。

### 七、待跑：两个重复实验（用户跑）

预期先写下来，跑完好对账 —— 这正是防"事后解释得通"的办法：

| | 预测 |
|---|---|
| `tie_qk_r2` | 轮数 330~420；CD_t 6.25~6.33；**缺损覆盖 3.17~3.32（与对照 3.24 重叠）**；扎堆 1.8~2.5%。预期判决：保持默认关闭，写进审计章，不作为模型改进 |
| `notext_r2` | 轮数 340~410；CD_t 6.29~6.35；**缺损覆盖 3.35~3.46（明显差于对照）**。约 80~85% 把握复现 |

**什么结果会推翻我**：`notext_r2` 的缺损覆盖回到 3.24 附近 —— 那第一次就是偶然，
"文本分支是隐式偏置学习率杠杆"整个故事作废。
---

## 2026-08-24（`tie_qk_r2`：❌ 否决，这条线收尾；⚠️ 噪声底线 0.004mm 被推翻）⭐⭐⭐

`tie_qk` 的重复实验。结论是**没有复现，而且反向**；但本条真正的价值在第三节。

### 一、结果 —— 与跑之前写下的预期对账

预期写在本日上一条目第七节，跑之前定的。**4 条对 1 条。**

| | 预测 | 实际 | |
|---|---|---|---|
| 轮数 | 330~420 | **246** | ❌ |
| CD_t | 6.25~6.33 | **6.412** | ❌ 比预测还差 |
| 缺损覆盖（主指标） | 3.17~3.32 | **3.403** | ❌ |
| 扎堆率 | 1.8~2.5% | 2.37% | ✅ |

方向判断（"tie_qk 的优势不实"）是对的，**幅度错得离谱** —— 我以为会部分复现，实际完全反向。

| | `tie_qk` | `tie_qk_r2` | 对照（CD+rep 两次） |
|---|---:|---:|---:|
| 轮数 | 411 | 246 | 256 / 249 |
| CD_t (mm) | 6.262 | 6.412 | 6.355 / 6.359 |
| **缺损覆盖 (mm)** | **3.170** | **3.403** | **3.236 / 3.242** |
| 扎堆率 (%) | 2.14 | 2.37 | 1.24 / 1.45 |

第一次比对照好 0.066mm，第二次比对照**差 0.167mm**。两次取平均 **+0.048mm** —— 零。

`tie_qk_r2` 本身是一次**完全健康**的训练：EarlyStopping 正常停、LR 降 6 次、末 30 轮 std
0.0094mm、val/train 1.16×。四项自检全绿，不存在"这轮跑坏了"。

### 二、判决：❌ 否决。判决清单四条全不过

| 判据 | 结果 |
|---|---|
| 同轮次上仍领先 | ❌ `tie_qk` @246 = 6.247（领先 0.02），`tie_qk_r2` @246 = 6.319（落后 0.05） |
| 配对检验主指标过关 | ❌ `tie_qk_r2` vs 对照：缺损覆盖 **+0.167、2/20、p=0.0007，显著变差** |
| 重复差异 < 声称效应 | ❌ 重复差异 **0.233** 是声称效应 0.066 的 **3.5 倍** |
| 方向与机制一致 | ❌ 机制本就未明（8/21 记过"增益机制未明，不解除注意力坍缩"） |

**唯一复现的效应是密度变差**：两次都比对照差（扎堆 1.34% → 2.14% / 2.37%，
配对 0/20 与 2/20 一致，p<0.0001 / p=0.0002）。

**8/21 那条"缺损覆盖 3.170 是本项目至今最好"正式撤回。** 默认值保持 `tie_qk_init=False`。

### 三、⭐ 真正重要的发现：尺子①（同配置重跑）是**配置相关**的

| 重复对 | CD_t | 缺损覆盖 | 扎堆率 |
|---|---:|---:|---:|
| `cd_rep05_full` vs `cd_rep05_r2` | 0.0040 | 0.0065 | 0.211 |
| **`tie_qk` vs `tie_qk_r2`** | **0.1506** | **0.2328** | 0.233 |
| 倍数 | **38×** | **36×** | — |

**那个 0.004mm 的"噪声底线"是一次撞大运，不是本方法的固有精度。**

而且这不是"某几颗颅骨抖了一下"：`tie_qk` 两次之间的差异**跨颅骨高度一致**
（缺损覆盖 2/20、p=0.0004；CD_t 2/20、p=0.0007）。

> ⚠️ **方法学教训，值得单独记**：**配对检验能证明"两个模型不同"，但不能证明"这个配置更好"**。
> 同一个配置训练两次，本身就能产生跨颅骨高度一致的差异。尺子①和尺子②缺一不可，
> 本条是活生生的演示。

机制大概是**早停时机的抽签**：`tie_qk` 第一次熬过某个平台继续降到 411 轮（全部 9 次里最久），
第二次在 246 轮被 patience=20 掐掉（第二短）。同一条命令。
**所以大方差未必是 `tie_qk` 特有的，别的配置也可能踩到，只是此前没抽到。**

#### 其它结论按新尺度重读（保守起见用大的那个）

| 结论 | 效应量 | vs 0.15/0.23 | 现状 |
|---|---:|---|---|
| repulsion 治扎堆（13.6% → 1.34%） | 12.4pp | 重复差 0.21~0.23pp | ✅ **岿然不动（50 倍）**，最硬的一条 |
| `pp_attn` 否决 | CD_t +0.29 | 2× | ✅ 仍站得住（配对 1/20、p=0.0007），但**措辞改掉倍数**，不要再写"72× 噪声" |
| repulsion 改善缺损覆盖 | −0.204 | 同量级 | ⚠️ 归 k 折终验 |
| DCD × rep 交互 | ±0.17~0.18 | 同量级 | ⚠️ 归 k 折终验 |
| `notext` 缺损覆盖变差 | +0.175 | **小于** | ⚠️ `notext_r2` 更必要；此前"80~85% 把握复现"下调到**五五开** |

⚠️ 反过来也不能滥用：大方差是在 `tie_qk` 配置上量到的，`cd_rep05_full` 那一对只差
0.004/0.0065。两个估计差 36 倍，说明**方差本身有波动**，不能断言"所有配置都这么抖"。
**保守按大的读，但终审仍然是 k 折。**

#### 对将来那次 k 折的定量影响（定稿时用）

训练间方差按 0.15mm 算，5 折均值的标准误约 `0.15×√2/√5 ≈ 0.09mm`，
而 2×2 的效应是 0.17~0.20mm → **t ≈ 2，勉强够，不宽裕**。三条推论：

1. k 折**必须四格全跑**（本来就是计划，现在有了定量理由）
2. **别指望 k 折能分辨 0.1mm 以下的差异** —— 低于这条线的，论文里就该写"无可测差异"
3. 万一 2×2 卡在边缘，补救是**关键格每折跑两次**，不是加折数（折数只减划分方差，减不了训练方差）

### 四、⚠️ 一个必须澄清的问题：这次审计**不要求重跑任何东西**

用户问：审计是在做了这么多改进之后才做的，那之前的训练要不要重做？**不用。** 三条：

**① `tie_qk_init` 在 13 次训练里全是 `False`**（`run.json` 里该字段不存在＝当时代码没这行）。
它是恒定的共同底色，所有横向比较两边都一样，差值不受影响。

**② 它和推理完全无关**，两条独立理由：
- 那行 `k_layer.set_weights(q_layer.get_weights())` 在**建图**时执行一次，
  `load_weights` 之后被完全覆盖 —— **初始化只对从零训练有意义**；
- DCD 1.42772 vs 论文 1.41269 那个核对根本不经过 `msn_skullfix.py`，走的是
  `msn_demo_arch.py`（逐字抽取的原架构，独立文件），而**它的第 121 行本来就有那行绑定**。
  → 预训练权重那条路一直和原实现一模一样，`eval_val20.csv` 不用重算。

**③ 和 LR patience 那个错误对比**（那个才需要重划分）：

| | LR patience 冲突 | Q/K 初始绑定 |
|---|---|---|
| 材料性地影响结果 | ✅ 是 | ❌ 否（实测无可复现效应） |
| 把各轮劈成两种状态 | ✅ 是（4 轮 / 9 轮） | ❌ 否（13 轮全 False） |
| 后果 | 必须重划分有效性 | **什么都不用动** |

**审计的作用不是"发现你要重做"，而是"证明你不用重做"。** 晚做的唯一代价是这个
不确定性多挂了几周，不是任何数据作废。

### 五、论文怎么写（草稿，第 3 章实现审计那节）

定性要准：**这不属于"自己写错的错误"那一类**（那类按 8/21 的原则不进论文）。
重写时不复制一行**无文档说明的初始化技巧**是完全正常的默认做法（独立初始化才是标准），
它从未产出过错误数字。这是**与参考实现的差异 + 受控验证**，属于可复现性的正面材料。

> The reimplementation was audited line by line against the released implementation;
> one undocumented difference was found. In the released code the key projection of
> each encoder self-attention block is initialised with a copy of the query
> projection's weights — the two remain separate trainable variables, so this affects
> initialisation only — whereas our rewrite initialises them independently. We tested
> this explicitly, training two independent runs of each setting under otherwise
> identical configurations. The tie produced no reproducible change in accuracy: the
> two tied runs differed from one another by more (CD_t 6.26 and 6.41 mm; defect
> coverage 3.17 and 3.40 mm) than their mean differed from the untied control (6.35
> and 6.36 mm; 3.24 and 3.24 mm). Its only consistent effect was on point density,
> which was worse under the tie in both runs (clumping 2.1% and 2.4%, against 1.2%
> and 1.4%). All results reported here therefore use independent initialisation.

第 4 章（方差讨论）配一句：

> Repeating a configuration end to end changed CD_t by 0.004 mm in one case and
> 0.151 mm in another (0.007 and 0.233 mm for defect coverage). Differences of this
> magnitude between single runs are therefore not interpretable on their own.

**语气上没有一处道歉** —— 通篇是"审计 → 查出一处 → 受控实验 → 结论无影响 → 因此采用 X"。
这读起来是能力，不是失误。

### 六、收尾时的处置

- 代码：`--tie-qk-init` **保留**（默认 `False`，关掉时行为逐位不变）。删了的话
  "我们测试过"这句话就不可复现
- 权重：两份 `best.h5`（1.4 GB）**暂留** —— 注意力坍缩那张表用过 `tie_qk` 的权重，
  等注意力自查脚本正式写出来之后再决定删不删
- 记录：两个 run 的 `run.json` + `history.csv` **保留**，它们就是尺子①的第二个样本

### 七、顺带记一件待办（与 tie_qk 无关）

`experiments_log/pretrained_baseline/eval_val20.csv` 的列只有
`id, CD_t_mm, CD_p_mm, DCD, scale_mm` —— **没有缺损区指标、没有 HD95、没有 F1**。
而论文主指标是缺损区覆盖，主表要把"预训练权重基线"和本工作并排时，基线那行会缺主指标。

将来要补跑一次 `MSN_baseline_pretrained.ipynb` 的推理（`MSN_weights3.h5` 已保留），
并把算指标那处改成传 `inp=` 才会出缺损区那几列。**是推理不是训练**，几分钟。已列入 TODO。
---

## 2026-08-24（`notext_r2`：✅ 成立，文本分支那条可以进论文；⚠️ 昨天的"噪声很大"要修正）⭐⭐

### 一、结果 —— 三对重复实验里最干净的一次

跑之前定死的阈值：**≥3.35 成立 / ≤3.28 推翻 / 中间悬而未决**。实测 **3.414**。

| | `notext` | `notext_r2` | 两次差 | 对照均（CD+rep 两次） |
|---|---:|---:|---:|---:|
| 轮数 | 380 | 227 | — | 256 / 249 |
| CD_t (mm) | 6.3171 | 6.3158 | **0.0013** | 6.3567 |
| **缺损覆盖 (mm)** | **3.4107** | **3.4144** | **0.0037** | **3.2388** |
| 洞内点数 | 454.0 | 437.7 | 16.4 | 485.2 |
| 扎堆率 (%) | 1.3932 | 1.3997 | 0.0065 | 1.3416 |

自检全绿：EarlyStopping 停、LR 降 8 次、末 30 轮 std 0.0055mm、val/train 1.18×。

预测对账（预测写在本日第二条第七节）：缺损覆盖 3.25~3.48 ✅、CD_t 6.28~6.45 ✅、
轮数 250~420 ❌（实际 227）。**定性上我太悲观** —— 说五五开，实际复现得极紧。

### 二、判决：✅ 成立，判决清单四条全过

| 判据 | 结果 |
|---|---|
| 同轮次上仍成立 | ✅ `notext` @227 = 6.248 vs 对照 @227 = 6.272/6.286。更强的是：**缺损区那条效应在 380 轮和 227 轮两个差很远的停止点上一模一样** |
| 配对检验主指标 | ✅ 缺损覆盖 **+0.179、17/20 变差、p=0.0012**（过 0.002 线） |
| 重复差异 < 声称效应 | ✅✅ **0.0037 vs 0.174，差 47 倍** |
| 方向与机制一致 | ✅ 洞里少放点：**−54.6 个、20 颗颅骨全部更少（0/20）**、p=0.0001 |

零对照（`notext` vs `notext_r2`）在每一项上干净落零（9/20、10/20、p 0.6~0.96）。
唯一例外是 `defect_n_pred`（−16.4、p=0.009）—— 它有真实的运行间漂移，但方向与效应同向，
且效应（−54.6）是漂移（−16.4）的 3.3 倍。

**因此那条机制观察可以写进论文**：单类别任务下文本分支退化成一个常数偏置，
去掉它让模型在**必须外推的缺损区变保守**（少放 54 个点、覆盖差 0.174mm），
而在**有输入支撑的区域反而更准**（CD_t −0.040、HD95 −0.147，17/20）。
⚠️ 第五节那个判决性实验（给 4 个 `D1-STA{i}_LBR_lin` 的 bias 单独调大学习率）**仍未做**，
所以"隐式偏置学习率杠杆"这个**机制解释**仍是假说；**成立的是现象，不是机制**。措辞要分清。

### 三、⚠️ 修正昨天那条"噪声很大"

现在有**三对**重复实验：

| 配置 | CD_t | 缺损覆盖 | 轮数 | 末段还在降吗 |
|---|---:|---:|---|---|
| CD+rep | 0.0040 | 0.0065 | 256 / 249 | 已走平 |
| **`tie_qk`** | **0.1506** | **0.2328** | 411 / 246 | **还在降** |
| `notext` | 0.0013 | 0.0037 | 380 / 227 | 已走平 |

**三对里两对极紧，`tie_qk` 是例外。** 而且关键证据在这：**`notext` 那对轮数差了 153 轮
（380 vs 227），照样复现到 0.0013mm** —— 所以"轮数差得多"本身**不**导致大方差。

真正的分水岭是**曲线有没有走平**（同轮次表就能看出来）：

```
tie_qk    @227 6.267 → 411 轮 6.198    多降 0.069   ← 对照停下时它还在改善
notext    @227 6.243 → 380 轮 6.225    只降 0.018   ← 基本走平
```

**修正后的判据**（替换昨天写的"一律保守按 0.15/0.23 读"）：

> 同配置重跑的差异**通常 ≲0.005mm**；但当一个 run 在另一个停下的位置上**仍在下降**时，
> 可以达到 0.15mm。**读小差异之前先看同轮次表的末几列有没有走平** ——
> `@222 → @300` 还在明显下行的 run，其"全程最优"不能直接拿去比。

⚠️ 昨天写的"一律按 0.15/0.23 读"**过头了**，已在 `experiments_log/README.md` 改写。
但也不能倒回去无条件采信 0.004 —— 三对样本证明那条尾巴真实存在。

⚠️ 顺带发现一个此前没注意的细节：**early stopping 盯的是 `val_loss`，而结论读的是
`val_cd_t` / 缺损覆盖**。`lr_fix_only` 就是例子：它按 val_loss 在 279 轮停（最优 259），
但 val_cd_t 一直改善到第 277 轮。**"按停止准则收敛了"≠"按报告指标收敛了"**，
这是第 2 项（可见区域透传）之后做 k 折时要留意的。

### 四、这对 2×2 意味着什么

昨天说"2×2 的效应（0.17~0.20mm）与噪声同量级、要归 k 折"。按修正后的判据要**重看**：
四格里 `cd_only`（305 轮，@222 6.423 → @300 6.353，**还在降 0.07**）和
`lr_fix_only`（val_cd_t 直到 277 轮仍在改善）**都属于"没走平"那一档**，
而 `cd_rep05_full`/`r2` 已走平。**所以 2×2 的不确定性主要来自那两格没跑到平**，
不是"这套方法本来就抖"。k 折时应给足 `--epochs`，别让任何一格在下行段被截断。

### 五、剩下的账

- ✅ `tie_qk` 否决（本日第三条）、✅ `notext` 成立（本条）—— **B 类（n=1 待复核）已清空**
- 下一步是 A 类里的实验项：**可见区域透传**（架构侧唯一在牌桌上的），或 Poisson 重建对照
- k 折仍是所有改进做完之后的终审（用户 8/24 明确：重复实验是分辨运气的工具，不是 k 折的替代）
---

## 2026-08-25（TODO 第 18 项收尾：早停口径 —— patience 20→30、解耦 LR patience、主指标进 history）⭐

第 18 项当时只记了一句"early stopping 盯 val_loss 而结论读 val_cd_t"。量清楚之后
发现它是**四件事**捆在一起，其中两件可以直接划掉。

### 一、可以划掉的两件

**(a) 「checkpoint 存的轮 ≠ 报告值那一轮」** —— 实测差 **≤0.007mm**，`cd`+rep 那一族
**完全为零**（`argmin(val_loss)` 与 `argmin(val_cd_t)` 恰好同一轮）。只有两个 `cd_dcd`
的 run 差了 18 轮，但那段曲线是平的。不构成问题。

**(b) 「全程最优」这个统计量本身有乐观偏置**（min-over-noise）—— 实测比"末 30 轮均值"
低 **0.005~0.018mm**。但它**碰不到论文**：`eval_all_runs.csv` 的数字是加载 `best.h5`
之后重算的，对应一个具体 epoch，不是全程最小。受影响的只有 `run.json` 口径那一列
（`experiments_log/README` 的表、训练 notebook 的参考表），**论文别引它就行**。

### 二、真问题：patience=20 卡在刀口上

统计每个 run「val_loss 隔多久刷新一次纪录」，看中途最大间隔：

| run | 中途最大间隔 | 含义 |
|---|---:|---|
| `rep_w05` / `cd_rep05_full` / `cd_rep05_r2` / `tie_qk_r2` | 11~13 | patience=30 只会多等 10 轮 = +1.8 分钟 |
| `notext_r2` / `lr_fix_only` / `cd_only` / `tie_qk` / `notext` | 15~20 | **距离被 patience=20 提前掐死只差 1~5 轮** |

**9 个里 5 个曾经擦着边活下来**（`notext` 那次空窗正好 20 轮）。而"能不能熬过一段平台"
是抽签 —— `tie_qk` 抽中了（411 轮，6.198），`tie_qk_r2` 没抽中（246 轮，6.319）。
**那 0.15mm 的运行间差异就是从这儿来的。**

另一条独立证据：扣掉"取 min 天然会捡到新低"这个统计伪影（对 std=σ 的平稳序列，
取 k 步 min 期望还会降 σ·√(2ln k)）之后，9 个 run 里 **3 个明确还在进步**
（实降/伪影 = 2.2~3.3×），4 个说不清。**没有一个是真收敛的。**

⚠️ 「末 20 轮降 0.000」是**结构性的**，不能当收敛证据 —— 早停就是在最优轮之后正好
20 轮停，那一列必然为零。要看末 50/100 轮。

### 三、第四件：主指标根本没被记录

`history.csv` 只有 `cd_t` / `cd_p` / `dcd` / `clump`。**缺损区覆盖（论文主指标）
一个 epoch 都没记过**，所以"主指标收敛了没有"用现有数据**根本回答不了**。

### 四、改了什么（用户拍板：k 折留到后面，这几项单折也用得上，先改）

| 改动 | 值 | 理由 |
|---|---|---|
| `--early-stop-patience` | 20 → **30** | 见第二节。用户选的保守值（我原提议 50） |
| **`--lr-patience`**（新） | **10**，不再从早停 patience 推导 | ⚠️ **耦合项**：原来是 `max(3, p//2)`，patience 20→30 会让 LR patience 10→15，等于一次改两件事、退火节奏也与已有 9 个 run 不同。现在只改停止条件。防呆断言（LR patience 必须 < 早停 patience）改成启动时报错 |
| `--epochs` / `--minutes` | 300→**600** / 90→**180** | **兜底不是预算，提高上限不花时间**。它唯一起作用的场合是"还在下降时被截断"，而那种 run 要作废（`cd_rep05_truncated`），代价远大于一个没人碰到的上限 |
| **`--defect-every`**（新） | **10**（0=关） | callback，每 10 轮在验证集上算一次缺损区覆盖，写进 `history.csv` 的 `val_defect_cov_mm`。定义与 `report._defect_metrics` 完全一致（掩码用"到最近输入点 > 5mm"，按每颗颅骨自己的 scale 换算），所以能直接和 `eval_all_runs.csv` 的 `defect_cov_mm` 比 |

**⚠️ 为什么诊断指标不能参与选择**：拿它挑 checkpoint，等于在**报告结果的同一批 20 颗
颅骨上、用报告结果的同一个指标**挑最好看的一轮 → 报告值被乐观偏置。
**现在这样（用 `val_loss` 选、用缺损覆盖报）反而是干净的**，论文里值得写一句。

实现上用 callback 而不是 Keras metric：缺损区掩码是 `(输入, GT)` 的属性，不是
`(y_true, y_pred)` 能表达的，做成 metric 得改 `y_true` 的形状、波及所有损失函数的签名。
callback 是隔离的，`--defect-every 0` 就完全不影响训练路径。

### 五、验证

- 防呆：`--lr-patience 30 --early-stop-patience 30` → 拒绝启动并说明理由 ✅
- 真跑 3 轮：`val_defect_cov_mm` 出现在 logs 和 `history.csv`，采样轮有值、非采样轮为 `nan` ✅
  （CSVLogger 在第 1 轮就固定列名，所以 epoch 0 必须算一次，否则整列会被丢掉）
- 开销：稳态 9 s/轮，算与不算那一轮都是 9 s → **实测开销可忽略**
- `run.json` 记下 `lr_patience` / `defect_every`，`_REPLAY` 同步登记 → `--from-run` 现在复制 20 个字段 ✅
- ⭐ **对老 run 的 `--from-run` 保留它自己的 `patience=20`** —— 所以**重复实验仍然是严格重复**，
  新默认值只作用于新 run ✅
- `report.fig_curves` 加了第四格（有该列才画），顺带把 x 轴从 0-based 改成 1-based ——
  此前曲线是 0-based 而 LR 竖线是 1-based，差一个 epoch

### 六、结论

**第 18 项关闭。** 已有 9 个 run 不用重跑：它们的相对比较有 `epoch_matched` 兜底，
且这条线上的两个结论（`tie_qk` 否决、`notext` 成立）都已用重复实验独立确认过。
新口径从下一个 run 开始生效。

⚠️ 遗留（不属于第 18 项，但同源）：`--epochs`/`--minutes` 的**默认值**变了，而
`--from-run` 会复制老 run 记录的旧值（500/150）。这是对的（重复实验要严格），
但**新配置的实验记得别手动传旧值**。
---

## 2026-08-25（TODO 第 19 项：文本分支「多余」那一半被**证明**了 —— 折叠验证，零训练成本）⭐⭐

第 19 项的状态是"现象已坐实，机制仍是假说"。这次把假说拆成两半，**证掉了一半**。

### 一、怎么复现（用户自己跑）

```bash
cd /root/comp0190-organ-completion
/root/miniconda3/envs/comp0190-msn/bin/python src/eval/fold_text_branch.py
```

**只打印到终端，不写任何文件；约 40 秒；要 GPU（建两个 187M 模型，跑之前先 Restart
notebook 的 kernel，否则 OOM）。** 推理是确定性的，重跑逐位相同。

### 二、为什么用折叠验证，而不是再训练一轮

原计划的判决性实验是"给那 4 个 bias 单独调大学习率，跑一轮无文本的"。
**但那个实验回答不了「多余」这个问题。**

- 「文本分支在表达能力上是否多余」是一个**代数恒等式**的断言，不是统计断言。
  代数命题可以**精确验证**，不需要抽样、没有噪声、不用 p 值。
- 而**再训练一轮，无论结果如何都无法区分两种解释**：无文本更差，既可能是"表达能力
  变弱"，也可能是"同样的函数更难优化到"。**训练实验对这个问题是错的仪器**，
  不只是更贵。
- 唯一的障碍是注意力：`att @ V(m1)` 一般不是线性的。**但 D1 的注意力已经被实测为
  结构性均匀**（`m1` 各行完全相同 → key 全同 → softmax 恒为 1/1024，行间差异 0.000e+00）。
  **那个"负面结果"恰好是这里能做精确证明的前提条件。**
- 成本对比：**40 秒 vs 1 小时训练**，而且产出的是一个**确定性事实**而不是又一个来自
  噪声分布的样本。在这个项目最近两周的处境下（单次实验噪声压不住效应），
  能把一个统计问题换成一个恒等式，是很大的便宜。

代数在 devlog 8/21 里已经写过了（`W·r+b = W·(x−V(g)) − W·V(c) + b`）；这次新增的是
**注意到它可以数值验证**，以及**注意到均匀注意力正是许可它的条件**。

### 三、结果

```
折叠公式：b' = b − (c @ W_V) @ W_LBR        c = relu(BERT常数 @ W_text + b_text)
```

| | 差异 |
|---|---|
| 逐点坐标 max \|Δ\| | 3.792e-05 归一化 = **0.004124 mm** |
| 逐点坐标 mean \|Δ\| | 6.610e-07 = **0.000072 mm** |
| **CD_t（论文要引的）** | **1.7e-06 ~ 6.4e-06 mm**（3 颗颅骨） |

预测坐标本身跨度 1.924 个归一化单位 → 这就是 float32 的算术噪声。**等价是精确的。**

⭐ 顺带：这个等式**依赖 D1 注意力恒为均匀**，所以它同时是**注意力退化的独立二次确认** ——
若那 4 个块真在做选择，折叠不可能对得上。

### 四、⚠️ 修正一个数：不是"只到三分之一"，是 46%

脚本直接给出了无文本模型**本该学到的目标值**，以及它**实际**学到的：

| 块 | 目标 ‖b'‖ | 实际 | 达成 |
|---|---:|---:|---:|
| D1-STA1 | 0.2650 | 0.0979 | 37% |
| D1-STA2 | 0.2372 | 0.1115 | 47% |
| D1-STA3 | 0.1911 | 0.1029 | 54% |
| D1-STA4 | 0.1819 | 0.0865 | 48% |

devlog 8/21 写的"只到约三分之一"是把两个范数**相加**当合计（‖文本等效‖+‖b‖），
正确的是**和的范数**（‖b−Δ‖）。方向不变，**幅度改为均值 46%**。

### 五、⚠️ Adam 那条张力：分析上基本能解掉（但仍是分析，不是证据）

实测常数：`‖t‖₂ = 15.10`、`‖t‖₂² = 228.0`、**`‖t‖₁ = 340.8`**。

| | 每步对"等效偏置"的推动 | 放大 |
|---|---|---|
| 直接 bias | `lr` | 1× |
| 经文本路（**SGD**）| 梯度 `t_i·g_j` 回乘 `t_i` 求和 | ‖t‖₂² = **228×** |
| 经文本路（**Adam**）| 每个权重步长被归一化到 ≈`lr·sign(...)`，768 个步长通过线性映射**相加** | ≈‖t‖₁ = **341×** |

**Adam 不但没抵消杠杆，放大倍数还略大。** 机制是：Adam 归一化的是**每个权重的步长**，
而放大发生在**把这些步长通过线性映射相加**的时候。

⚠️ 三处打折：`m̂/√v̂ ≈ ±1` 只在梯度符号稳定时成立；`text_proj` 的 ReLU 只让 4096 维里
**18 维**存活；优化器开着 `clipnorm=1.0`（逐变量裁剪）。
**所以这条张力从"矛盾"降级成"次要不确定性"，但没有被实验解决。**

### 六、论文怎么写

> 该文本条件分支在单类别任务下退化为常数。我们通过将其折叠进四个偏置向量验证了它在
> 表达能力上完全冗余——折叠前后输出的最大差异为 4×10⁻³ mm，即浮点噪声。然而移除该
> 分支并重新训练会使缺损区覆盖从 3.24 mm 退化到 3.41 mm（两次独立训练确认，17/20 颅骨），
> 从零训练的模型只达到目标偏置的 46%。**差异因此在优化而非表达能力。**

三句话的分量完全不同，措辞要分清：
- ✅ **"多余"是被证明的**（代数恒等式 + 数值验证）
- ✅ **"移除会变差"是被实验确认的**（n=2，配对 17/20）
- ⚠️ **"杠杆"是假说**（有四项一致的实测支持 + 一段分析，但没有判决性实验）

### 七、第 19 项的处置

**关掉一半，另一半移交 future work。**
判决性实验（给那 4 个 bias 单独调大学习率）在 Keras + Adam 下不 trivial：
`tensorflow_addons`（有 `MultiOptimizer`）这个环境没装，而 Adam 会把"乘常数放大学习率"
的重参数化技巧归一化掉，所以只能写自定义 `train_step` 或双优化器 —— 1~2 小时编码
+ 1 小时训练，**且不改模型、不改任何指标**。写进 future work，附实现难点。

### 八、顺带

`src/eval/README.md` 从 placeholder 写成了脚本清单，每个脚本注明
**怎么跑 / 结果在哪看 / 写不写文件 / 要不要 GPU / 耗时**，并写明
"结果只打印不写文件"什么时候可以接受（脚本在仓库里 + 结果确定性可复现）。
用户从此自己跑分析脚本。
---

## 2026-08-25（一次运行关掉 TODO 第 16 与第 20 项；第 19 项措辞定案）⭐⭐

```bash
/root/miniconda3/envs/comp0190-msn/bin/python src/eval/eval_pretrained_baseline.py
```
20 颗 × 5 次独立推理，产物 `experiments_log/pretrained_baseline/eval_val20_x5.csv`。
旧的 `eval_val20.csv` 保留不动（单次抽样、且只有三列）。

### 一、第 20 项（采样器不确定性）：**实测可忽略，不改代码**

`msn_demo_arch` 的 `UniformSampler` 用有状态的 `tf.random.uniform` 抽质心（且有放回），
所以同一权重同一颅骨每次推理都不同。量了 5 次：

| 指标 | 跨抽样 std | 拿什么比 | 占比 |
|---|---:|---|---:|
| CD_t | **0.030 mm** | 基线与本工作差 4.36mm | 0.7% |
| 缺损覆盖 | **0.075 mm** | 两者差 7.03mm | 1.1% |
| DCD | **0.0007** | 与论文差 1.1% | 0.05% |

再换个参照：**本项目自己的训练间方差是 0.004~0.151mm**（三对重复实验）——
**作者模型的采样器噪声落在我们自己训练噪声的范围之内**，不是一个特殊问题。

⚠️ **更正一个吓人的旧数字**：devlog 8/05 记的"同一输入两次调用输出差 **1.03**"
让这条挂了三周。那 1.03 是所有点坐标差的某种总和/范数，**落到报告指标上只有 0.03mm**。
**实测是个非问题。**

**处置：不改采样器。** `msn_demo_arch.py` 是原实现的逐字复制，随机抽样是**作者模型的
真实行为**，改掉就等于拿一个被我们改过的版本充当他们的基线。论文里报 ±0.03 即可。

⚠️ 唯一相对噪声较大的是 `defect_n_pred`（174 ± 15.5，约 9%）—— 点数是最受采样器影响的量。
不影响任何结论，但引用点数时要带上这一点。

### 二、DCD 那个论据更硬了

```
实测 1.42786 ± 0.00069（5 次）      论文 Table 1: 1.41269
差 1.1%，而采样噪声 0.05%  →  差异是噪声的 22 倍
```

**这反而让论据更干净**：那 1.1% 不是运气抖动，是一个真实的、很小的系统差异
（他们在自己的 20,962 个点云上评，我们在 20 颗颅骨上评）。**测量精度远高于差异本身**，
所以"指标实现忠实 + 权重加载正确"这个推论是稳的。

### 三、第 16 项（基线补主指标）：⭐ 出了一个很有价值的结果

旧 CSV 只有 `CD_t / CD_p / DCD`。补齐之后（同一批 20 颗，基线取 5 次均值）：

| 指标 | 预训练基线 | 本工作 `cd_rep05_full` | 倍数 |
|---|---:|---:|---:|
| CD_t（全点云） | 10.715 | 6.355 | 1.69× |
| **缺损覆盖（主指标）** | **10.264** | **3.236** | **3.17×** |
| 缺损精度 | 3.100 | 2.913 | **1.06×** |
| **洞里放的点数** | **174** | 492 | 0.35× |
| 缺损 F1@0.05 | 0.295 | 0.965 | 0.31× |

配对检验：CD_t 与缺损覆盖 **20/20 颅骨、p = 1.9e-06**；缺损精度只有 14/20、p = 0.008。

**三条可写进论文：**

**① 全点云指标严重低估差距**（1.69× vs 缺损区 3.17×）。这是「以缺损区覆盖为主指标」
最强的证据，而且**是在一个外部模型上验证的**，不是自家配置之间的比较。

**② 缺损精度那 1.06× 是假象，而且是个完美的反面教材。** 基线只往洞里放了 **174 个点**
（GT 约 395，本工作 492）——**它基本没填洞**，而放进去的那少数点碰巧离表面不远。
这正是 `defect_prec_mm` 文档里警告的"可被糊弄"的情形，**现在有了真实案例**：
只看它会得出"基线填洞填得和我们一样准"的荒谬结论。
**`defect_prec` 必须和 `defect_n_pred` 一起看** —— 这条从"文档里的告诫"变成"有实例的观察"。

**③ 配对检验里缺损精度只有 14/20**，远弱于另外两项的 20/20 —— 再次印证那一列判别力低。

⚠️ 诚实的注脚：基线从没在颅骨上训练过，"它不填洞"是意料之中的。但**指标的行为**这一点
与此无关，仍然成立。

### 四、第 19 项：措辞定案（用户选 B）

结论保持不变（"多余"已证明、"变差"已实验确认、"杠杆"是假说）。**论文按 B 写**：

> 该分支在表达能力上完全冗余（折叠验证，输出差 4×10⁻³ mm），但移除后重新训练使缺损区
> 覆盖从 3.24 退化到 3.41 mm；从零训练的模型只达到所需偏置的 46%。差异因此在优化而非
> 表达能力。**一个可能的解释是原参数化放大了该方向的有效梯度（BERT 常数的 ‖t‖₁ ≈ 341），
> 但本文未做判决性实验。**

即"事实 + 一句带保留的猜测"。**第 19 项全关**，不再作为待办；剩下的是写作选择，不是任务。

### 五、连带更新

- `experiments_log/README.md`：基线那一行改写（5 次均值、跨抽样 std、新旧 CSV 的区别），
  并新增「基线 vs 本工作」完整对照表 + 两条方法学结论
- `MSN_compare_runs.ipynb` 第 10 节：改读 `eval_val20_x5.csv`，表里补上缺损区各列，
  并去掉"这个 CSV 没有缺损区列"那句已过时的话。⚠️ 该 cell 的存档输出是旧的，下次跑会刷新
---

## 2026-08-25（TODO 第 21/22/23 项：撤回一条假说 + 两处小修；⚙️「已知但未修」清空）

收尾的最后三条，都很小，一起记。

### 一、第 21 项：λ=2「梯度饱和」假说 —— ⛔ **撤回，不再追**

原假说（devlog 8/06）：λ=2 让 `1/count^λ` 对严重扎堆的点衰减太快，密度因子趋近 0、
该点损失顶到上界 1，**梯度反而死了**；于是优化压力回流到距离因子上，
表现为"精度变好、密度不变"。

**撤回理由：它的全部证据来自 `dcd_l2`，而那一轮属于 ⛔ 错误性实验档**
（学习率衰减从未触发，"最优值"是随机游走上的最小值）。前提没了，假说无从谈起 ——
**它不是"没验证"，是"没有需要解释的现象"**。

而且已无意义：最优配置（CD + repulsion）**根本不含 DCD**，这个旋钮不会再被用到。

顺带改了两处 docstring（`msn_skullfix.make_loss` 和 `--dcd-lambda` 的 help），
原来写着"raising it targets clumping SPECIFICALLY"、"the more targeted knob for the
11.2% problem"——那是**设计意图**（指数确实作用在密度因子上），但被写得像实测结论，
而唯一测过它的那一轮已作废。改成"BY CONSTRUCTION ... that is design intent, not a
measurement"，并注明本项目没有有效测量。

### 二、第 22 项：模块 reload —— 从"待修缺陷"改为"已堵住"

这条本来就不是缺陷，是 Python 的正常行为（`import` 有缓存）。问题只是**它会静默地
让你看到上一版的图和数字**。

`MSN_compare_runs.ipynb` 早就有 `importlib.reload(rp)`；补上了
`MSN_surface_quality.ipynb`（它 import 了 `mesh_viz` + `report`）。

⚠️ **只 reload 那两个纯 numpy/plotly 模块**：`msn_skullfix` 定义 Keras 层，
热重载会让已建好的模型和新类对不上，出的怪事比它解决的问题多。已在注释里写明。

`MSN_baseline_pretrained` / `MSN_train_skullfix` / `explore_skull` 不 import 这两个模块，
不受影响。

### 三、第 23 项：`.npz` 路径写死 —— 收成一个常量

四个文件里逐字重复同一个文件名（`report.eval_runs` / `fold_text_branch` /
`eval_pretrained_baseline` / `make_report_figures`）。**没有任何东西保证它们一致**，
而一旦不一致就是"两个 run 在不同数据上被评估"这种静默错误。

改成 `report.DATA_CACHE` 一个常量，四处共用；`eval_runs` 另加可选 `data=` 参数。
`train_skullfix.py` 的 `--data` 本来就是参数，没动；`prepare_skullfix.py` 是**生产者**，
它的 `--out` 保持原样。

⚠️ **这是去重，不是为换数据集做准备。** SkullBreak 要不要做还没定，
本次改动不隐含任何计划（TODO 里那一条也已标注「未决定是否要做」）。

### 四、⚙️「已知但未修」清空 —— 以及这一批的一个规律

六条（18~23）全部处理完，TODO 的 ⚙️ 那一节**第一次变成空的**。

回头看这一批的处置方式：

| 处置 | 条数 | 哪些 |
|---|---|---|
| **量一下就划掉**（根本不是问题） | **3** | 18 的前两件、20 整条 |
| 真要动代码 | 2 | 18 的早停口径、23 的路径 |
| 定性撤回（前提没了） | 1 | 21 |
| 补文档/堵坑 | 1 | 22 |

> **"待修问题"清单上的条目，量一下往往比修一下便宜。**
> 第 20 项挂了三周，源头是一个听起来吓人的数字（"两次调用差 1.03"），
> 实测落到报告指标上只有 **0.03mm**。第 18 项四件事里有两件同样是量完就划掉。
>
> 收尾阶段值得先对每条问"它到底有多大"，再问"怎么修"。
---

## 2026-08-25（三个悬了三周的问题有了答案：zero-shot ❌ / 自建缺损 ❌ / PROGRESS 弃用）⭐

用户直接给了答案，都不用再查。

### 一、⭐ 「zero-shot」不能写 —— 这套权重**见过颅骨数据**

挂在 TODO 上从 8/05 到今天。之前只有两条**间接**证据（MedShapeNet 含 bones 类、
部分源自 AutoImplant），所以一直用中性表述。**现在确认了。**

这不只是措辞问题，它对论文有**两个方向相反**的后果，都要写进去：

**① 基线并没有吃亏 —— 这让本工作的结论更强。**
它见过颅骨，所以"通用基础模型 vs 颅骨专精训练"的差距（**缺损区覆盖 3.17×**）
**不能**用"它没见过这类形状"来解释。增益确实来自**专精化**，而不是来自
"给它一个没见过的领域"。这比 zero-shot 的叙事强得多，也守得住。

⭐ 顺带把另一条旧观察坐实了：devlog 8/06 记过"论文报 DCD 1.41269，本项目实测 1.42772，
说明基础模型在颅骨上并未失效"。现在知道**颅骨本来就在它的训练分布里**，
所以"表现和在自己验证集上一样"是理所当然的 —— 那条观察从"有点意外"变成"意料之中"，
但作为**指标实现忠实**的证据不受影响。

**② ⚠️ 可能存在训练/测试污染，而方向对本工作不利地保守。**
若 MedShapeNet 收录的 AutoImplant 数据**包含本项目这 20 颗验证颅骨**，
那基线是在**它自己训练过的数据**上被评估的 —— 它的分数被抬高了，
**本工作的领先幅度因此是低估的**。

**论文要主动写这一条**，并说明这个不确定性**只会让结论更保守，不会反过来**。
主动交代一个对自己有利的不确定性，比被审阅者问出来强。

（是否真的包含那 20 颗，需要去核对 MedShapeNet 的样本清单；即使不核对，
上面这句话也是成立且安全的。）

### 二、"self-built corrupted dataset" —— ❌ 取消

SkullFix 自带缺损/完整配对，本项目全部结果用的都是它；`src/corruption/` 从建仓库起
就是 placeholder，从未实现。**这个说法对本工作从来不成立**，挂了三个月只是因为它
写在最初的 README 标题里。

已改 `README.md`：标题从
"Comparing voxel-based and geometry-aware deep learning ... under a self-built,
controlled corruption dataset" 改为
"Geometry-aware deep learning for 3D cranial shape completion, evaluated with a
defect-region-restricted protocol"，并在开头单列一段「Scope changes」说明取消了什么、
什么还未定。

⚠️ **删掉一个没做的东西，比留着当"计划中"诚实。** 论文的主命题现在与代码一致了。

### 三、voxel baseline —— ⏸ 仍未定，这是现在唯一的范围悬念

用户 8/25：**还在犹豫**。所以 README 里明确标成 undecided，而不是悄悄删掉或假装在做。

若最终要做，做法早就想清楚了（TODO 里一直挂着）：把体素模型的二值输出丢进
`prepare_skullfix.py` **同一条管线**（marching cubes → 密集采样 → FPS 到 6144），
两边共用采样地板，"谁更好"的比较才成立 —— 但仍不能拿去和论文发表的体素域指标并排。

### 四、`PROGRESS.md` —— 弃用

停在 6/25 的"P0 建仓库"。用户 8/25：不再维护，待办从 TODO 里删掉。
进度以 `devlog.md` + `TODO.md` 为准。
⚠️ 文件本身还在仓库里且内容已完全过时，要不要删由用户定。

### 五、TODO 的处理方式（这次破了一次例）

用户要求**直接改第七次快照，不新增第八次**。照办了。

破例的理由成立：这三条不是"完成了一项工作"，而是**回答了清单上的问题**——
没有新的认识替代旧的认识，只是把"待查证"换成了答案。追加式约定防的是
"就地覆盖会抹掉认识变化的过程"，而这里没有过程可抹。

📌 那一节现在只剩三条：体素基线（未定）、采样地板写进论文、训练配置差异表。
---

## 2026-08-25（TODO 第 13 项：采样地板测出来了，**旧数字偏低**；第 9 项加了前置步骤）⭐⭐

```bash
/root/miniconda3/envs/comp0190-msn/bin/python src/eval/sampling_floor.py
```
100 颗完整颅骨全跑，约 3~4 分钟，纯 CPU。产物 `experiments_log/sampling_floor.csv`（逐颅骨一行）。

### 一、结果，以及⚠️ 旧数字作废

| | 旧记录（不可复现） | **本次实测（100 颗）** | 差 |
|---|---:|---:|---:|
| CD_t 地板 | 4.43 | **4.619 ± 0.252** | +0.19 |
| HD95 地板 | 3.79 | **3.974 ± 0.214** | +0.18 |
| 单向地板 | ~2.2 | **2.310 ± 0.126** | +0.11 |

颅骨间范围 4.06~5.24，**变异系数只有 5.5%** —— 地板本身很稳。

⚠️ 100 颗的标准误是 0.025mm，而差距 0.19mm 是 **7.5 个标准误** —— **不是抽样差异，
是旧测量的方法与这次不同**（旧脚本从未入库，无从对照）。方向是：**地板更高 →
模型自身的贡献比原以为的更小**。`TODO` / compare notebook / `experiments_log/README`
里引用旧值的地方都已更新。

**这是第三个"临时脚本产出的数字"被正式化的例子**（前两个是粗糙度和注意力坍缩，
后者仍欠着）。而且这一次**旧数字是错的**，不只是不可复现 —— 这比前两次更有说服力：
**不入库的数字不但没法复核，它还可能一直是错的。**

### 二、这个数把两件事重新框定了

```
本工作  CD_t  6.355  =  地板 4.619  +  高出地板 1.736      地板占 73%
基线    CD_t 10.715  =  地板 4.619  +  高出地板 6.096      地板占 43%
缺损覆盖      3.236  =  单向地板 2.310  +  0.926           只剩 0.93mm
```

**① 你报告的 CD_t 里，73% 是表示方式而不是模型。** 这一条是硬的（4.619/6.355），
也是"本文数值不可与体素域方法并排"最直接的证据。

**② 扣掉共模地板之后，基线的差距从 1.69× 变成约 3.5×。**
⚠️ 但**这个减法是粗略记账，不是严格分解** —— 两种误差不线性相加（按平方相加会得到
完全不同的数）。所以 3.5× 只能当**指示**。

⭐ **但有一条独立证据指向同一个方向**：缺损区覆盖比是 **3.17×**，那是直接测的、
不依赖任何减法。**两条路都说"真实差距约 3 倍多，而不是 1.7 倍"** —— 这个收敛比任一条单独可信。

**③ 缺损覆盖离地板只剩 0.926mm**，比原先以为的（~1.0）还小。
"这条线优化空间有限"现在有了准确的数。

⚠️ 而且 4.619 还是**理想分布**的下界：真实预测有扎堆（1.34%、CV 0.25 vs GT 0.145），
分布不如 FPS 均匀，实际连它都够不到。所以"高出地板的 1.74mm"里**还掺着密度的代价** ——
这也正好解释了 repulsion 为什么会顺带改善 CD_t（点铺得越开，GT→预测那一项越小）。

### 三、⚠️ k 折之后不需要重跑 —— 而这是个设计选择，不是运气

用户问了一个好问题："预测的 6144 个点在 k 折后会变，那地板不用重测吗？"

**不用，因为地板的测量里根本没有模型。** 两次采样 A、B 都"完全正确"（都落在真实表面上），
差 4.619mm 只是因为它们是同一张表面上的**不同的点**。一个**完美**的模型也会得到
约 4.619 —— 它输出的点不会正好是 GT 抽中的那 6144 个。

但这里有个真实的分叉，**正是它决定了脚本的默认值**：

| 想报什么 | k 折后要重跑吗 |
|---|---|
| "**这 20 颗验证颅骨**的平均地板" | ✅ 要 —— 颅骨间 std 0.252，换一批就变 |
| "**这个数据集**（全部 100 颗）的地板" | ❌ 不用 —— 与划分无关 |

**默认跑全部 100 颗就是为了落在第二行。** 而且即使以后想要"某一折自己的地板"，
CSV 是**逐颅骨一行**的，按那折的 `val_ids` 过滤取平均即可，**贵的那部分只算一次**。
这就是用户要求的"留好输入口"的具体形态。

### 四、第 9 项（Poisson）加了一个前置步骤

用户接着问："做了 Poisson 之后指标会变，那点云这一套是不是就没价值了？"

**会变，而且可能变好很多** —— 因为换成"点到面"之后**采样地板会消失**（预测点落在
真实表面上 → 距离 0）。地板是"点对点"这个记分方式的产物。

**但点云指标不会因此作废**，四条：
1. **所有比较结论不受影响** —— 地板是共模的，比大小时自己抵消（与体素基线那条论证同理）
2. **密度那条线在网格上根本量不了** —— `clump_%` / `spacing_CV` 是点集的性质，
   重建那一步恰好把它抹掉。而 repulsion 是全项目最硬的结论，它只存在于点云域
3. **损失函数本身定义在点云上** —— 指导开发的指标必须和被优化的对象同空间
4. **重建自带伪影和参数** —— 实测距离场重建让原始点位移中位 5.3mm、p95 12.0mm，
   与模型自身 CD_t 同量级；这正是 `mesh_viz` 里"重建 mesh 只能看不能算指标"那条硬规矩的由来

⚠️ **陷阱**：如果 Poisson 的数字好看，**不能把它换成主结果** —— 那是 metric shopping。
诚实的结构是**主体报点云**（方法在这个空间里开发和比较），**Poisson 单列一节**作为
对外可比性的桥。

⭐ **所以第 9 项加了前置步骤（已写进 TODO）：先量"重建地板"。**
做法与采样地板同构：拿 **GT 的点**做 Poisson 重建，和**真正的 GT 网格**比，全程无模型。
若那个差异是 3mm 量级，则"Poisson 之后 HD95 = X"主要在量重建器而不是模型，白做；
0.5mm 量级才谈得上和 AutoImplant 的 1.52mm 并排。**先量这个，再决定要不要报。**

⭐ 顺带：**第 13 项不但不会因为 Poisson 作废，它恰恰是做 Poisson 的理由。**
没有地板这个数，"我们的数字不能和体素域并排"只是托词；有了它才是测量结论，
也才说明为什么要多做一步重建。
---

## 2026-08-26（TODO 第 10 项：注意力坍缩自查正式入库；⚠️ 8/21 的 D2 数字作废 + 一条新发现）⭐⭐⭐

```bash
/root/miniconda3/envs/comp0190-msn/bin/python src/eval/attention_collapse.py
```
默认跑 `cd_rep05_full` / `tie_qk` / `pp_attn` 三个 run × 16 块 × 3 颅骨（083 053 070），
约 3~5 分钟，要 GPU。产物 `experiments_log/attention_collapse.csv`（**合并写**，按 `run`+`block`）。

**这是第三个"临时脚本数字"被正式化的例子**（前两个是采样地板、文本分支折叠）。
和采样地板那次一样，**旧数字里有一部分是错的**。

### 一、核心结论复现，而且比原来说得清楚

⭐ **最重要的产出不是那个 12/16，是把"均匀"拆成了两种。** 原来记的是"16 块中 12 块坍缩"，
这句话把唯一有内容的那一半抹掉了：

| | 判据 | 是什么 | 论文里算什么 |
|---|---|---|---|
| **STRUCTURAL** | `key_row_range` **恰好 = 0** | key 各行逐位相同（`m1 = tile(全局向量)`），softmax 必然返回 1/n，**Q/K 学成什么样都无关** | 架构事实，不是发现。这个块**不可能**注意 |
| **learned collapse** | keys 有区分度但 `eff_frac` ≈ 1 | Q/K 没长出对比度 | ⭐ **这才是"80 颗颅骨学不出 key 选择性"的证据** |

三个 run 的结果：

| run | uniform | 拆分 | attend |
|---|---|---|---|
| `cd_rep05_full` | 12/16 | **4 structural**（D1-STA1~4）+ **8 collapsed**（E-SA1~8） | 4（D2） |
| `tie_qk` | 12/16 | 同上 | 4 |
| `pp_attn` | 12/16 | **0 structural + 12 collapsed** | 4 |

⭐ `pp_attn` 正好演示了这个区别：给 D1 塞进 2048 个逐点 key 之后，它的 D1 从
structural **变成** learned collapse（`key_row_range` 0 → 1.47）。所以
**"注意力从未启动"在 `pp_attn` 上是实测结论，在默认架构上只是结构事实** —— 混着说会被问穿。

编码器八块（`cd_rep05_full`）：`eff_frac` 全 1.000000，而 `key_row_range` 是 0.025~0.038
—— **keys 明明不同，注意力却完全均匀**。`energy_std` 7.8e-05 ~ 2.8e-04。

控制列（把同一批分数换成教科书的 `softmax(q·k/√d, axis=keys)`）：所有坍缩块
`ctrl_eff_frac` ≥ **0.99999988**。⭐ **"你 softmax 轴选错了"这个反驳被堵死**，
而且现在是每次运行都自动跑的一列，不再是当时手算一次。

`tie_qk` 的 `energy_std` 比对照大 **5.1~12.3 倍**（沿栈递增：E-SA1~4 约 5~6×，
E-SA5~8 约 8.6~12.3×），但绝对值仍是 1e-3 量级，**`eff_frac` 照样 0.999999**。
**"绑定不解除坍缩"完全成立**，8/21 的结论不动。

### 二、⚠️ 8/21 那张 D2 表作废（已在原条目加指针，数字未改）

| | 8/21 临时脚本 | 本次 | 处置 |
|---|---|---|---|
| 编码器 `energy_std` | 0.00030 / 0.00038 / 0.00010 / 0.00018 | 2.27e-4 / 2.78e-4 / 7.77e-5 / 1.50e-4 | 系统性偏高 **1.20~1.49×**，8 个点全部同向 → **定义不同**（旧脚本大概率对整个 (n_q,n_k) 矩阵取 std，按全方差分解必然更大）。**结论不变** |
| `pp_attn` D1 `eff_keys` | 2048.6~2048.7 / 2049 | **2048.60 / 2048.73 / 2048.75 / 2048.76** | ✅ **精确复现** |
| `pp_attn` D1 全局 token 权重 | 0.99~1.00× | 0.9945 / 0.9972 / 0.9980 / 0.9986 | ✅ 复现 |
| **`D2-STA4` 有效 key** | 39.7% / **3.7%** | **51.4% / 19.9%** | ❌ **复现不了，作废** |

⚠️ 那两个 D2 数字**不能**用"旧脚本没做行归一化"解释掉 —— 若是那样，`cd_rep05_full` 那格
（无饿死，归一化是恒等操作）应该对得上，而它是 39.7 vs 51.4。**旧脚本还有别的不同，
而它没入库，无从对照。** 论文引本次的。

### 三、⭐ 新发现：`tie_qk` 让 D2 的两个块把 92% 的 query 饿死

`_offset_attention` 沿 **query 轴**做 softmax，然后沿 key 轴 L1 归一化，**带一个 epsilon**：

```python
att = softmax(energy, axis=1) / (1e-9 + reduce_sum(..., axis=2))
```

一个在所有 key 上都输掉 query 轴竞争的 query，其行和 `S_q` 会落到 1e-10 量级，
**此时那个 1e-9 是分母的主要成分**，该行被压掉一个数量级。

| `tie_qk` | 行质量 | 被饿死的 query 行 | `peak_x_unif` |
|---|---:|---:|---:|
| D2-STA1 | 1.000 | 0% | 2.0 |
| **D2-STA2** | **0.079** | **92.4%** | 665.6 |
| **D2-STA3** | **0.079** | **92.2%** | 302.4 |
| D2-STA4 | 0.982 | 27.4% | 411.5 |

**全表只有 `tie_qk` 出现，`cd_rep05_full` 与 `pp_attn` 每一块都是 0%。**
跨 3 颅骨极稳（std 0.0019 / 0.0007）。行质量 0.079 反推 `S_q` ≈ 8.6e-11。

**这给 8/21 那条挂着的"只是观测，没有解释"补上了机制，同时纠正它两处**：
① 块号错了，剧变的是 **STA2/STA3** 不是 STA4；
② **"注意力大幅收窄"这个描述不准** —— 实际是沿 query 轴的极端集中：少数 query 吃掉了
几乎所有 key 的质量，**其余 92% 的解码位置拿到的注意力输出被衰减约 12 倍**。

⚠️ 但要分清同时发生的两件事：`tie_qk` 的 D2 `peak_x_unif` 是 302~666（对照只有 2.8~79），
**注意力确实也真的更尖**。饿死与尖锐是同一现象的两面（沿 query 轴集中 ⇒ 多数行没质量），
措辞上不能只说一半。

⭐ **对论文第 3 章（审计）是加分材料**：`tie_qk` 原本只有"无可复现的精度效应 + 密度确定变差"，
现在多一条机制 —— 它把解码器 D2 的两个块推进了一个**退化的归一化状态**。
这比"密度变差"更能说明为什么默认保持 `tie_qk_init=False`。

### 四、⚠️ 方法学：入了库的脚本也会算错，靠不变量抓

这个脚本在交到用户手里之后**炸了两次**，两次都是我的问题，记下来因为教训不同于以往：

**bug 1（会崩，无害）**：把派生列 `eff_frac` 列进了"模型输出"清单 → `KeyError`。
根因是我**分片测了但没端到端测** —— 测了构图、测了打印，恰好跳过中间组装行那段。

**bug 2（不崩，会给出错数）**：`keys_identical` 判的是 `key_row_spread == 0`。
实测 D1 的 key 行**确实逐位相同**（`max|行−第0行| = 0.000e+00`），但 `reduce_std` 在这些
完全相同的行上返回 **~4e-07**（它减去的均值自己就是舍入过的）。于是四个 D1 块全被判成
learned collapse，汇总会打成 **"0 structural + 12 collapsed"** —— 正好把第一节那个区分抹掉。
**这条不报错，会安静地进论文。**

**bug 3（不崩，会给出错数）**：熵直接算在**未归一化**的行上（见第三节）。
一行本来均匀铺在 3072 个 key 上、被 epsilon 压到质量 0.48 之后，`exp(熵)` 读成约 67 而非 3072
—— 看起来像极度尖锐的注意力，实际意思正相反。

> ⭐ **抓到 bug 3 靠的是一条数学不变量**：注意力行若和为 1，则 `max_k × n_keys ≥ 1`。
> 实测 `tie_qk` D2-STA3 的 `peak_x_unif = 0.71`，**在数学上不可能**，所以读的行和不为 1。
> 这条现在**写进脚本、每次运行都跑**，一旦出现就直接打警告说"这个读数是错的别用"。
>
> **教训**：校验不能只对着"我预期它是多少"，要对着"它必须满足什么"。前者只能发现
> 与预期不符的数，后者能发现自己预期本身就错的情形。
> 采样地板那次的教训是"不入库的数字可能一直是错的"；**这次是"入了库也可能错，
> 差别在于错得会不会被抓住"。**

修正生效的方式本身是验证：`cd_rep05_full` 和 `pp_attn` 的 D2 数字**一位未变**
（行质量 1.000、归一化是恒等操作），**只有 `tie_qk` 的 STA2/STA3 动了**
（0.059→0.115、0.048→0.151）—— 与修之前写下的预测完全一致。

### 五、处置

- **TODO 第 10 项关闭。** `tie_qk` / `cd_rep05_full` / `pp_attn` 的权重（约 2.1 GB）
  **现在可以删了** —— 它们被这条待办卡着的唯一理由已消除，指标已冻进 CSV
- `src/eval/README.md` 加了脚本行与「k 折后要重跑什么」行（**每个最终模型各一次**）
- ⚠️ **n=3 够不够**：`eff_frac` 跨颅骨 std 中位 1.66e-07，核心的 12/16 与编码器数字绰绰有余；
  但 `tie_qk` D2-STA3 是 0.058、`frac_starved` 在 STA4 上是 27%±12%。
  **若论文要引 `tie_qk` D2 的具体数，建议 `--n 20` 重跑一次**（约 20 分钟）
- ⚠️ `row_mass` / `frac_starved` 这条 epsilon 饿死的观察**是否写进论文未定** ——
  它是关于原架构归一化的独立发现，**不属于"自己写错的错误"** —— 已核实那个 epsilon
  逐字来自原实现（`MSN_model_inference_demo.ipynb` cell 4 与 `MSN_model_training_Demo.ipynb`
  cell 8 都有 `t / (1e-9 + tf.reduce_sum(t, axis=2, keepdims=True))`，
  `src/models/msn_demo_arch.py:126,150` 是它的逐字复制）。
  但它只在 `tie_qk` 这个**已被否决**的配置上出现，写进去要想清楚放在哪一节

## 2026-08-26（范围决策：可见区域透传移出计划 → 论文 Future Work；k 折的门槛因此满足）⭐⭐

用户当天晚些时候的三个决定，都是**回答清单上的开放问题**，不是完成工作，
所以按 CLAUDE.md 的例外条款**就地改了 TODO 第八次快照**，没另起第九次。
（这是第二次破例，第一次是 2026-08-25。）

### 一、⭐ 可见区域透传：**本次不做**，写进论文 Future Work

它一直是「唯一还在牌桌上的实验项」，也是预期收益最大的一项。用户决定不做，
**在论文里作为后续展望呈现**。

⚠️ **措辞是这条的关键**：要写成「下一步该怎么走」，不是「我们本来想做但没做完」。
区别在于**有没有依据**。本项目恰好有三条实测依据，全部写进去：

1. **瓶颈是量出来的，不是猜的。** dropout 那轮（8/06）证明模型**欠拟合**
   （val/train 1.03~1.08×，train CD_t 6.86mm 远非记忆化），所以问题不在容量，
   在 `E-OUT_MaxPool` 把整颗颅骨压成一个 4096 维向量这个信息通道。
2. ⭐ **它绕开了杀死 `pp_attn` 的那个原因。** `pp_attn` 试图让 D1 学会「该看哪儿」，
   实测注意力从未启动（`attention_collapse.csv`：`eff_frac` 0.9998、
   `energy_std` 0.015~0.019）。**透传的路由是写死的，不需要学习** ——
   这不是"再试一次同样的事"，是攻击同一瓶颈的**非学习**路径。
3. **评测口径已经就位。** 透传会让 CD_t 虚高（可见区照抄），必须以缺损区覆盖为准，
   而这套指标本项目已经建好并验证过（2×2 在它上面四格全显著、全点云全不显著）。

**这三条让 Future Work 那一节读起来是「我们知道下一步该做什么、也知道为什么它会work」，
而不是「时间不够」。** 顺带：文本分支那个「杠杆」判决性实验（8/25 已移交 future work）
和 SkullBreak / 体素基线也归到同一节。

### 二、⭐⭐ 连带后果：k 折的前置条件满足了

第 1 项（k 折）挂着的门槛一直是**「所有改进做完之后的终审，不是现在做的事」**。
透传移出计划后，🟡 实验那一节**已无任何计划中的项目**（3~8 一直只是备选）。

> **所以 k 折现在是关键路径上唯一的大件**（4 配置 × 5 折 ≈ 18~20h），
> 它跑完之后剩下的就全是写作了。**要不要开跑待用户拍板**，已在 TODO 标出。

⚠️ 开跑前把这几条一并带上（散在各处，集中记一次）：
①四格全跑；②`--epochs` 给足 —— `cd_only`（@222 6.423 → @300 6.353，还在降 0.07）
与 `lr_fix_only`（val_cd_t 直到 277 轮仍在改善）都属「没走平」那一档，
别让任何一格在下行段被截断；③按训练间方差 0.15mm 算，5 折均值标准误约 0.09mm，
而 2×2 的效应是 0.17~0.20mm → t ≈ 2，**勉强够、不宽裕**，别指望它分辨 0.1mm 以下；
④卡在边缘时补救是**关键格每折跑两次**，不是加折数（折数只减划分方差，减不了训练方差）；
⑤`report.paired_stats` 在 k 折下不适用，得新写折间聚合，
`MSN_compare_runs.ipynb` 第 1 节那条 assert 会先拦下你。

### 三、两条小的

- **`attention_collapse.py --n 20` 重跑：不做，该条已从 TODO 删除。**
  `experiments_log/README.md` 里那段改写成**读表时的注意事项**而不是待办：
  12/16 的计数、structural/learned 的划分、编码器各数都远在噪声之上可以直接引；
  `tie_qk` D2-STA3（std 0.058）和 `frac_starved`（27%±12%）**只能引数量级**。
- **`tie_qk` / `cd_rep05_full` / `pp_attn` 的权重先留着。** 保留理由（注意力自查）
  确已消除、指标也冻进 CSV 了，但磁盘不紧张，而**删权重是单向的**（训练不可逐位复现）。
  不做没必要的单向操作。


## 2026-08-26（TODO 第 11 项：粗糙度度量入库 —— **旧数字的符号是反的**，但正确答案是第三种）⭐⭐

```bash
/root/miniconda3/envs/comp0190-msn/bin/python src/eval/roughness.py
```
`mesh_viz.local_roughness` + `src/eval/roughness.py` → `experiments_log/roughness.csv`
（`cd_rep05_full` × 8 颗验证颅骨 × 5 个 k，合并写，约 1 分钟）。

**这条待办支撑的是一个否定性决策**：导师提议加表面平滑惩罚项，2026-08-05 的测量说
"GT 0.736 vs pred 0.760，基本持平、没有可捡的便宜"，于是整个表面质量方向转向**密度**
（→ repulsion，全项目最硬的结论）。而那个脚本没入库，devlog 8/07 自己标过
"这个结论目前无法复现，不应直接写进论文"。

### 一、⭐ 结论：**测不出差别** —— 三种可能里的第三种

| k=16（原始论断的口径） | GT | 预测 | Δ |
|---|---:|---:|---:|
| `rough_norm`（局部平面残差 / 点间距） | **0.512** | **0.496** | **−0.016** |
| 旧脚本（已丢失） | 0.736 | 0.760 | **+0.024** |

**绝对值和符号都对不上。** 但按 Δ 的均值读成"预测更平滑"同样是错的 ——
逐颅骨 std **0.029**、配对 SE **0.010**、**Wilcoxon p = 0.195、3/8 颗方向相反**，
按本项目的判决清单远不过线。各 k 上方向一致（都是负），但**没有一档显著**。

> **正确表述：GT 与预测的粗糙度在统计上无法区分。**
> n=8 的检出下限约 Δ≈0.03（GT 值的 6%），所以能说的是：
> **若真有差距，也在 6% 以内、方向未知。**
> ⭐ **平滑惩罚项没有可优化的靶子 —— 2026-08-05 的决策成立，而且理由比原来硬**：
> 原来是"差距很小"，现在是"差距测不出来，且我们知道能测到多小"。

### 二、⚠️ 第三个临时脚本数字，第三个被发现有错的

```
采样地板       4.43   → 实测 4.619        偏低，方法不同
注意力 D2      39.7% / 3.7%  → 51.4% / 19.9%   复现不了
粗糙度         +0.024 → −0.016（且不显著）  连符号都反了
```

**三个全中。** `src/eval/README.md` 开头那条约定（新脚本必须进表）不是洁癖，
已在那里补了这句。

### 三、顺带归档了「没有哪个 k 是干净的」

这个论证此前同样只在 devlog、同样来自丢失的脚本。`spread_mm` = 邻域沿法向的展开
（即"这个邻域自己有多厚"）：

| k | 8 | 16 | 24 | 48 | 128 |
|---|---:|---:|---:|---:|---:|
| GT `spread_mm` | 5.44 | 7.87 | 8.72 | 10.25 | **14.17** |

骨壳厚 5~7mm。若污染只来自厚度，这一列该在 6mm 附近**走平**；实测一路爬到 14.17mm
→ **大 k 处污染换成了颅骨自身的曲率**。旧记的 k 扫描（3.48/5.50/6.31/7.52/8.98）
绝对值同样对不上（定义不同），但**形状一致：单调爬升、从不走平** —— 这个论证本身经受住了复核。

⭐ 合成数据把污染从"断言"变成了"演示"（写进 `local_roughness` 的 docstring）：
**一个完全平整的表面**，仅因存在 6mm 的第二层，粗糙度就从 0.00mm 被抬到 1.56mm，
而 `spread_mm` 读出 6.73mm ≈ 层间距。

### 四、一个观察，不构成结论

**k=128 时预测在 8/8 颗颅骨上都比 GT 平滑**（Δ −0.108）。方向与 devlog 2026-08-05
的一个旧猜测一致 —— GT 来自二值体素的 marching cubes、带阶梯伪影，模型不复现它，
所以"模型的表面在解剖学上可能比 GT 更正确"。⚠️ 但 k=128 正是曲率污染最重的一档，
**不要拿它下结论**，记在这里备查。

### 五、写作口径（这条的实际用途）

论文若要交代"为什么没做平滑惩罚"，现在可以写成**测量结论**而不是印象：

> 我们在 8 颗验证颅骨上比较了预测与真值的归一化局部粗糙度，未发现可测差异
> （Δ = −0.016，配对 Wilcoxon p = 0.20；该设计可检出的最小差异约为真值的 6%）。
> 因此平滑先验没有可优化的目标，表面质量的工作转向了点密度分布。
> ⚠️ 该度量受颅骨骨壳厚度污染，绝对值不可解释；报告的是同一有偏度量下的**差值**。

⚠️ **不得引用 0.736 / 0.760**（旧值，符号错）。

### 六、这次的流程

⭐ **按 8/26 定的规矩，这个脚本是我自己端到端跑过之后才交付的**（GPU 空闲，1 MiB/24 GiB），
且先在合成几何上验了不变量：平面残差必须为 0 ✅、法向噪声 σ 必须被还原 ✅、
球面残差必须随 k 单调增 ✅、尺度不变性 ✅、双层壳的展开必须≈层间距 ——
**第五条第一次没过，查下来是我的测试构造错了**（点太密、k=16 的邻域半径够不着另一层），
按真实比例（面内 4mm / 层间 6mm）重做后通过。这次没让用户替我踩坑。

---

## 📎 补充记录（2026-08-07）— 讨论所得，**不构成待办**

> 以下是训练间隙的讨论笔记，包含**对先前记录的一处更正**、**一处可复现性问题**，
> 以及两条备选思路。除了标 ⚠️ 的两条属于"已有记录有误、需要知道"，
> 其余**只是记下来备查，不一定要做**。

### ⚠️ 更正：「换个更大的邻域去度量粗糙度」这条不成立

前面「平滑惩罚项：反对与支持的理由」里我写过——粗糙度是尺度相关的，
k=16 邻域看不到 10~20mm 尺度的波浪起伏，"换个更大的邻域，结论可能完全不同"。

**实测否定了这条。** GT 上按 k 统计局部邻域的展开（4 颗颅骨中位数）：

| k | 邻域半径 | **法向展开** | 面内展开 | 平面度 λ0/Σλ |
|---:|---:|---:|---:|---:|
| 8 | 7.10mm | 3.48mm | 7.54mm | 0.129 |
| 16 | 9.34mm | 5.50mm | 9.39mm | 0.173 |
| **24**（`signed_deviation` 现用） | 11.03mm | **6.31mm** | 10.95mm | 0.164 |
| 48 | 14.91mm | 7.52mm | 14.78mm | 0.125 |
| 128 | 23.41mm | 8.98mm | 23.29mm | 0.073 |

（平面度：完美平面=0，各向同性=0.333）

- **k=24 时法向展开 6.31mm，正好等于骨壳厚度** —— 证实了"局部平面拟合被内外两层表面污染"。
- **但法向展开并没有在 6mm 处饱和**，一路涨到 8.98mm。若纯粹是厚度，邻域够大后应该停住。
  继续涨说明大 k 时污染源换成了**曲率**：23mm 的патch 上颅骨自身弯曲就有好几毫米。
- **结论：没有哪个 k 是干净的。小 k 被厚度污染，大 k 被曲率污染。换尺度不是解法。**

要做的话得换机制（拟合二次曲面吃掉曲率，或用法向把邻居限制在同一侧表面），
比原先设想的工作量大得多。

**一点缓和**：对 GT 和 pred 用完全相同的有偏度量时，污染是共模的，**差值仍有部分意义**。
所以"GT 0.736 vs pred 0.760"这个比较比它的绝对值可信 —— 但也不完全干净，
两者点密度不同，污染程度就不同。

### ⚠️ 可复现性问题：粗糙度那两个数字，代码没留下

`GT 0.736 vs pred 0.760` 来自当时一个临时脚本，**仓库里没有任何粗糙度度量的代码**
（`mesh_viz.surface_stats` 只有 clump/spacing/deviation 那几项）。

**所以"局部粗糙度基本持平"这个结论目前无法复现，不应直接写进论文。**
若要保留这个论据，第一步是把度量正式补进 `mesh_viz.py`。

### 记录：mesh 可视化不是 Poisson，以及这带来的盲区

有文献指出"点云分布不均会导致 Poisson 重建失败或出现破洞"。
本项目的 `pc_to_mesh` **不是 Poisson**，而是距离场 + marching cubes
（原因见 `mesh_viz.py` 的 docstring：Poisson 需要法向、薄壳法向不可靠、且会补掉缺损洞）。

**两者失效模式相反**：
- 距离场法 = 每点套一个 6mm 球取并集。**点挤在一起只是浪费，不产生洞**；
  只有点太稀疏（间距 > 2×半径 = 12mm）才会破洞。实测最大间距 12.7mm、绝大多数 3~5mm，
  所以 12% 扎堆对它几乎无影响 —— **这解释了"重叠严重却看不到破洞"**。
- Poisson 靠法向解方程，局部过密/过疏都会让法向估计不稳，**才会对分布不均敏感**。

**由此产生一个盲区（值得记）**：当前可视化**按设计就看不见密度问题的后果**。
于是"扎堆 12%"这个数字和"临床可用性"之间**证据链是断的**。

→ 若要补上，可**额外跑一次 Poisson 重建**（open3d / trimesh 都有），
展示它在扎堆点云上退化、在改善后成功。成本不高，且是答辩容易被问到的地方。
**记录备选，不列入待办。**

### 备选思路：点到面引力，可能比 Laplacian 平滑惩罚更合适

文献里的 **Attraction-Repulsion Loss** 其实就是把两股力打包命名：
attraction = 把预测点拉向真实表面（**CD 已经是**），repulsion = 把预测点彼此推开（已加）。
所以当前的 `CD + repulsion` **已经是**一个 attraction-repulsion 损失，只是没这么叫。
**不需要"一对一真实点"才能加引力** —— 这个顾虑不成立。

真正值得借鉴的是引力的**形式**：

- **CD 是"点到点"**：把预测点拉向某个离散采样点。而 GT 点只是真实表面的采样、不是表面本身，
  所以预测点被拉到"某个采样点附近"就满意了，**在表面两侧散开照样满意** ——
  这正是 `signed_deviation` 测到的 ±2mm 锯齿的来源。
- **"点到面"**：拟合 GT 局部平面，把预测点拉向**平面**。

和 Laplacian 平滑惩罚相比：

| | Laplacian 平滑惩罚 | 点到面引力 |
|---|---|---|
| 依据 | 通用先验（"表面应该光滑"） | **GT 的真实信息** |
| 与 repulsion 的关系 | ⚠️ **直接对抗**（一个拉拢一个推开） | ✅ **正交**：只沿法向拉，切向不管 |
| 能否验收 | ❌ 受阻于上面那个度量问题 | ✅ **`signed_deviation` 现成可测**（dev_std 应下降） |
| 成本 | 每步要对预测点算 kNN | ✅ **GT 固定，局部平面可在数据准备阶段预计算并缓存** |

⚠️ 已知副作用：点到面引力**不约束切向**，点可沿表面自由滑动，**必须和 repulsion 配套**
（正好已经有了，两者是天然一对）。

**如果最终要满足导师"让表面更平滑"的诉求，这条路可能比 Laplacian 更好走**：
目标一致（消除表面两侧的散布）、可验收、不和 repulsion 打架、还更便宜。
**仍属备选，不列入待办。**

---

## 待办总览（截至 2026-08-06，合并历次清单）

> ⚠️ **这一节已冻结，不再更新。** 待办清单从 2026-08-20 起移到仓库根目录的
> **[`TODO.md`](TODO.md)**，那份是追加式的：每完成一项就另起一节写日期、
> 重列完整清单，不在原地覆盖。**当前有效的清单永远是 `TODO.md` 的最后一节。**
> 下面这份保留作历史快照。

### 已完成
- ~~`dcd_weight` 参数化~~ ✅ 有效但兑换率差，弃用（保持默认 1）。
- ~~`dcd_lambda` 实验~~ ✅ 改善精度而非密度，采纳 λ=2。
- ~~"密度这条路能走多远"~~ ✅ **结掉了**：DCD 超参调不动密度，转下面的 roadmap。

### 基础（改动小）
| 方向 | 内容 | 预期 |
|---|---|---|
| ~~**正则化**~~ | ❌ **已否决并删除代码**。实测 val/train = 1.03~1.08×，模型不过拟合；dropout 让 val CD_t 差 2.07 倍。详见 2026-08-06 条目 | — |
| **数据增强** | 绕 SI 轴小角度旋转 + 轻微 jitter。⚠️ 大角度旋转会破坏颅骨在 LPS 下的方向先验。⚠️ **理由已改**：不再是"防过拟合"（前提被否），只能是"提升覆盖/多样性"，预期下调 | 小~中 |
| 收敛曲线重扫 | 用 `--run-name` 扫几个 `--minutes`，更新 notebook 里那张过时的"3/5/7分钟"表 | 无（只是修文档） |

### 进阶（改动大）
| 方向 | 内容 | 预期 |
|---|---|---|
| **可见区域透传** | 解码器现在只吃编码器 max-pool 出的**一个全局向量**，残缺颅骨里本来完好的表面也被整个重新生成。补上"可见区域直接透传 + 只生成缺损部分"的合并逻辑 | **最大** |
| **预训练权重微调** | 用 `MSN_weights3.h5` 微调而非从零训练。⚠️ 障碍：权重只认 `msn_demo_arch` 那套拓扑，加载不进 `msn_skullfix`，得先解决架构问题 | 中~大 |
| 真 FPS 采样 | `PointSampler(mode="unique")` 只是不重复随机采样，不是真正的 FPS。数据准备已在用 `fpsample`，可搬进 encoder 的 `E-SG1`/`E-SG2` | 小~中 |
| 模型容量 | 在 `paper()`(187M) 和 `small()`(9.4M) 之间找一个更匹配 100 样本规模的中间值 | 小~中 |

### 数据工程
| 方向 | 内容 | 预期 |
|---|---|---|
| **接入 SkullBreak** | README 里提到的目标数据集之一，从根本上缓解样本量不足 | **比任何单项建模改动都大** |

### 保留待做（导师提出）
| 方向 | 内容 |
|---|---|
| **平滑惩罚项** | 正反理由见前面条目。⚠️ 原先写的"先补多尺度粗糙度度量（k=16/48/128）"**已被实测否定** —— 没有哪个 k 是干净的，见 2026-08-07 补充记录。若做，要么换度量机制（二次曲面/同侧法向约束），要么改走**点到面引力**那条备选路径 |

### 建议顺序（2026-08-07 DCD 消融之后重排）
1. **k 折交叉验证** —— 现在这是最紧的一件。所有结论都建立在单折 20 颗上，
   而且 `rep_w05` 只跑过一次、消融的 2×2 还缺一格。k 折能一次性把这些
   统一到同一口径，顺带给出均值±标准差 —— 论文本来就该这样报。
   **纳入 k 折的配置：完整 2×2（CD / CD+DCD / CD+repulsion / CD+DCD+repulsion）。**
   单折的 2×2 已在 2026-08-20 跑完，k 折是把它抬到可发表的口径 ——
   目前每格只有 1~2 次运行，且各配置的重复性只在 CD+repulsion 上验证过。
2. ~~缺损区限定的 CD / HD95 / F1~~ ✅ **已完成（2026-08-20）**。结论：改进确实
   作用在难的那部分（缺损覆盖 -17.3% vs 全点云 -12.0%），且暴露出一个全点云
   指标看不到的交互效应（DCD 与 repulsion 一起用反而更差）。**论文应以缺损区
   指标为主。**
3. **可见区域透传** —— 收益预期最大，瓶颈是 `E-OUT_MaxPool` 把整颗颅骨压成
   一个全局向量。⚠️ 会让 CD_t 大幅变好但部分属「不劳而获」，**必须先有第 2 项
   才能判断真实收益**。工作量最大。
4. **平滑惩罚项** —— 必须先解决度量问题（原计划的多尺度粗糙度已被否定，
   见 2026-08-07 补充记录）。⚠️ 与 repulsion **方向相反**（前者把点拉向邻域质心，
   后者把点推开），若都做必须让它们作用在不同尺度。或改走「点到面引力」。

~~Repulsion loss~~ ✅ 已完成，是密度问题的有效解（扎堆 5.6% → 1.4%）。
~~DCD 消融~~ ✅ 已完成，**2×2 已补齐**（2026-08-20）：最优配置是 CD + repulsion，
DCD 在有 repulsion 时冗余。DCD 单独仍有效（扎堆 13.6% → 5.6%），只是弱得多。
~~正则化~~ ❌ 已否决，模型不过拟合。

**加损失项的原则（已被这一轮验证）：一次只加一个，加完要考虑剪枝。**
"证明可以去掉一项"比"叠更多项"是更强的结果 —— 这次就是这样收的。

"密度这条路能走多远"已由 `dcd_w3` / `dcd_l2` 两次实验结掉（见上），
DCD 超参不用再扫了，直接从 1 开始。
