# 待办清单 (TODO)

## 如何用这个文件

**规则：完成某项之后，不要在原清单上改，而是在文件末尾另起一节，写上日期，
把更新后的完整清单重新列一遍。**

这样做的原因：清单本身会随认识变化。某一项被否决、某一项的理由被推翻、优先级
重排 —— 这些都是有信息量的，就地覆盖会把它们抹掉。保留历次快照，之后回看时能
知道"当时为什么这么排"，写论文时也不用重新推导判断过程。

- **最末尾的那一节永远是当前有效的清单**，前面的都是历史快照。
- 每节的标题格式：`## YYYY-MM-DD — 触发这次更新的事`
- 完成的项用 `~~删除线~~ ✅` 保留在清单里，附一句结论，不要直接删掉。
- 被否决的项用 `❌` 保留，附否决理由。
- 详细的实验记录仍然写 `devlog.md`；这里只放"接下来做什么"。

---

## 2026-08-20 — 首次建立（缺损区限定指标完成后）

### 🔴 优先级最高

| | 内容 | 成本 | 为什么现在做 |
|---|---|---|---|
| **1** | **k 折交叉验证**，配置为完整 2×2（CD / CD+DCD / CD+rep / CD+DCD+rep） | 4 配置 × 5 折 ≈ 15h<br>2 配置 ≈ 7.5h | 一次性解决五件事：所有结论都是单折 20 颗；`lr_fix`/`rep_w05`/`cd_only` 各只跑过一次；噪声底线只在 CD+rep 上验证过；前四轮精度只有 ~0.25mm；**以及确认缺损区那个交互效应**。代码 `--n-folds` 早就就位，从没跑过 |

### 🟡 实验（要跑训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| 2 | **逐点交叉注意力** —— 让解码器 attend 编码器的逐点特征，而不是一个复制出来的全局向量 | ~10 行 + 测显存 | **实测当前注意力是退化的**（key 全都一样 → 权重恒为均匀 0.125）。同时修掉"信息通道过窄"和"注意力没在工作"两个问题，且**不会让指标虚高**。比透传便宜得多 |
| 3 | **可见区域透传** | 最大 | ⚠️ 会让 CD_t 大幅变好但部分属"不劳而获"。缺损区指标已就位，现在**可以判断真实收益了**。⚠️ 原 demo 也没有这一步，不是我们删掉的 |
| 4 | 数据增强（绕 SI 轴小角度旋转 + jitter） | 小 | ⚠️ 理由已改写：不再是"防过拟合"（前提被否），只能是"提升覆盖"，预期下调 |
| 5 | 平滑惩罚项 / **点到面引力** | 中 | 原计划的多尺度粗糙度度量已被实测否定（没有哪个邻域尺度干净）。若做，改走点到面引力：与 repulsion 正交、可用现成 `signed_deviation` 验收、GT 局部平面可离线预计算 |
| 6 | 预训练权重微调 | 中~大 | ⚠️ 障碍：权重只认 `msn_demo_arch` 拓扑，加载不进 `msn_skullfix` |
| 7 | 真 FPS 采样（搬进 encoder 的 `E-SG1`/`E-SG2`） | 小~中 | 现在的 `mode="unique"` 只是不重复随机采样 |
| 8 | 模型容量中间值（187M 与 9.4M 之间） | 小~中 | |
| 9 | 接入 SkullBreak | 大 | 预期比任何单项建模改动都大 |

### 🟢 评测（不用训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| 10 | **粗糙度度量正式补进 `mesh_viz.py`** | 半天 | ⚠️ 现在那两个数字（GT 0.736 / pred 0.760）**来自临时脚本、无法复现**，不该写进论文。⚠️ 但先得解决度量本身的问题（见第 5 项） |
| 11 | Poisson 重建对照 | 半天 | 当前可视化按设计就看不见密度问题的后果，"扎堆率 → 临床可用"的证据链是断的 |
| 12 | 缺损区**精度**方向的改进 | — | 覆盖 3.24mm 已接近采样地板（约 2.2mm），空间不大；**精度 4.37mm 离地板更远，还有空间**。要优化盯这个 |

### 📌 文档 / 论文（最容易漏，但都影响论文）

| | 内容 | 挂了多久 |
|---|---|---|
| **13** | **查证 `MSN_weights3.h5` 是否见过颅骨数据** —— 决定「zero-shot」这个措辞能不能用。已知 MedShapeNet 含 bones 类且部分源自 AutoImplant，但**没去数据集主页核实类别清单**。落实前对照组只能用中性表述 | 8/05 |
| **14** | **`README.md` 仍写着 "voxel baseline" 和 "self-built corrupted dataset"** —— 而这两项都没有代码。**需要和导师确认是正式收窄还是延后** | 8/06 |
| 15 | **采样地板必须写进论文** —— 6144 点下 CD_t 地板 4.43mm、HD95 地板 3.79mm（同一表面两次独立采样实测）。**本文数值不可与体素域方法直接比较**（AutoImplant 2020 冠军 HD95 = 1.52mm） | 8/20（新） |
| 16 | `PROGRESS.md` 还停在 6/25 的"P0 建仓库" | 用户说先不写 |

### ⚙️ 已知但未修的小问题

| | 内容 |
|---|---|
| 17 | `UniformSampler` 推理不确定性 —— 同一输入两次调用差 1.03。基线 notebook 固定了全局种子，但要逐位可复现得改 demo 架构 |
| 18 | λ=2 那个"梯度饱和导致死亡"的假说**始终没验证**（要测扎堆点上的梯度） |
| 19 | 前四轮（`baseline`/`dcd_w3`/`dcd_l2`/`rep05_void`）跑在无学习率衰减条件下，其"最优值"是彩票，精度只有 ~0.25mm。它们之间的细微差异不可读 |
| 20 | 改 `report.py` / `mesh_viz.py` 之后 notebook 需要 `importlib.reload` 或重启 kernel，否则拿到的是缓存的旧模块 |

### 🔮 如果要做体素基线（目前已延后）

体素模型输出的是二值体积，和 GT 的 `.nrrd` 同类。**把它的输出丢进
`prepare_skullfix.py` 同一条管线**（marching cubes → 密集采样 → FPS 到 6144），
两边就完全同口径。采样地板是共模的，所以"谁更好"的比较依然有效 ——
只是不能拿去和论文发表的体素指标并排。

### ✅ 已完成 / ❌ 已否决

- ~~repulsion loss~~ ✅ 密度问题的有效解（扎堆 5.6% → 1.4%），且不损失精度
- ~~DCD 消融（完整 2×2）~~ ✅ **最优配置是 CD + repulsion，不要 DCD**。DCD 单独仍有效（扎堆 13.6% → 5.6%），只是远弱于 repulsion；两者一起用在缺损区反而互相干扰
- ~~缺损区限定指标~~ ✅ 改进确实作用在难的那部分（缺损覆盖 -17.3% vs 全点云 -12.0%）。**论文应以缺损区指标为主**
- ~~修 `ReduceLROnPlateau`~~ ✅ 本项目至今最大的单项收益（CD_t 6.945 → 6.317），此前从未触发过
- ~~`run.json` 的 seed 措辞~~ ✅ 已注明只固定划分与初始化，训练本身不可复现
- ~~`dcd_weight` / `dcd_lambda` 调参~~ ❌ 兑换率差 / 收益小于噪声，DCD 超参不用再扫
- ~~正则化（dropout / weight decay）~~ ❌ 模型根本不过拟合（val/train 1.03~1.08×），dropout 让 CD_t 差 2.07 倍，代码已删
- ~~体素基线~~ ⏸ 范围决策，暂缓（见第 14 项）

---

## 2026-08-20（第二次）— 缺损区掩码定义修正之后

**触发**：验证"模型过量填充且填不准"这个结论时发现它是错的 —— 预测侧的掩码把
洞周围一圈漂移点也圈了进来。掩码已修正，devlog 对应一节已撤回。
**清单相对上一版只动了第 12 项**（它建立在那个错误结论上），其余不变。

### 🔴 优先级最高

| | 内容 | 成本 | 为什么现在做 |
|---|---|---|---|
| **1** | **k 折交叉验证**，配置为完整 2×2 | 4 配置 × 5 折 ≈ 15h<br>2 配置 ≈ 7.5h | 一次性解决五件事：所有结论都是单折 20 颗；`lr_fix`/`rep_w05`/`cd_only` 各只跑过一次；噪声底线只在 CD+rep 上验证过；前四轮精度只有 ~0.25mm；**以及确认缺损区那个交互效应** |

### 🟡 实验（要跑训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| 2 | **逐点交叉注意力** | ~10 行 + 测显存 | 实测当前注意力退化（key 全一样 → 权重恒为均匀 0.125）。同时修掉"通道过窄"和"注意力没工作"，且不会让指标虚高 |
| 3 | **可见区域透传** | 最大 | ⚠️ CD_t 会大幅变好但部分属"不劳而获"。缺损区指标已就位，可以判断真实收益了 |
| 4 | 数据增强 | 小 | ⚠️ 理由已改写为"提升覆盖"，预期下调 |
| 5 | 平滑惩罚项 / **点到面引力** | 中 | 原计划的多尺度粗糙度度量已被否定，改走点到面引力 |
| 6 | 预训练权重微调 | 中~大 | ⚠️ 权重只认 `msn_demo_arch` 拓扑 |
| 7 | 真 FPS 采样 | 小~中 | |
| 8 | 模型容量中间值 | 小~中 | |
| 9 | 接入 SkullBreak | 大 | 预期比任何单项建模改动都大 |

### 🟢 评测（不用训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| 10 | **粗糙度度量正式补进 `mesh_viz.py`** | 半天 | ⚠️ 现有那两个数字来自临时脚本、无法复现。先得解决度量本身的问题（见第 5 项） |
| 11 | Poisson 重建对照 | 半天 | "扎堆率 → 临床可用"的证据链目前是断的 |
| ~~12~~ | ~~缺损区**精度**方向的改进~~ | — | ❌ **删除。** 这一项建立在被撤回的结论上。修正掩码后各配置的缺损精度全部落在 2.89~3.01mm、**根本不区分模型**；有判别力的是**覆盖**（3.24~3.91mm，跨度是噪声的 90 倍）。而覆盖已接近采样地板（单向约 2.2mm），空间同样有限。**缺损区这条线的优化空间比原以为的小。** |

### 📌 文档 / 论文

| | 内容 | 挂了多久 |
|---|---|---|
| **13** | **查证 `MSN_weights3.h5` 是否见过颅骨数据** —— 决定「zero-shot」措辞能不能用 | 8/05 |
| **14** | **`README.md` 仍写着 "voxel baseline" 和 "self-built corrupted dataset"** —— 需和导师确认是收窄还是延后 | 8/06 |
| 15 | **采样地板必须写进论文** —— 6144 点下 CD_t 地板 4.43mm、HD95 地板 3.79mm。不可与体素域方法直接比较（AutoImplant 冠军 HD95 = 1.52mm） | 8/20 |
| 16 | `PROGRESS.md` 还停在 6/25 | 用户说先不写 |

### ⚙️ 已知但未修

| | 内容 |
|---|---|
| 17 | `UniformSampler` 推理不确定性（同输入两次调用差 1.03） |
| 18 | λ=2 那个"梯度饱和"假说始终没验证 |
| 19 | 前四轮跑在无学习率衰减条件下，"最优值"是彩票，精度只有 ~0.25mm |
| 20 | 改 `report.py` / `mesh_viz.py` 后 notebook 需 `importlib.reload` 或重启 kernel |

### 🔮 如果要做体素基线（目前已延后）

体素输出丢进 `prepare_skullfix.py` 同一条管线（marching cubes → 密集采样 → FPS
到 6144），两边就同口径。采样地板是共模的，比较依然有效 —— 只是不能和论文发表
的体素指标并排。

### ✅ 已完成 / ❌ 已否决

- ~~repulsion loss~~ ✅ 密度问题的有效解（扎堆 5.6% → 1.4%），不损失精度
- ~~DCD 消融（完整 2×2）~~ ✅ **最优配置是 CD + repulsion**。DCD 单独仍有效（13.6% → 5.6%）但远弱；两者一起用在缺损区反而互相干扰
- ~~缺损区限定指标~~ ✅ 改进确实作用在难的那部分（缺损覆盖 -17.3% vs 全点云 -12.0%）。**论文应以缺损区覆盖为主指标**
- ~~缺损区掩码定义修正~~ ✅ **8/20 新增。** 预测侧改用"距缺损 GT 点 < 5mm"；换定义前做了排名检验，结果对自家配置不利，说明不是注水
- ~~修 `ReduceLROnPlateau`~~ ✅ 至今最大的单项收益（6.945 → 6.317）
- ~~`run.json` 的 seed 措辞~~ ✅ 已注明训练本身不可复现
- ~~`dcd_weight` / `dcd_lambda` 调参~~ ❌ 兑换率差 / 收益小于噪声
- ~~正则化~~ ❌ 模型不过拟合，dropout 让 CD_t 差 2.07 倍，代码已删
- ~~体素基线~~ ⏸ 范围决策，暂缓（见第 14 项）

---

## 2026-08-21（第三次）— 逐点交叉注意力被否决之后

**触发**：第 2 项（逐点交叉注意力）跑完，**全面变差且原因查清**——注意力从未启动。
同时 `--no-text` 那一轮暴露出一条待确认的新线索。
**相对上一版的改动**：第 2 项移入否决区；透传升为最高优先级的实验项；
新增两项（`notext_r2` 复核、编码器注意力坍缩自查）。

### 🔴 优先级最高

| | 内容 | 成本 | 为什么现在做 |
|---|---|---|---|
| **1** | **k 折交叉验证**，配置为完整 2×2 | 4 配置 × 5 折 ≈ 15h<br>2 配置 ≈ 7.5h | 理由不变：所有结论都是单折 20 颗；`lr_fix`/`rep_w05`/`cd_only` 各只跑过一次；噪声底线只在 CD+rep 上验证过；确认缺损区那个交互效应。**逐点注意力被否决后，架构侧短期没有别的动作，现在正是做它的时候** |

### 🟡 实验（要跑训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **2** | **可见区域透传** | 最大 | **升为架构侧唯一在牌桌上的选项。** ⚠️ CD_t 会大幅变好但部分属"不劳而获"，读结果**必须以缺损区覆盖为准**。<br>⭐ **逐点注意力的失败不构成反对理由**：那次死于"要从 80 个样本里学会该看哪儿"，而透传的路由是**写死的、不需要学习**。而且那次实验**并没有检验到"max-pool 是信息瓶颈"这个假设**，该假设仍然成立且仍未验证——透传是攻击同一瓶颈的非学习路径 |
| **3** | **`notext_r2`** —— 同配置重复 `--no-text` | 45~60 分钟 | 复核 devlog 2026-08-21 那条：去掉文本分支后缺损覆盖变差 0.17mm（26× 噪声）。**噪声底线是从别的配置借来的，本配置自己没重复过。** 在它跑出来之前，那条结论不进论文 |
| 4 | 数据增强 | 小 | ⚠️ 理由已改写为"提升覆盖"，预期下调 |
| 5 | 平滑惩罚项 / **点到面引力** | 中 | 多尺度粗糙度度量已被否定，改走点到面引力 |
| 6 | 预训练权重微调 | 中~大 | ⚠️ 权重只认 `msn_demo_arch` 拓扑 |
| 7 | 真 FPS 采样 | 小~中 | |
| 8 | 模型容量中间值 | 小~中 | |
| 9 | 接入 SkullBreak | 大 | 预期比任何单项建模改动都大 |

### 🟢 评测（不用训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **10** | ⭐ **自查编码器的 8 个 self-attention 是否也坍缩** | 半小时 | **新增，而且应该先做。** 逐点注意力那轮实测 D1 的 `energy std = 0.02`、有效 key 数 2048.7/2049 —— 完全没学出选择性。**同样的坍缩有没有发生在 `E-SA1..8` 和 D2 上，我没查过。** 若整个模型的注意力都是均匀的，那对论文的分量完全不同（"我们用了 PCT" vs "PCT 的注意力在这个规模下没工作"）。成本极低，信息量极大 |
| 11 | **粗糙度度量正式补进 `mesh_viz.py`** | 半天 | ⚠️ 现有那两个数字来自临时脚本、无法复现。先得解决度量本身（见第 5 项） |
| 12 | Poisson 重建对照 | 半天 | ⚠️ **优先级应上调**：它是唯一能回答"离临床多远"的路径。当前 6144 点下 HD95 地板 3.79mm，而 AutoImplant 冠军是 1.52mm（0.45mm 体素网格）—— 这个差距**主要来自表示方式而非模型**，只有过一遍体素才能拆开 |

### 📌 文档 / 论文

| | 内容 | 挂了多久 |
|---|---|---|
| **13** | **查证 `MSN_weights3.h5` 是否见过颅骨数据** —— 决定「zero-shot」措辞能不能用 | 8/05 |
| **14** | **`README.md` 仍写着 "voxel baseline" 和 "self-built corrupted dataset"** —— 需和导师确认是收窄还是延后 | 8/06 |
| 15 | **采样地板必须写进论文** —— 6144 点下 CD_t 地板 4.43mm、HD95 地板 3.79mm。不可与体素域方法直接比较 | 8/20 |
| 16 | `PROGRESS.md` 还停在 6/25 | 用户说先不写 |

### ⚙️ 已知但未修

| | 内容 |
|---|---|
| 17 | `UniformSampler` 推理不确定性（同输入两次调用差 1.03） |
| 18 | λ=2 那个"梯度饱和"假说始终没验证 |
| 19 | 前四轮跑在无学习率衰减条件下，"最优值"是彩票，精度只有 ~0.25mm |
| 20 | 改 `report.py` / `mesh_viz.py` 后 notebook 需 `importlib.reload` 或重启 kernel |
| 21 | **8/21 新增**：`--no-text` 那条机制解释（隐式偏置学习率放大 ≈ ‖t‖²）与 Adam 的逐参数自适应**存在张力**，未解决。判决性实验：给 4 个 `D1-STA{i}_LBR_lin` 的 bias 单独调大学习率，跑一轮无文本的 |

### 🔮 如果要做体素基线（目前已延后）

体素输出丢进 `prepare_skullfix.py` 同一条管线（marching cubes → 密集采样 → FPS
到 6144），两边就同口径。采样地板是共模的，比较依然有效 —— 只是不能和论文发表
的体素指标并排。

### ✅ 已完成 / ❌ 已否决

- ~~**逐点交叉注意力**~~ ❌ **8/21 否决。** 实现是 B 方案（保留全局 token +
  2048 个逐点 key，参数量一字不差 187,462,503）。结果**全面变差**：
  CD_t 6.356 → 6.648（73× 噪声）、HD95 6.068 → 6.490、F1@0.05 0.930 → 0.907、
  DCD 0.685 → 0.747。
  **原因已查清且是本条真正的价值所在**：训练后 `energy std = 0.02`、有效 key 数
  **2048.7 / 2049**、全局 token 权重仅为均匀值的 0.99× —— **注意力从未启动**
  （经典坍缩：权重近均匀 → 输出是平均 → 回传给 Q/K 的梯度极小 → 更学不出区分度）。
  换成标准 `softmax(QKᵀ/√d, axis=-1)` 后有效 key 数仍是 2048.5，**所以不是
  softmax 轴的问题，是 Q/K 本身没分化**。
  净效果等于：把"max-pool 的全局条件"换成了"mean-pool 的",并把全局与文本那条路
  **稀释约 2000 倍**。
  ⚠️ **措辞很重要**：不能写成"逐点注意力没用"，准确的说法是
  **"PCT 的 offset-attention 块在本数据规模下没有发展出 key 选择性，因此这次实验
  并未检验到『max-pool 是信息瓶颈』这一假设"**。代码保留（默认 `False`），
  **论文写不写待定，本次进度报告不写**
- ~~repulsion loss~~ ✅ 密度问题的有效解（扎堆 5.6% → 1.4%），不损失精度
- ~~DCD 消融（完整 2×2）~~ ✅ **最优配置是 CD + repulsion**
- ~~缺损区限定指标~~ ✅ 改进作用在难的那部分。**论文应以缺损区覆盖为主指标**
- ~~缺损区掩码定义修正~~ ✅ 8/20；换定义前做了排名检验，结果对自家配置不利
- ~~修 `ReduceLROnPlateau`~~ ✅ 至今最大的单项收益（6.945 → 6.317）
- ~~架构版本追踪~~ ✅ **8/21 新增。** `run.json` 记 `use_text` / `per_point_attn`，
  `report.Run.arch_key` 读回，`eval_runs` 按架构分组。堵的是 `load_weights` 在
  拓扑维度上不设防（权重形状不变 → 旧 checkpoint 静默读进新拓扑）
- ~~`dcd_weight` / `dcd_lambda` 调参~~ ❌ 兑换率差 / 收益小于噪声
- ~~正则化~~ ❌ 模型不过拟合，dropout 让 CD_t 差 2.07 倍，代码已删
- ~~体素基线~~ ⏸ 范围决策，暂缓（见第 14 项）

---

## 2026-08-21（第四次）— 有效性重划分 + 论文范围收口

**触发**：查证发现 `ReduceLROnPlateau(patience=40) > EarlyStopping(patience=20)`
这个错误是**本项目训练脚本自己引入的**，与原实现无关——原项目的训练 demo
只有 `ReduceLROnPlateau(patience=5)`，没有 `EarlyStopping`，不存在此冲突。
此前多次把它当作"研究发现 / 最大单项收益"记录，**定性错了**。

**本次决定（用户拍板）：**
1. **前四轮（`baseline_es20` / `dcd_w3` / `dcd_l2` / `rep05_void`）整档划为⛔错误性实验**，
   不作为论文中的基线或对照；依赖它们的 `dcd_weight` / `dcd_lambda` 调参结论一并作废
2. **方法比较的基线改用 `lr_fix_only`（6.317mm）**，不是 `baseline_es20`（7.076mm）
3. **自己引入的错误不写进论文**（EarlyStopping/LR 冲突、掩码定义错误、
   repulsion 未无量纲化等），只留在 devlog 作为工作记录
4. **逐点交叉注意力的阴性结果不写进论文**
5. devlog 保留全部记录不删——它是私人档案；论文层面用本节的范围约束

### 🔴 优先级最高

| | 内容 | 成本 | 为什么现在做 |
|---|---|---|---|
| **1** | **k 折交叉验证**，配置为完整 2×2 | 4 配置 × 5 折 ≈ 15h | ⭐ **重要性因本次重划分而上升**：可用数据从 12 轮缩到 7 轮，且全部单折。k 折是唯一能把结论抬到可发表口径的办法 |

### 🟡 实验（要跑训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **2** | **可见区域透传** | 最大 | 架构侧唯一在牌桌上的选项。⚠️ CD_t 会虚高，读结果以缺损区覆盖为准。路由写死、不需要学习，不受注意力坍缩影响 |
| **3** | `notext_r2` —— 复核文本分支那条 | 45~60 分钟 | ⚠️ 未复核前不进论文 |
| 4 | 数据增强 | 小 | 理由为"提升覆盖" |
| 5 | 平滑惩罚项 / 点到面引力 | 中 | |
| 6 | 预训练权重微调 | 中~大 | ⚠️ 权重只认 `msn_demo_arch` 拓扑 |
| 7 | 真 FPS 采样 | 小~中 | |
| 8 | 模型容量中间值 | 小~中 | |
| 9 | 接入 SkullBreak | 大 | |
| — | ~~`tie_qk` 完整训练~~ | 进行中 | 审计出的唯一未记录移植差异，跑完即闭环 |

### 🟢 评测（不用训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **10** | Poisson 重建对照 | 半天 | ⭐ **唯一能回答"离临床多远"的路径**，①②两种论文定位都需要它 |
| 11 | 粗糙度度量正式补进 `mesh_viz.py` | 半天 | ⚠️ 现有数字来自临时脚本、不可复现 |
| ~~12~~ | ~~自查编码器 self-attention 是否坍缩~~ | ✅ **已完成** | 见下方已完成区 |

### 📌 文档 / 论文

| | 内容 | 挂了多久 |
|---|---|---|
| **13** | 查证 `MSN_weights3.h5` 是否见过颅骨数据 —— 决定「zero-shot」措辞 | 8/05 |
| **14** | `README.md` 仍写着 "voxel baseline" / "self-built corrupted dataset" —— 需和导师确认 | 8/06 |
| 15 | **采样地板必须写进论文** —— 6144 点下 CD_t 地板 4.43mm、HD95 地板 3.79mm | 8/20 |
| **16** | ⭐ **训练配置与原实现的差异表必须写进论文** —— 8/21 新增。已知：损失（原作 `calc_dcd` 单独 vs 本项目 cd/+rep）、初始学习率（1e-7 vs 3e-4，差 3400 倍）、factor（0.9 vs 0.5）、min_lr（1e-20 vs 1e-6）、LR patience（5 vs 派生值）、EarlyStopping（无 vs 20）、batch（8 vs 4，显存所限）、validation_split（0.1 vs 0.2）。**"我们的训练配置与原实现有何不同、为什么"是必答题** | 8/21 |
| 17 | `PROGRESS.md` 还停在 6/25 | 用户说先不写 |

### ⚙️ 已知但未修

| | 内容 |
|---|---|
| 18 | `UniformSampler` 推理不确定性（同输入两次调用差 1.03） |
| 19 | λ=2 那个"梯度饱和"假说始终没验证 —— ⚠️ 该假说建立在⛔错误性实验上，**建议直接撤掉不再追** |
| 20 | 改 `report.py` / `mesh_viz.py` 后 notebook 需 `importlib.reload` 或重启 kernel |
| 21 | `--no-text` 的"隐式偏置学习率"解释与 Adam 逐参数自适应存在张力，未解决 |

### ✅ 已完成 / ❌ 已否决 / ⛔ 作废

- ~~**自查全模型注意力是否坍缩**~~ ✅ **8/21 完成，⭐ 论文核心材料。**
  16 个块中 **12 个**产生均匀权重：`E-SA1..8` 全部（有效 key 占比 100.0%，
  跨 8 颗颅骨零浮动）、`D1-STA1..4`（结构强制）。**训练主动把编码器自注意力的
  energy 方差相对随机初始化压低约 3 个数量级**（0.27 → 0.0002）。
  唯一工作的注意力在解码器第二阶段（D2 有效 key 占比 39.7%~91.9%）。
  两个独立训练的模型收敛到同一结果
- ~~**重写 vs 原实现的逐行审计**~~ ✅ **8/21 完成。** 模型拓扑只有**一处**未记录差异
  （`Self_Attention` 的 `W_k.set_weights(W_q.get_weights())`）；其余要么一致，
  要么是已记录的刻意改动（采样器无放回、距离核、BERT 预计算、块命名）。
  ⚠️ **训练超参未审**——已另立为第 16 项
- ~~repulsion loss~~ ✅ 密度问题的有效解。**结论改用修复后的两轮陈述**：
  `cd_only` 13.60% → `cd_rep05_full` 1.34%
- ~~DCD 消融（完整 2×2）~~ ✅ **最优配置是 CD + repulsion。四格全部在 LR 修复之后，不受重划分影响**
- ~~缺损区限定指标~~ ✅ **论文应以缺损区覆盖为主指标**
- ~~逐点交叉注意力~~ ❌ 全面变差；原因是注意力从未启动。**决定：不写进论文**
- ⛔ ~~`ReduceLROnPlateau` 配置修复~~ **不是研究发现**，是本项目自己的配置错误。
  **不写进论文**；其唯一后果是划出了上面那条有效性分界线
- ⛔ ~~`dcd_weight` / `dcd_lambda` 调参~~ **作废**——建立在错误性实验上
- ~~正则化~~ ❌ 模型不过拟合，dropout 让 CD_t 差 2.07 倍，代码已删
- ~~体素基线~~ ⏸ 范围决策，暂缓（见第 14 项）

---

## 2026-08-24（第五次）— `tie_qk` 否决 + 噪声底线 0.004mm 被推翻

**触发**：`tie_qk_r2` 跑完。两次结果的差异（CD_t 0.151mm / 缺损覆盖 0.233mm）
**大于它与对照的差异**，取平均 +0.048mm＝零 → `tie_qk` 否决。同时暴露出
「同配置重跑只差 0.004mm」这条噪声底线是**一次撞大运**：在 `tie_qk` 配置上是 0.15~0.23mm，
相差 36~38 倍。

**相对上一版的改动**：
1. `tie_qk` 从「进行中」移入 ❌ 否决区，这条线**收尾**
2. 第 1 项 k 折补上定量理由（5 折均值 SE≈0.09mm vs 效应 0.17~0.20mm，t≈2，勉强够）
3. 第 3 项 `notext_r2` 理由更新：今天的方差发现使它**更必要**，但复现把握从 80~85% 下调到五五开
4. 新增第 17 项：预训练基线缺缺损区指标（是推理不是训练）
5. 判读工具已入库（`report.epoch_matched` / `paired_stats`），notebook 已拆成训练/判读两本

### 🔴 优先级最高

| | 内容 | 成本 | 为什么现在做 |
|---|---|---|---|
| **1** | **k 折交叉验证**，配置为完整 2×2 | 4 配置 × 5 折 ≈ 15h | ⭐ **定量理由（8/24 新增）**：训练间方差保守取 0.15mm，5 折均值的 SE ≈ 0.09mm，而 2×2 的效应是 0.17~0.20mm → **t≈2，勉强够、不宽裕**。推论：①四格必须全跑；②**别指望它能分辨 0.1mm 以下的差异**；③万一卡在边缘，补救是关键格每折跑两次，不是加折数。⚠️ **按用户的规矩：k 折是所有改进做完之后的终审，不是现在做的事** |

### 🟡 实验（要跑训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **2** | **可见区域透传** | 最大 | 架构侧唯一在牌桌上的选项。⚠️ CD_t 会虚高，读结果以缺损区覆盖为准。路由写死、不需要学习，不受注意力坍缩影响 |
| **3** | `notext_r2` —— 复核文本分支那条 | 45~60 分钟 | ⚠️ **8/24 更新**：notext 的效应（缺损覆盖 +0.175mm）**小于**今天量到的运行间方差（0.233mm），所以它更可能是运气 —— 复核**更必要**，但把握下调到五五开。⚠️ 未复核前不进论文 |
| 4 | 数据增强 | 小 | 理由为"提升覆盖" |
| 5 | 平滑惩罚项 / 点到面引力 | 中 | |
| 6 | 预训练权重微调 | 中~大 | ⚠️ 权重只认 `msn_demo_arch` 拓扑 |
| 7 | 真 FPS 采样 | 小~中 | |
| 8 | 模型容量中间值 | 小~中 | |
| 9 | 接入 SkullBreak | 大 | ⚠️ 会撞到 `report.eval_runs` 里写死的 `.npz` 路径，要先加参数 |

### 🟢 评测（不用训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **10** | Poisson 重建对照 | 半天 | ⭐ **唯一能回答"离临床多远"的路径** |
| 11 | 粗糙度度量正式补进 `mesh_viz.py` | 半天 | ⚠️ 现有数字来自临时脚本、不可复现 |
| **12** | **注意力坍缩自查写成脚本** | 半小时 | ⚠️ **8/24 新增**：结论（12/16 块坍缩）是论文核心材料，但它和粗糙度那两个数一样**只来自临时脚本**。写成脚本之前，`tie_qk` / `cd_rep05_full` 的权重不要删 |

### 📌 文档 / 论文

| | 内容 | 挂了多久 |
|---|---|---|
| **13** | 查证 `MSN_weights3.h5` 是否见过颅骨数据 —— 决定「zero-shot」措辞 | 8/05 |
| **14** | `README.md` 仍写着 "voxel baseline" / "self-built corrupted dataset" —— 需和导师确认 | 8/06 |
| 15 | **采样地板必须写进论文** —— 6144 点下 CD_t 地板 4.43mm、HD95 地板 3.79mm | 8/20 |
| **16** | **训练配置与原实现的差异表必须写进论文**（损失、初始学习率、factor、min_lr、LR patience、EarlyStopping、batch、validation_split） | 8/21 |
| **17** | ⭐ **预训练基线补缺损区指标** —— `eval_val20.csv` 只有 `CD_t / CD_p / DCD`，**没有缺损区、HD95、F1**，而主指标是缺损覆盖。主表要并排就得补跑一次 `MSN_baseline_pretrained.ipynb` 的推理（权重已保留），并把算指标那处改成传 `inp=`。**是推理不是训练，几分钟** | 8/24（新） |
| 18 | `PROGRESS.md` 还停在 6/25 | 用户说先不写 |

### ⚙️ 已知但未修

| | 内容 |
|---|---|
| 19 | `UniformSampler` 推理不确定性（同输入两次调用差 1.03）—— 只影响 demo 架构，本项目模型推理已是确定性的 |
| 20 | λ=2 那个"梯度饱和"假说 —— ⚠️ 建立在⛔错误性实验上，**建议直接撤掉不再追** |
| 21 | 改 `report.py` / `mesh_viz.py` 后 notebook 需 `importlib.reload` 或重启 kernel |
| 22 | `--no-text` 的"隐式偏置学习率"解释与 Adam 逐参数自适应存在张力，未解决 |
| 23 | **8/24 新增**：`report.eval_runs` 把 `.npz` 路径写死了，换数据集（SkullBreak）前要先加参数 |

### ✅ 已完成 / ❌ 已否决 / ⛔ 作废

- ~~**Q/K 初始绑定（`tie_qk`）**~~ ❌ **8/24 否决，这条线收尾。** 移植审计查出的唯一一处
  未记录差异。跑了两次：**两次之间的差异（CD_t 0.151mm / 缺损覆盖 0.233mm）大于它与对照的
  差异**，取平均 +0.048mm＝零。唯一可复现的效应是**密度变差**（扎堆 1.34% → 2.14%/2.37%，
  配对 0/20 与 2/20）。8/21 写的「缺损覆盖 3.170 至今最好」**已撤回**。
  **默认保持 `False`；代码保留（删了"我们测试过"就不可复现）；这一条要写进论文第 3 章**
  —— 它不是"自己写错的错误"（那类不写），是**与参考实现的差异 + 受控验证**，属可复现性正面材料
- ~~**判读方法学**~~ ✅ **8/24。** 三把尺子（同配置重跑 / 逐颅骨配对 / 末段抖动）分开，
  `report.epoch_matched` + `paired_stats` 入库；notebook 拆成「训练」与「判读」两本
- ~~**噪声底线 0.004mm**~~ ⛔ **8/24 推翻。** 是一次撞大运；保守按 0.15/0.23 读，
  `clump_%` 按 0.2pp。⚠️ 并且：**配对检验能证明"两个模型不同"，不能证明"这个配置更好"**
- ~~自查全模型注意力是否坍缩~~ ✅ 8/21，16 块中 12 块产生均匀权重。⚠️ 但脚本未入库（见第 12 项）
- ~~重写 vs 原实现的逐行审计~~ ✅ 8/21，模型拓扑只有一处未记录差异，现已由 `tie_qk` 闭环
- ~~repulsion loss~~ ✅ 密度问题的有效解（`cd_only` 13.60% → `cd_rep05_full` 1.34%），
  **效应是噪声的 50 倍，是全项目最硬的一条**
- ~~DCD 消融（完整 2×2）~~ ✅ **最优配置是 CD + repulsion。** 四格全在 LR 修复之后。
  ⚠️ 缺损覆盖上的效应（0.17~0.20mm）与保守噪声同量级，**归 k 折终验**
- ~~缺损区限定指标~~ ✅ **论文以缺损区覆盖为主指标** —— 8/24 新增实测依据：2×2 在缺损覆盖上
  四格全部统计显著，而在全点云 CD_t 上全部不显著
- ~~缺损区掩码定义修正~~ ✅ 8/20
- ~~逐点交叉注意力~~ ❌ 全面变差，注意力从未启动。⚠️ 措辞改掉倍数（"72× 噪声"按新尺度只有 2×，
  但配对 1/20、p=0.0007 仍站得住）。**决定：不写进论文**
- ~~仓库整理 / `--run-name` 覆盖保护 / `--from-run`~~ ✅ 8/24
- ⛔ ~~`ReduceLROnPlateau` 配置修复~~ **不是研究发现**，不写进论文
- ⛔ ~~`dcd_weight` / `dcd_lambda` 调参~~ **作废**——建立在错误性实验上
- ~~正则化~~ ❌ 模型不过拟合，代码已删
- ~~体素基线~~ ⏸ 范围决策，暂缓（见第 14 项）

---

## 2026-08-24（第六次）— `notext` 复核通过；⚠️ 噪声判据从"按大的读"改成"看走没走平"

**触发**：`notext_r2` 跑完，**两次差 0.0037mm（声称效应 0.174mm 的 1/47）**，判决清单四条全过
→ `notext` 成立。同时它成为第三对重复实验，推翻了上一版"一律保守按 0.15/0.23 读"的判据。

**相对上一版的改动**：
1. 第 3 项 `notext_r2` 完成 → 移入已完成区；**B 类（n=1 待复核）已清空**
2. 噪声判据改写：三对里两对 ≲0.005mm，`tie_qk` 是例外；分水岭是**曲线走没走平**
3. 第 1 项 k 折补一条执行要求：**给足 `--epochs`，别让任何一格在下行段被截断**
4. 第 2 项可见区域透传**升为当前唯一在牌桌上的实验项**

### 🔴 优先级最高

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **1** | **k 折交叉验证**，完整 2×2 | 4 配置 × 5 折 ≈ 15h | ⚠️ **按用户的规矩：这是所有改进做完之后的终审，不是现在做的事**。<br>执行要求：①四格全跑；②**给足 `--epochs`** —— `cd_only`（@222→@300 还降 0.07）和 `lr_fix_only`（val_cd_t 直到第 277 轮仍在改善）都属于"没走平"那一档，被截断会把结论搅浑；③别指望它分辨 0.1mm 以下的差异；④卡在边缘时补救是关键格每折两次，不是加折数 |

### 🟡 实验（要跑训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **2** | **可见区域透传** | 最大 | ⭐ **当前唯一在牌桌上的实验项。** 解码器只吃 max-pool 出的一个全局向量，缺损颅骨里本来完好的表面也被整个重新生成。⚠️ CD_t 会虚高，读结果**以缺损区覆盖为准**。⭐ 路由**写死、不需要学习**，所以不受注意力坍缩影响（那正是 `pp_attn` 的死因） |
| 3 | 数据增强（绕 SI 轴小角度旋转 + jitter） | 小 | 理由是"提升覆盖"，不是"防过拟合"（前提已被否） |
| 4 | 平滑惩罚项 / **点到面引力** | 中 | 多尺度粗糙度度量已被否定；点到面引力与 repulsion 正交、可用 `signed_deviation` 验收 |
| 5 | 预训练权重微调 | 中~大 | ⚠️ 权重只认 `msn_demo_arch` 拓扑 |
| 6 | 真 FPS 采样 | 小~中 | |
| 7 | 模型容量中间值 | 小~中 | |
| 8 | 接入 SkullBreak | 大 | ⚠️ 会撞到 `report.eval_runs` 里写死的 `.npz` 路径（见第 23 项） |

### 🟢 评测（不用训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **9** | Poisson 重建对照 | 半天 | ⭐ 唯一能回答"离临床多远"的路径 |
| **10** | **注意力坍缩自查写成脚本** | 半小时 | ⚠️ 结论（16 块中 12 块坍缩）是论文核心材料，但**只来自临时脚本**。写完之前 `tie_qk` / `cd_rep05_full` 的权重不要删 |
| 11 | 粗糙度度量正式补进 `mesh_viz.py` | 半天 | ⚠️ 现有数字来自临时脚本、不可复现 |

### 📌 文档 / 论文

| | 内容 | 挂了多久 |
|---|---|---|
| **12** | 查证 `MSN_weights3.h5` 是否见过颅骨数据 —— 决定「zero-shot」措辞 | 8/05 |
| **13** | `README.md` 仍写着 "voxel baseline" / "self-built corrupted dataset" —— 需和导师确认 | 8/06 |
| 14 | **采样地板必须写进论文** —— CD_t 地板 4.43mm、HD95 地板 3.79mm | 8/20 |
| **15** | **训练配置与原实现的差异表必须写进论文** | 8/21 |
| **16** | **预训练基线补缺损区指标** —— `eval_val20.csv` 只有 `CD_t / CD_p / DCD`，主表并排要补跑一次推理（**是推理不是训练**） | 8/24 |
| 17 | `PROGRESS.md` 还停在 6/25 | 用户说先不写 |

### ⚙️ 已知但未修

| | 内容 |
|---|---|
| 18 | ⭐ **8/24 新增**：**early stopping 盯 `val_loss`，而结论读 `val_cd_t` / 缺损覆盖** —— "按停止准则收敛了"≠"按报告指标收敛了"。`lr_fix_only` / `cd_only` / `rep_w05` 都属这档。k 折前要想清楚 |
| 19 | `--no-text` 的机制解释（隐式偏置学习率杠杆）与 Adam 逐参数自适应存在张力 —— **现象已坐实，机制仍是假说**，判决性实验（给 4 个 `D1-STA{i}_LBR_lin` 的 bias 单独调大学习率）未做 |
| 20 | `UniformSampler` 推理不确定性 —— 只影响 demo 架构 |
| 21 | λ=2 那个"梯度饱和"假说 —— 建立在⛔错误性实验上，**建议撤掉不再追** |
| 22 | 改 `report.py` / `mesh_viz.py` 后 notebook 需 `importlib.reload` |
| 23 | `report.eval_runs` 把 `.npz` 路径写死了，换数据集前要先加参数 |

### ✅ 已完成 / ❌ 已否决 / ⛔ 作废

- ~~**`notext` 复核**~~ ✅ **8/24 成立。** 两次差 0.0037mm（效应的 1/47），判决清单四条全过：
  缺损覆盖 +0.174mm（17/20，p=0.0012）、洞里少放 54 个点（**20 颗全部更少**）、
  全点云反而更准（CD_t −0.040、HD95 −0.147）。**结论：保留文本分支；现象可写进论文。**
  ⚠️ 但机制解释仍是假说 —— **成立的是现象，不是机制**（见第 19 项）
- ~~**Q/K 初始绑定（`tie_qk`）**~~ ❌ **8/24 否决。** 两次之间的差异大于它与对照的差异，
  取平均＝零；唯一可复现的效应是密度变差。**默认保持 `False`；这一条要写进论文第 3 章**
  —— 它是"与参考实现的差异 + 受控验证"，不是"自己写错的错误"
- ~~**噪声判据**~~ ⚠️ **8/24 两次修正。** 现在是：三对重复实验里两对 ≲0.005mm、`tie_qk` 那对
  0.15/0.23；**分水岭是曲线走没走平**，`notext` 那对轮数差 153 轮照样复现到 0.0013mm。
  **读小差异前先看 `report.epoch_matched` 的末几列。**
  ⚠️ 且：**配对检验能证明"两个模型不同"，不能证明"这个配置更好"**
- ~~判读方法学工具~~ ✅ 8/24：`report.epoch_matched` / `paired_stats`；notebook 拆成训练/判读两本
- ~~自查全模型注意力坍缩~~ ✅ 8/21（⚠️ 脚本未入库，见第 10 项）
- ~~重写 vs 原实现的逐行审计~~ ✅ 8/21，唯一一处差异已由 `tie_qk` 闭环
- ~~repulsion loss~~ ✅ **全项目最硬的一条**（`cd_only` 13.60% → `cd_rep05_full` 1.34%，效应是噪声的 50 倍）
- ~~DCD 消融（完整 2×2）~~ ✅ **最优配置是 CD + repulsion**。⚠️ 缺损覆盖上的效应归 k 折终验
- ~~缺损区限定指标~~ ✅ **论文以缺损区覆盖为主指标**（2×2 在它上面四格全显著，在全点云 CD_t 上全不显著）
- ~~缺损区掩码定义修正~~ ✅ 8/20
- ~~逐点交叉注意力~~ ❌ 全面变差，注意力从未启动。⚠️ 措辞去掉倍数。**不写进论文**
- ~~仓库整理 / `--run-name` 覆盖保护 / `--from-run`~~ ✅ 8/24
- ⛔ ~~`ReduceLROnPlateau` 配置修复~~ 不是研究发现，不写进论文
- ⛔ ~~`dcd_weight` / `dcd_lambda` 调参~~ 作废（建立在错误性实验上）
- ~~正则化~~ ❌ 模型不过拟合，代码已删
- ~~体素基线~~ ⏸ 范围决策，暂缓（见第 13 项）

---

## 2026-08-25（第七次）— 收尾开始：⚙️「已知但未修」清空

**触发**：开始逐条收尾。从 ⚙️ 已知但未修（18~23）入手，六条全部处理完；
第 16 项（预训练基线补主指标）随第 20 项一次运行一并关掉。
**⚙️ 那一节第一次变成空的。**

**这一批的处理方式值得记**：六条里**三条是量一下就划掉的**（18 的前两件、20 整条），
只有两条真要动代码（18 的早停口径、23 的路径），一条是定性撤回（21），一条是补文档（22）。
> **"待修问题"清单上的条目，量一下往往比修一下便宜** —— 第 20 项挂了三周，
> 源头是一个听起来吓人的数字（1.03），实测落到报告指标上只有 0.03mm。

### 🔴 优先级最高

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **1** | **k 折交叉验证**，完整 2×2 | 4 配置 × 5 折 ≈ 18~20h | ⚠️ **所有改进做完之后的终审，不是现在做的事**。执行要求：①四格全跑；②给足 `--epochs`（`cd_only` / `lr_fix_only` 都属「没走平」那一档）；③别指望它分辨 0.1mm 以下的差异；④卡在边缘时补救是关键格每折两次，不是加折数 |

### 🟡 实验（要跑训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **2** | **可见区域透传** | 最大 | ⭐ **唯一还在牌桌上的实验项。** 解码器只吃 max-pool 出的一个全局向量。⚠️ CD_t 会虚高，读结果以缺损区覆盖为准。⭐ 路由**写死、不需要学习**，不受注意力坍缩影响（那正是 `pp_attn` 的死因） |
| 3 | 数据增强（绕 SI 轴小角度旋转 + jitter） | 小 | 理由是"提升覆盖"，不是"防过拟合" |
| 4 | 平滑惩罚项 / **点到面引力** | 中 | 多尺度粗糙度度量已被否定；点到面引力与 repulsion 正交、可用 `signed_deviation` 验收 |
| 5 | 预训练权重微调 | 中~大 | ⚠️ 权重只认 `msn_demo_arch` 拓扑 |
| 6 | 真 FPS 采样 | 小~中 | ⚠️ **8/25 收窄**：`paper()` 的 `E-SG1` 是 4096/4096 —— 全选，只做了个排列，真 FPS 在那儿是 **no-op**。**只影响 `E-SG2`（4096 选 2048）一层**。<br>而且输入点云本身已是 FPS 采样（间距约 5mm、零扎堆），从已经均匀的集合里随机取一半，FPS 能改善的余地小。**预期收益进一步下调**（推理，未实测）
| 7 | 模型容量中间值 | 小~中 | |
| 8 | 接入 SkullBreak | 大 | ⚠️ **未决定是否要做**，不是既定计划 |

### 🟢 评测（不用训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **9** | Poisson 重建对照 | 半天 | ⭐ 唯一能回答"离临床多远"的路径。<br>⚠️ **8/25 新增前置步骤：先量「重建地板」再决定要不要报指标。** 做法与采样地板同构 ——<br>拿 **GT 的点**做 Poisson 重建，和**真正的 GT 网格**比，全程无模型参与。若那个差异是 3mm 量级，<br>则「Poisson 之后 HD95 = X」主要在量重建器而不是模型，白做；0.5mm 量级才谈得上和 AutoImplant 的 1.52mm 并排。<br>⚠️ 而且**重建后的指标不能替换主结果** —— 换表示方式会让地板消失、数字普遍变好看，拿它当主指标是 metric shopping。主体报点云，Poisson 单列一节作为对外可比性的桥 |
| **10** | **注意力坍缩自查写成脚本** | 半小时 | ⚠️ 结论（16 块中 12 块坍缩）是论文核心材料，却**只来自临时脚本**。写完之前 `tie_qk` / `cd_rep05_full` 的权重不要删。<br>参考 `src/eval/fold_text_branch.py` 的写法 |
| 11 | 粗糙度度量补进 `mesh_viz.py` | 半天 | ⚠️ 现有数字来自临时脚本、不可复现 |

### 📌 文档 / 论文

| | 内容 | 挂了多久 |
|---|---|---|
| **12** | ⏸ **最终基线要不要包含体素基线** —— 用户 8/25：**还在犹豫**。<br>"self-built corrupted dataset" 已确定取消（README 已改）；voxel baseline 这一半仍未定。<br>若做：把体素输出丢进 `prepare_skullfix.py` 同一条管线（marching cubes → 密集采样 → FPS 到 6144），两边共用采样地板才可比 | 8/06 |
| 13 | **采样地板必须写进论文** —— ✅ **8/25 已测量并入库**（`src/eval/sampling_floor.py` → `experiments_log/sampling_floor.csv`，100 颗全跑，**k 折后不用重跑**）。<br>**CD_t 地板 4.619 ± 0.252、HD95 地板 3.974 ± 0.214、单向 2.310 ± 0.126**（旧记的 4.43/3.79 偏低，已作废）。<br>⭐ 换算：本工作 CD_t 6.355 里 **73% 是地板**；缺损覆盖 3.236 离单向地板只剩 **0.926mm**。<br>剩下的是**写作**：把它写进第 4 章，并说明本文数值不可与体素域方法并排 | 8/20 |
| **14** | **训练配置与原实现的差异表必须写进论文** | 8/21 |

### ⚙️ 已知但未修

**（空）** —— 2026-08-25 起。六条全部处理完，见下方已完成区。

### ✅ 已完成 / ❌ 已否决 / ⛔ 作废

**本批（8/25）**

- ~~**「zero-shot」措辞查证（原 12）**~~ ✅ **8/25 用户确认：这套权重训练时见过颅骨数据。**
  → **"zero-shot" 不能写。** 两个后果，方向相反：① **基线没吃亏** —— 3.17× 的差距
  不能用"它没见过颅骨"解释，增益确实来自专精化，这比 zero-shot 的说法更强；
  ② ⚠️ **可能有训练/测试污染**（MedShapeNet 部分源自 AutoImplant = SkullFix 的来源），
  若含这 20 颗验证颅骨，则基线在自己训练过的数据上被评估、分数被抬高，
  **本工作的领先是低估的** —— 论文要主动写明，并说明它只会让结论更保守
- ~~**"self-built corrupted dataset"（原 13 的一半）**~~ ❌ **8/25 取消。** SkullFix 自带
  缺损/完整配对，全部结果用的都是它；`src/corruption/` 从未实现，这个说法对本工作
  从来不成立。README 标题与 Idea 段已改写。**另一半（voxel baseline）仍未定，见第 12 项**
- ~~**`PROGRESS.md`（原 16）**~~ ❌ **8/25 删除该待办**，不再维护。进度以 `devlog.md`
  与本文件为准

- ~~**早停口径（原 18）**~~ ✅ 拆成四件：①「checkpoint 那轮 ≠ 报告值那轮」实测差 ≤0.007mm →
  非问题；②「全程最优」的 min-over-noise 偏置 0.005~0.018mm → 只影响 `run.json` 口径，
  论文用的 eval CSV 不受影响；③ **patience 20→30**（9 个 run 里 5 个曾擦着边活下来，
  抽签价值 0.15mm）；④ **新增 `--defect-every`**，主指标终于进 `history.csv`。
  顺带**解耦 `--lr-patience`**（原来从早停 patience 推导，改一个会连带改退火节奏）。
  ⚠️ **诊断指标绝不参与选择** —— 用它挑 checkpoint 会让报告值乐观偏置
- ~~**文本分支机制（原 19）**~~ ✅ **折叠验证证明了「多余」那一半**：把 3,149,824 个参数
  折进 4 个偏置向量，输出差 4×10⁻³ mm（浮点噪声）。脚本 `src/eval/fold_text_branch.py`。
  ⚠️ 修正：无文本模型达成的是目标偏置的 **46%**（原记「三分之一」是把两个范数相加当合计）。
  Adam 那条张力分析上基本解掉（‖t‖₁=341 > ‖t‖₂²=228，杠杆没被抵消反而略大）。
  **论文按「事实 + 一句带保留的猜测」写**；判决性实验移交 future work
- ~~**采样器不确定性（原 20）**~~ ✅ **实测可忽略，不改代码**：跨抽样 std CD_t 0.030mm、
  缺损覆盖 0.075mm、DCD 0.0007，落在本项目自己训练间方差（0.004~0.151mm）范围内。
  ⚠️ 挂了三周的「两次调用差 1.03」是坐标差的某种范数，**落到指标上只有 0.03mm**。
  不改采样器：`msn_demo_arch.py` 是逐字复制，随机抽样是作者模型的真实行为
- ~~**λ=2「梯度饱和」假说（原 21）**~~ ⛔ **撤回。** 它建立在 `dcd_l2` 上，而那一整档是
  ⛔ 错误性实验，前提没了。两处 docstring 已改成「设计意图，本项目未验证」。
  且已无意义：最优配置根本不含 DCD
- ~~**notebook 模块 reload（原 22）**~~ ✅ 从"待修缺陷"改为"已堵住"：
  `MSN_surface_quality.ipynb` 补上 `importlib.reload(mv/rp)`（只 reload 纯 numpy/plotly 模块，
  `msn_skullfix` 定义 Keras 层、热重载会出怪事），`src/eval/README.md` 与两个 notebook 都写明
- ~~**`.npz` 路径写死（原 23）**~~ ✅ 收成 `report.DATA_CACHE` 一个常量，四处读取共用，
  `eval_runs` 另加可选 `data=` 参数。⚠️ **这是去重，不是为换数据集做准备**
- ~~**预训练基线补主指标（原 16）**~~ ✅ 随第 20 项一次运行关掉。⭐ 结果很有价值：
  全点云差 1.69×，**缺损区覆盖差 3.17×** —— 全点云指标严重低估差距，而且这次是在
  **外部模型**上验证的。⚠️ 缺损**精度**只差 1.06× 是假象：基线只往洞里放 174 个点
  （GT 约 395），根本没填洞 —— **`defect_prec` 必须和 `defect_n_pred` 一起看**的真实案例

**此前**

- ~~`notext` 复核~~ ✅ 8/24 成立（两次差 0.0037mm = 效应的 1/47）。**保留文本分支**
- ~~Q/K 初始绑定 `tie_qk`~~ ❌ 8/24 否决（两次之间的差异大于它与对照的差异）。
  **仍要写进论文第 3 章** —— 它是「与参考实现的差异 + 受控验证」，不是自己写错的错误
- ~~判读方法学~~ ✅ 三把尺子分开；`report.epoch_matched` / `paired_stats` 入库；
  notebook 拆成训练/判读两本
- ~~噪声判据~~ ⚠️ 两次修正，最终：三对重复实验里两对 ≲0.005mm、`tie_qk` 那对 0.15/0.23；
  **分水岭是曲线走没走平**。且**配对检验能证明"两个模型不同"，不能证明"这个配置更好"**
- ~~自查全模型注意力坍缩~~ ✅ 8/21（⚠️ 脚本未入库，见第 10 项）
- ~~重写 vs 原实现的逐行审计~~ ✅ 8/21，唯一一处差异已由 `tie_qk` 闭环
- ~~repulsion loss~~ ✅ **全项目最硬的一条**（13.60% → 1.34%，效应是噪声的 50 倍）
- ~~DCD 消融（完整 2×2）~~ ✅ **最优配置是 CD + repulsion**。⚠️ 缺损覆盖上的效应归 k 折终验
- ~~缺损区限定指标~~ ✅ **论文以缺损区覆盖为主指标**（2×2 在它上面四格全显著，全点云 CD_t 全不显著）
- ~~缺损区掩码定义修正~~ ✅ 8/20
- ~~逐点交叉注意力~~ ❌ 全面变差，注意力从未启动。**不写进论文**
- ~~仓库整理 / `--run-name` 覆盖保护 / `--from-run`~~ ✅ 8/24
- ⛔ ~~`ReduceLROnPlateau` 配置修复~~ 不是研究发现，不写进论文
- ⛔ ~~`dcd_weight` / `dcd_lambda` 调参~~ 作废（建立在错误性实验上）
- ~~正则化~~ ❌ 模型不过拟合，代码已删
- ~~体素基线~~ ⏸ 范围决策，暂缓（见第 13 项）

---

## 2026-08-26（第八次）— TODO 第 10 项关闭：注意力坍缩自查入库

**触发**：第 10 项（注意力坍缩写成脚本）做完。⭐ 产出比预期大：不但把论文核心材料
从临时脚本变成了 `src/eval/attention_collapse.py` + `experiments_log/attention_collapse.csv`，
还**推翻了 8/21 那张表的两个数**，并测出一条新的架构观察。

**这一批值得记的**：脚本在交付后**炸了两次**（一次崩、一次不崩但给出错数），
第三个问题（熵算在未归一化的行上）是靠一条**数学不变量**抓出来的 ——
「注意力行若和为 1，则 max×n ≥ 1」，而实测读到 0.71。
> **采样地板那次的教训是「不入库的数字可能一直是错的」；这次是
> 「入了库也可能错，差别在于错得会不会被抓住」。** 校验要对着「它必须满足什么」，
> 不能只对着「我预期它是多少」。相关不变量已写进脚本，每次运行都跑。

### 🔴 优先级最高

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **1** | **k 折交叉验证**，完整 2×2 | 4 配置 × 5 折 ≈ 18~20h | ⚠️ **所有改进做完之后的终审，不是现在做的事**。执行要求：①四格全跑；②给足 `--epochs`（`cd_only` / `lr_fix_only` 都属「没走平」那一档）；③别指望它分辨 0.1mm 以下的差异；④卡在边缘时补救是关键格每折两次，不是加折数 |

### 🟡 实验（要跑训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **2** | **可见区域透传** | 最大 | ⭐ **唯一还在牌桌上的实验项。** 解码器只吃 max-pool 出的一个全局向量。⚠️ CD_t 会虚高，读结果以缺损区覆盖为准。⭐ 路由**写死、不需要学习**，不受注意力坍缩影响（那正是 `pp_attn` 的死因）。<br>⚠️ 它会是**第一个用新口径的 run**（patience 30 / epochs 600 / `--defect-every`），与 9 个老 run 对照必须走 `epoch_matched` |
| 3 | 数据增强（绕 SI 轴小角度旋转 + jitter） | 小 | 理由是"提升覆盖"，不是"防过拟合" |
| 4 | 平滑惩罚项 / **点到面引力** | 中 | 多尺度粗糙度度量已被否定；点到面引力与 repulsion 正交、可用 `signed_deviation` 验收 |
| 5 | 预训练权重微调 | 中~大 | ⚠️ 权重只认 `msn_demo_arch` 拓扑 |
| 6 | 真 FPS 采样 | 小~中 | ⚠️ 8/25 收窄：`paper()` 的 `E-SG1` 是 4096/4096 全选，真 FPS 在那儿是 **no-op**，**只影响 `E-SG2`** 一层。输入点云本身已是 FPS 采样，**预期收益已下调** |
| 7 | 模型容量中间值 | 小~中 | |
| 8 | 接入 SkullBreak | 大 | ⚠️ **未决定是否要做** |

### 🟢 评测（不用训练）

| | 内容 | 成本 | 备注 |
|---|---|---|---|
| **9** | Poisson 重建对照 | 半天 | ⭐ 唯一能回答"离临床多远"的路径。<br>⚠️ **前置步骤：先量「重建地板」再决定要不要报指标**（拿 GT 的点做 Poisson，和真正的 GT 网格比，全程无模型）。3mm 量级则白做，0.5mm 量级才谈得上和 AutoImplant 的 1.52mm 并排。<br>⚠️ 重建后的指标**不能替换主结果**（换表示方式会让地板消失、数字普遍变好看，那是 metric shopping）。主体报点云，Poisson 单列一节 |
| 11 | 粗糙度度量补进 `mesh_viz.py` | 半天 | ⚠️ 现有数字（GT 0.736 / pred 0.760）来自临时脚本、不可复现。**这是三个"临时脚本数字"里最后一个还欠着的** |
| **15** | ⏸ `attention_collapse.py --n 20` 重跑 | 20 分钟 | **只在论文要引 `tie_qk` D2 的具体数时才需要**。`eff_frac` 跨颅骨 std 中位 1.66e-07，12/16 与编码器数字 n=3 绰绰有余；但 `tie_qk` D2-STA3 是 0.058、`frac_starved` 在 STA4 上 27%±12%。⚠️ 若要跑，**在删那三份权重之前** |

### 📌 文档 / 论文

| | 内容 | 挂了多久 |
|---|---|---|
| **12** | ⏸ **最终基线要不要包含体素基线** —— 用户 8/25：**还在犹豫**。<br>"self-built corrupted dataset" 已取消（README 已改）；voxel baseline 这一半仍未定。<br>若做：把体素输出丢进 `prepare_skullfix.py` 同一条管线（marching cubes → 密集采样 → FPS 到 6144），两边共用采样地板才可比 | 8/06 |
| 13 | **采样地板必须写进论文** —— ✅ 已测量入库（**CD_t 4.619 ± 0.252、HD95 3.974 ± 0.214、单向 2.310 ± 0.126**）。<br>⭐ 本工作 CD_t 6.355 里 **73% 是地板**；缺损覆盖 3.236 离单向地板只剩 **0.926mm**。<br>剩下的是**写作**：写进第 4 章，并说明本文数值不可与体素域方法并排 | 8/20 |
| **14** | **训练配置与原实现的差异表必须写进论文** | 8/21 |
| **16** | ⏸ **epsilon 饿死那条要不要写进论文** —— 新增，见下方第 10 项。<br>它**不属于"自己写错的错误"**（`t/(1e-9+Σ)` 逐字来自原实现，已核实 `msn_demo_arch.py:126,150`），<br>但只在 `tie_qk` 这个**已否决**的配置上出现。写的话放第 3 章审计节、作为否决 `tie_qk` 的机制补充 | 8/26 |

### ⚙️ 已知但未修

**（空）** —— 2026-08-25 起保持为空。

### ✅ 已完成 / ❌ 已否决 / ⛔ 作废

**本批（8/26）**

- ~~**注意力坍缩自查写成脚本（原 10）**~~ ✅ **关闭。** `src/eval/attention_collapse.py`
  → `experiments_log/attention_collapse.csv`（3 run × 16 块 × 3 颅骨，合并写）。四条产出：
  - ⭐ **把「均匀」拆成两种**，这是最重要的产出。**STRUCTURAL**（`key_row_range` 恰好 0，
    key 逐行相同 → 不可能注意，**是架构事实不是发现**）vs **learned collapse**
    （keys 有区分度但 Q/K 没长出对比度 → **这才是「80 颗颅骨学不出 key 选择性」的证据**）。
    原来那句"16 块中 12 块坍缩"把唯一有内容的一半抹掉了。
    `cd_rep05_full` / `tie_qk` = 4 structural + 8 collapsed；`pp_attn` = **0 + 12**
  - ✅ **核心结论全部复现**：编码器八块 `eff_frac` = 1.000000 而 keys 明确不同；
    控制列（教科书 softmax）≥ 0.99999988 → **「softmax 轴选错」被堵死**；
    `tie_qk` 的 `energy_std` 大 5.1~12.3× 但仍不解除坍缩
  - ⚠️ **8/21 那张表两处作废**：`energy_std` 系统性高 1.20~1.49×（定义不同，结论不变）；
    **`D2-STA4` 的 39.7%/3.7% 复现不了**（新值 51.4%/19.9%），旧脚本没入库、无从对照
  - ⭐ **新发现**：`tie_qk` 让 **D2-STA2/STA3 的 92% query 行**被该块自己的 1e-9 epsilon
    饿死（行质量 0.079）。8/21 那条"D2-STA4 注意力大幅收窄，只是观测没有解释"
    **块号记错、机制也不是收窄**。⚠️ 但 `peak_x_unif` 302~666（对照 2.8~79），
    注意力**确实也真的更尖** —— 两件事同时发生，措辞要分清
- ~~**`tie_qk` / `cd_rep05_full` / `pp_attn` 权重的保留理由**~~ ✅ **已消除**，
  这三份 `best.h5`（约 2.1 GB）**现在可删**。⚠️ 除非要做第 15 项，那就先跑完再删

**此前**

- ~~zero-shot 措辞查证~~ ✅ 8/25 **不能写 zero-shot**（权重见过颅骨数据）。两个后果方向相反：
  ① 基线没吃亏 → 3.17× 的增益确实来自专精化；② ⚠️ 可能有训练/测试污染 →
  **本工作的领先是低估的**，论文要主动写明
- ~~"self-built corrupted dataset"~~ ❌ 8/25 取消，README 已改
- ~~`PROGRESS.md`~~ ❌ 8/25 弃用
- ~~早停口径（原 18）~~ ✅ patience 20→30、解耦 `--lr-patience`、新增 `--defect-every`
  （主指标终于进 `history.csv`）。⚠️ **诊断指标绝不参与选择**
- ~~文本分支机制（原 19）~~ ✅ 折叠验证证明「多余」那一半（输出差 4×10⁻³ mm）；
  达成率修正为 **46%**。「杠杆」仍是假说，判决性实验移交 future work
- ~~采样器不确定性（原 20）~~ ✅ 实测可忽略（CD_t std 0.030mm），不改代码
- ~~λ=2「梯度饱和」假说（原 21）~~ ⛔ 撤回（建立在⛔错误性实验上）
- ~~notebook 模块 reload（原 22）~~ ✅ 已堵住
- ~~`.npz` 路径写死（原 23）~~ ✅ 收成 `report.DATA_CACHE`
- ~~预训练基线补主指标（原 16）~~ ✅ **全点云差 1.69×，缺损区覆盖差 3.17×**。
  ⚠️ 缺损精度 1.06× 是假象：基线只往洞里放 174 个点，根本没填洞
- ~~`notext` 复核~~ ✅ 8/24 成立（两次差 0.0037mm = 效应的 1/47）。**保留文本分支**
- ~~Q/K 初始绑定 `tie_qk`~~ ❌ 8/24 否决。**仍要写进论文第 3 章**（与参考实现的差异 + 受控验证）
- ~~判读方法学~~ ✅ 三把尺子分开；`epoch_matched` / `paired_stats` 入库
- ~~噪声判据~~ ⚠️ 最终：三对重复里两对 ≲0.005mm、`tie_qk` 那对 0.15/0.23；**分水岭是曲线走没走平**
- ~~重写 vs 原实现的逐行审计~~ ✅ 8/21，唯一一处差异已由 `tie_qk` 闭环
- ~~repulsion loss~~ ✅ **全项目最硬的一条**（13.60% → 1.34%，效应是噪声的 50 倍）
- ~~DCD 消融（完整 2×2）~~ ✅ **最优配置是 CD + repulsion**
- ~~缺损区限定指标 + 掩码修正~~ ✅ **论文以缺损区覆盖为主指标**
- ~~逐点交叉注意力~~ ❌ 全面变差，注意力从未启动。**不写进论文**
- ~~仓库整理 / `--run-name` 覆盖保护 / `--from-run`~~ ✅ 8/24
- ⛔ ~~`ReduceLROnPlateau` 配置修复~~ 不是研究发现，不写进论文
- ⛔ ~~`dcd_weight` / `dcd_lambda` 调参~~ 作废（建立在错误性实验上）
- ~~正则化~~ ❌ 模型不过拟合，代码已删
